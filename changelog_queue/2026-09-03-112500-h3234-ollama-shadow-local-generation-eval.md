# H3234 (этапы 5–6 + eval поверх dense-ноги H4001): ollama_shadow рядом с OpenRouter, BOT_LOCAL_GENERATION с деградацией без DeepSeek, knowledge:eval (OxAlpha z-ai/glm-5.3-flash, 03-09-2026)

Дельта H3234 поверх слитой в тот же день dense-ноги H4001 ([PR #2351](https://github.com/gasyoun/Systema-Sanscriticum/pull/2351)) — реюз `config/knowledge.php` (заметка H4001 «H4002 переиспользует этот файл для теневой генерации» исполнена), второй конфиг/вторая реализация ретривера не заводились. Гейт эксперимента (узел Ивана) подтверждён 03-09 дважды: 10:16 и 11:15 UTC — `qwen3:14b` + `bge-m3:latest` за sshd-туннелем.

- **Этап 5 — теневая генерация** ([OllamaShadowReplyJob](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Jobs/OllamaShadowReplyJob.php), флаг `BOT_OLLAMA_SHADOW` default OFF): после УСПЕШНОГО OpenRouter-ответа CuratorAi диспетчит Horizon-job с ТОТ ЖЕ массивом сообщений ([CuratorAi::messagesFor](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Bot/CuratorAi.php) — единственная точка сборки промпта, тень видит байт-в-байт то же, что ушло наружу); job прогоняет его через локальный qwen3:14b и пишет `SupportAiReplyEvent(ollama_shadow)` рядом с онлайн-логом (status ok|error, preview 240, model, usage, question_preview) — неделя живого сравнения до флипа этапа 6. Онлайн-путь студенту не меняется: тень НЕ блокирует и НЕ заменяет ответ (неудача узла → событие error, без исключений наружу).
- **Этап 6 — локальная генерация** (`CuratorAi::localReply/localChatWithUsage`, флаг `BOT_LOCAL_GENERATION` default OFF): Ollama отдаёт OpenAI-совместимый `/v1/chat/completions` на `knowledge.base_url`, модель `knowledge.generation_model` — смена провайдера сменой конфига, без правки класса (закреплено тестом URL/payload). ГЛАВНОЕ (текст issue): **откат в OpenRouter/DeepSeek в локальном режиме запрещён** — туннель умер → `reply() = null` → контроллеры отвечают детерминированно («позови куратора»), ни один байт не уходит наружу; тест фейкает OpenRouter заветным ответом и доказывает, что он не вызван. `KNOWLEDGE_GENERATION_TIMEOUT` (120 с) отделён от 5-секундного request-path таймаута эмбеддингов D2: генерация живёт в Horizon-job/после ответа, длинный таймаут там безопасен.
- **Этап 2 — `knowledge:eval`** ([KnowledgeEvalCommand](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/KnowledgeEvalCommand.php)): печатная таблица recall@K / MRR для BM25 (базовая линия) и гибрида — РЕЮЗ тех же `Bm25FaqRetriever` и `HybridRetriever` H4001, второй реализации скоринга нет. Дефолт `--evalset=tests/fixtures/faq_rag_eval.json` (100Q H3766 B5), `--evalset=tests/fixtures/faq_rag_eval_fresh.json` — fresh 20Q. Честные оговорки: флаги lane'а включаются in-process на время замера (команда меряет МЕХАНИКУ, не состояние прод-флагов); при недоступной dense-ноге (проба пуста/бросила) гибрид замеряется как BM25-пол — таблица показывает равенство, а не ложный выигрыш; живая гибридная таблица возможна только на проде с `KNOWLEDGE_EMBEDDING_DRIVER=ollama` и проиндексированным корпусом.
- **Dual-run заметка:** стартовый план прохода (собственная схема `knowledge_chunks` c `source_type`/`audience`, свой `App\Services\Knowledge`-неймспейс) выброшен после обнаружения слитого H4001 — same-day параллельная сессия построила ту же dense-ногу из Wave 3 leverage-плана. Собственный каркас удалён целиком (миграция/модель/провайдеры/ретривер/индексатор/тесты-дубли), дельта пересобрана поверх main; `App\Models\KnowledgeChunk` остался в единственном экземпляре H4001. Этап 4 (лекции) требует расширения схемы `source_type`/`audience` — осознанно не начат, чтобы не плодить третью схему поверх двух слитых.
- Тесты: `tests/Feature/Knowledge/` — KnowledgeShadowGenerationTest (6: диспетчerral рядом с онлайн-логом, флаг off, статус ok/error, промпт тени ≡ промпту онлайн), KnowledgeLocalGenerationTest (5: локальный ответ без утечки в OpenRouter, туннель умер → null без fallback, /v1/chat/completions контракт, флаг off → прежний путь, пустой ответ → null), KnowledgeEvalCommandTest (3: таблица + BM25-пол + приёмка на 100Q, fresh 20Q, громкий отказ без набора). Фильтр `--filter=Knowledge` зелёный целиком (H4001-набор + 11 новых), полный прогон: 4 фейла, все 4 воспроизводятся на чистом main (GrammarLab import search, Shop pretty URLs, Cabinet guide screenshots, Vitrina waitlist — pre-existing). Pint clean.
- **Осталось (операционная фаза, не код — @DO в GTD):** деплой → `KNOWLEDGE_EMBEDDING_DRIVER=ollama` + `knowledge:index` на проде → `FAQ_HYBRID_RETRIEVAL=true` → `BOT_OLLAMA_SHADOW=true` → неделя живой тени → `knowledge:eval` вердикт ≥ BM25 → человек флипает `BOT_LOCAL_GENERATION` до 01-10-2026 или оставляет OpenRouter.
