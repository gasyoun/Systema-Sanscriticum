- support/telegram: волна 1 leverage-плана ЛС (H3999) — резолверов фактов стало восемь вместо трёх: к ссылке/расписанию/записи добавлены статус ДЗ, остаток по оплате, состояние доступа, сертификат и изменения расписания (`SupportAnswerFactResolver`, каждый возвращает `null`, а не выдуманный ответ, когда записи нет). Решение «отправить» больше не выводится из категории: его принимают ДВЕ независимые вещи — политика самого ответа (`send_policy`) и список живых типов `support.facts.live_types` (дефолт ровно те три типа, что уходили до H3999, — трафик студентам байт-в-байт прежний). Деньги, доступ и сертификат закрыты для автоотправки НАВСЕГДА и В КОДЕ (`NEVER_AUTO_TYPES` вычёркивается из живого списка, `SupportAnswerSuggestion::isDraftOnly()` перепроверяет тип на самом черновике) — правка конфига запрет не снимает. Расхождение расчётного остатка с суммой, названной студентом, не отвечает ничем: событие `dm_balance_dispute`, follow-up финансовому ведущему (`support.escalation.finance_lead_user_id`), черновика под кнопку не создаётся вовсе. Кнопка «отправить как есть» выросла с одного FAQ-источника до трёх (факты → FAQ → шаблон категории) — и факты теперь ВЫШЕ FAQ: на «сколько мне осталось доплатить» ретривер уверенно доставал статью «Сколько длится обучение» и подменял ею расчёт по кабинету. Новая страница Filament «Очередь черновиков» рядом с «Аналитикой» (`features.support_draft_queue`, default OFF): Отправить / Изменить / Пропустить, отправка тем же `SupportHintSendButton::deliver()`, что и кнопка в Telegram; правка оставляет черновик pending и закрывает его статусом «отредактирован». SLA-сеть `support:sla-escalate` каждые 5 минут (`features.support_sla_escalation`, default OFF): пинг куратору на 15 рабочих минутах, второму — на 60, тихие часы 22:00–09:00 МСК ПАУЗЯТ накопление (`App\Support\SupportSlaClock`, сознательно НЕ `config/support_hours.php` — тот питает виджет сайта), кураторы из `support.sla.curators`, каждый пинг через клейм `TelegramSendGuard`, студенту не уходит ничего. Недельная сводка `support:link-invite-census` (вс 18:20 МСК) печатает числитель рядом со знаменателем и называет только идентификаторы чатов — сопоставления по телефону или @username нет нигде. Выходные данные волны: `support:fact-shadow-replay --days=30` — сколько вопросов ЛС закрылись бы фактами кабинета, по категориям и типам, read-only. Схемные находки, вскрытые тестами: `homework_submissions` уникальна по (user_id, lesson_id); `certificates.issued_at` — date NOT NULL без каста, поэтому «ещё не выдан» существует только как дата в будущем; прежнее время занятия в `schedules` не хранится, поэтому черновик говорит «изменилось — сейчас в расписании», а не «перенесено». Тесты: +64 в `tests/Feature/Support` (418 → все зелёные), время в каждом пинуется `Carbon::setTestNow`; Pint clean; `docs/ENVIRONMENT_VARIABLES.md` перегенерирован тем же проходом (11 новых ключей). Живые типы фактов и оба флага остаются OFF до недели тени и человеческого решения по точности (Opus 5 `claude-opus-5`, 06-09-2026, H3999)
