_Created: 09-09-2026 · Last updated: 09-09-2026_

# H4448: storefront_events.variant больше не пуст на next-step полосе — оба record-call-site продевают CTA A/B-вариант (process-mining wave 2, OxAlpha z-ai/glm-5.3-flash, 09-09-2026)

Wave-1 process-mining ([отчёт 08-09-2026](https://github.com/gasyoun/Uprava/blob/main/reports/PROCESS_MINING_BUSINESS_TRIO_08-09-2026.md)) дал вердикт NOT MEASURABLE: все 73 747 строк `storefront_events` несут `variant=null`, любое a/b-сравнение по ним — fabricated. `FlagshipExperiments::ctaVariant()` (H2010 house pattern: cookie `shop_cta_ab`, 50/50 `random_int`, assign at first hit, TTL 240 дней) существовал, но два call-site'а эксперимента `next_step` его не прокидывали.

- **[recordCardImpression](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Support/FlagshipExperiments.php)** — `card_impression` теперь пишет `variant: self::ctaVariant($request)`: на первом рендере каталога вариант назначается и уходит cookie, строка импрессии несёт его же; `dedupeDay` не тронут (ключ дедупа по-прежнему event/experiment/visitor/course/target — вариант в дедуп не входит, повторный рендер того же дня молчит независимо от варианта).
- **[StorefrontAnalyticsController::nextStep](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Http/Controllers/StorefrontAnalyticsController.php)** — `next_step_click` пишет `variant: FlagshipExperiments::ctaVariant($request)`: клик несёт тот же вариант, что посетителю показан (cookie уже стоит от импрессии/CTA-лендинга); на прямом заходе без cookie назначается тем же механизмом — второй схемы назначения нет.
- **Не менялось**: `sample_play`/`begin_checkout` (там уже был read-only `ctaVariantFromRequest` с H2762), схема таблицы, `countWindow`, флаги (`CATALOG_NEXT_STEP`/`FLAGSHIP_CTA_AB` остаются OFF до отдельного деплой-решения).
- **Тесты**: [tests/Unit/FlagshipExperimentsTest.php](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Unit/FlagshipExperimentsTest.php) +4 (first-hit назначение + cookie queue с тем же значением; существующая cookie `'b'` переносится без пере-назначения; дедуп дня держит 1 строку при разных вариантах; flag-OFF не пишет ни строки, ни cookie) и [tests/Feature/Shop/FlagshipExperimentsTest.php](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Shop/FlagshipExperimentsTest.php) +2 (клик с cookie `'b'` → строка `'b'`, без Set-Cookie; клик без cookie → строка a/b и `assertCookie(shop_cta_ab, variant)` на redirect) + вариант-ассерт в импрессионном тесте. `vendor/bin/phpunit --filter=FlagshipExperimentsTest` — 20/20; полный сьют 5452 tests — 4 fail, все 4 воспроизводятся на чистом origin/main без изменений (пре-существующие, вне полосы Flagship/Storefront). Pint clean.

_Dr. Mārcis Gasūns_
