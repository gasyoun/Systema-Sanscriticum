### Changed

- **No-dev parity gate расширен до HTTP, queue и console/scheduler швов: `scripts/prod_runtime_parity_check.php` + шаг `production-profile` job (OxAlpha `zai-coding-plan/glm-5.3-flash`, 18-09-2026, H5095).** Тот же самый gate из H5060/PR #2676 — без параллельного профиля — теперь после boot с кэшированным конфигом (`config:cache`) дополнительно прогоняет по одному представительному шву каждого runtime-входа, который может грузить классы после старта приложения: **HTTP** — `GET /sanskritorium` через настоящий HTTP-kernel (роутинг → middleware → контроллер → Blade → 200; фикстура stub-Vite-манифеста с byte-exact восстановлением); **queue** — реальный `TrackLessonViewJob` (сам встаёт в очередь `tracking`) через `dispatch` → сериализацию payload → `queue:work --once`: pop → decode → container-resolve → `handle()` выполнен до завершения на мигрированной sqlite-фикстуре; события читают `payload.data.commandName` (а не имя враппера `CallQueuedHandler@call`); guard по классу отказа: fixture-SQLSTATE допустим, `Class ... not found` — hard fail; **console/scheduler** — `anons:validate` (контейнер-инъекция fail-closed валидатора над тем же JSON-манифестом) и `schedule:list` (99 запланированных команд резолвятся под no-dev). Фикстура БД — свежая мигрированная disposable sqlite по пути из кэшированного конфига (консольный boot реально читает `marketing_settings`; прод грузится на мигрированной БД). Инвентарь runtime-входов с обоснованием и явными исключениями (telegram/zoom/madelineproto/openrouter, geo/ollama/n8n, money/checkout-роуты, backfill-команды) — в шапке скрипта. Приёмка: baseline 10/10, расширенный gate 16/16 GREEN на PHP 8.3.33 (= CI); посаженный dev-only `symfony/yaml` в `AnonsValidateCommand::handle()` → RED exit 1 с actionable-диагностикой («missing runtime class [Symfony\Component\Yaml\Yaml] … locate: composer why-not»); восстановленное дерево снова 16/16; Pint чист. Без секретов и сети; JSON-at-runtime контракт, прод-креденшалы и deploy-поведение не тронуты.
