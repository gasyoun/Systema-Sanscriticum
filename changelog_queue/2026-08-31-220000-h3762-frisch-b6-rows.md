- **Issue #2265 закрыт: последние 2 омоглифные строки «Фриша» починены — рулинг MG «⟨б⟩ = цифра 6» (Fable 5 `claude-fable-5`, 31-08-2026).** `⟨б⟩` в `/расб/` (112501) и `/sadб/` (115974) оказался печатной цифрой класса корня, съеденной OCR; в поле остаётся голый корень без цифры — как у соседних строк (`/paṇ/`): `/pac/ /pacati/ /pacate/` и `/sad/ /sīdati/ /sīdate/ /satta/ /sanna/`, все три колонки (деванагари `पच्`/`सद्` сверена с канонической `sanskrit-util`), слаги `rasb-…`→`pac-…`, `sadb-…`→`sad-…`. Карта [`slovar:fix-frisch-homoglyphs`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/FixFrischHomoglyphs.php) теперь покрывает все 16 строк переписи; уже починенные 14 при полном прогоне честно отказывают (recompute-at-apply-time), частичная пачка — `--only`. Перепись `iast REGEXP '[а-яА-ЯёЁ]'` на проде после прогона: **0**. Тесты: `FixFrischHomoglyphsTest` 7/7.