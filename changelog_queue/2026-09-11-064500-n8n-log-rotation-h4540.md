_Created: 11-09-2026 · Last updated: 11-09-2026_

# H4540: ротация docker-логов n8n-контейнера в managed override (tpl + живой файл синхронно) (OxAlpha z-ai/glm-5.3-flash, 11-09-2026)

Закрывает остаточный риск после ENOSPC 10-09 ([H4513](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4513-OxAlpha_Systema-Sanscriticum_n8n-zoom-cover-bypass-and-infra-verdict_10.09.26.md)): у n8n-контейнера на .91 `json-file` шёл без `max-size` — docker.log рос неограниченно.

- **[docker-compose.override.yml.tpl](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards_n8n/docker/docker-compose.override.yml.tpl)**: сервису n8n добавлен блок `logging: json-file, max-size "50m", max-file "5"` (значения — как у caddy). Статические строки без @@VAR@@, поэтому рендер байт-в-байт равен живому файлу — следующий `server_guards_apply.sh --profile n8n` не увидит drift.
- **Живой файл** `/opt/n8n/docker-compose.override.yml` пропатчен синхронно (бэкап `.bak-11.09` рядом), контейнер пересоздан `docker compose up -d`: `docker inspect` подтверждает `{"max-file":"5","max-size":"50m"}`, healthz 200, executions 431→431.
- **Та же проходка (на .91, вне репо):** SQLite VACUUM `database.sqlite` 166.5→130.6 МБ, freelist 8720→0 (бэкап `/root/backup-sqlite-11-09.sqlite`, integrity ok); удалён тег-обманка `n8nio/n8n:latest` (пин 2.27.5 не тронут); убит зависший с 29-08 `rclone config update gdrive` (PID 1206230) — ротация Google OAuth вынесена human-остатком в [GTD](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md).
_Dr. Mārcis Gasūns_
