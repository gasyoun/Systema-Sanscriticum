_Created: 11-09-2026 · Last updated: 11-09-2026_

# H4590: samskrte.ru guard set — git-целостность дерева, tamper-watch облика, дрейф ростера (OxAlpha zai-coding-plan/glm-5.3-flash via opencode, 11-09-2026)

Уроки разбора samskrtam95 ([H4570](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4570-OxAlpha_Uprava_samskrtam95-hijack-restore-quarantine-ovawp_11.09.26.md), [RESULTS](https://github.com/gasyoun/Uprava/blob/main/docs/RESULTS_SAMSKRTAM95_HIJACK_RESTORE_QUARANTINE_11-09-2026.md)) перенесены на .92 тремя управляемыми стражами — не пересборка уже стоящих BACKUP+FAIL2BAN+AUTOUPDATE, а ровно те три слепые зоны, которые на samskrtam дали имплантам жить неделями.

- **Git-целостность** ([systema-git-integrity.sh](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards/sbin/systema-git-integrity.sh), 03:21 UTC): `/var/www/html` — git-дерево прода, но никто не смотрел на его дельту. Суточный `git status --porcelain -z` + `git diff HEAD`: tracked-изменения (M/D/A/R) — подозрение всегда; untracked сверяются с базисом «законно грязного» [expected-dirty.conf](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards/expected-dirty.conf) (48 `.env.bak*` + `storage/` + известные артефакты, снято с живого дерева 11-09: 57 записей, 0 tracked-dirty). Не совпало — FAIL, exit 2, P1 в дайджест. `git -c safe.directory=` — иначе root в www-data-дереве слепнет на «dubious ownership».
- **Tamper-watch облика** ([systema-tamper-watch.sh](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards/sbin/systema-tamper-watch.sh), 03:33 UTC): 200 + маркер заголовка главной, глубокий маршрут `/login`, TTFB (warn ≥3 с, тревога ≥10 с — урок «25-секундные волны жили неделями»), конец TLS-сертификата <14 дн, диск >85 %. `--test-url` — оффлайн down-test для приёмки.
- **Дрейф привилегированного ростера** ([systema-admin-roster.sh](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards/sbin/systema-admin-roster.sh), 03:44 UTC): суточный sha256 среза `users` — привилегия AUTO-DETECT по колонкам (`is_admin=1 OR role IN (super_admin,admin,teacher,manager,accountant)`; `model_has_roles`/`roles` в схеме нет, 1050 users, привилегированное подмножество). Дрейф = P1 + ОБА состояния в `admin_roster.prev.txt`/`.cur.txt`. Руж-админ `ova_wp` на samskrtam жил 4 дня — здесь ловится сутками. Zero записей в живые данные.
- **Проводка**: манифест (`manifest.psv` +4 строки) + cron-строки в управляемом [root.crontab](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards/cron/root.crontab) + числа/URL в `server_guards.conf`; всё ставится `server_guards_apply.sh`, проверяется `guards:verify`. Тем же проходом вылечен дрейф: живую строку `samskrtam_hijack_heal.py` (H4570) первый же apply стёр бы — она внесена в шаблон.
- **Дайджест**: Hermes-foreman ([Uprava PR](https://github.com/gasyoun/Uprava/pulls)) несёт три строки `GREEN/FAIL` + пульс по `lanes.tsv` (P1). Приёмка: `guards:verify` GREEN с новыми строками; по одному намеренному тамперу на стража поймано (untracked-файл в дереве, мёртвый маршрут оффлайн, тестовая привилегированная строка insert+delete); дайджест несёт все три GREEN-строки; живые данные не тронуты.
- **Живой прогон поймал собственную ловушку**: `set -o pipefail` + `printf … | grep -q` — ранний выход grep по первому совпадению шлёт printf SIGPIPE (141), и маркер заголовка «не находился» при живом совпадении (7 вхождений в теле). Обе такие проверки (маркер главной, операционная ошибка среза ростера) переведены на here-string; там же убран косметический обрыв метки глубокого маршрута.

_Dr. Mārcis Gasūns_
