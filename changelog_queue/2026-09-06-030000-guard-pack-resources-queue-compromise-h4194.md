_Created: 06-09-2026 · Last updated: 06-09-2026_

# H4194: .92 guard pack — RAM/swap/load + queue-lag + compromise-integrity (OxAlpha z-ai/glm-5.3-flash, 06-09-2026)

SLI-3 mandate ([AGENT_FLEET_SLO_2026.md §1](https://github.com/gasyoun/Uprava/blob/main/docs/AGENT_FLEET_SLO_2026.md)): three surfaces `cabinet:probe` never reads. Three new `--dry`-capable artisan commands, `CheckStorageUsage.php` idiom (Filament DB notification to admins, no new Telegram wiring): [`guards:resources`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/CheckResourceGuards.php) (mem avail / swap used / load1 vs [`config/guard_pack.php`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/config/guard_pack.php) thresholds derived from the L1/L8 incident facts), [`guards:queue-lag`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/CheckQueueLag.php) (pending count + oldest-job age per queue from `jobs`, CRITICAL > 30 min), [`guards:compromise-integrity`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/CheckCompromiseIntegrity.php) (admin-count baseline + webroot `.php` inventory baseline, `--write-baseline` is the only way to accept new state — never auto-updated by the probe). Cron template updated ([`scripts/server_guards/cron/app-user.crontab`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards/cron/app-user.crontab) + `WATCHDOG_RESOURCES_*`/`WATCHDOG_QUEUE_LAG_*`/`WATCHDOG_COMPROMISE_*` in [`scripts/server_guards.conf`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/server_guards.conf)) as three separate `systema-watchdog-run.sh` cron lines — same "guard must not share the scheduler's fate" reasoning as `cabinet:probe`/`heartbeat:ping`. **Install stays MG-gated**: [`docs/GUARD_PACK_INSTALL.md`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/GUARD_PACK_INSTALL.md) names the exact `root@.92` SSH steps, parked as a GTD `@DO` row.
- Tests: [`tests/Feature/GuardPackTest.php`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/GuardPackTest.php) — 15 cases (healthy-silent + `--dry` + each alert path per command), synthetic `/proc/meminfo`/`/proc/loadavg`/`/proc/cpuinfo` fixtures via the existing `FakeSystemInspector`, synthetic `jobs` row, temp baseline files. Full `ServerGuards*`/`Guard*` suite (130 tests) green after the conf/cron template edits. Pint clean.

_Dr. Mārcis Gasūns_
