- **Вебчат samskrte.ru перестал молчать в первые 45 секунд: ack + FAQ-автоответ за двумя default-OFF флагами (OxAlpha `zai-coding-plan/glm-5.3-flash`, 24-09-2026, [H5450](https://github.com/gasyoun/Uprava/blob/main/handoffs/H5450-OxAlpha_Systema-Sanscriticum_support-webchat-45s-first-response_24.09.26.md)).** Замер 24-09: после отправки сообщения в виджет — ноль автоматики до человека (`PublicChatController::store` только писал в тред), а порог T-Бизнеса — 45 с. Новый [`SupportWebchatAutoReply`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Support/SupportWebchatAutoReply.php): 1) мгновенный ack-сообщение бота «приняли, куратор ответит» + ссылка на FAQ — один за cooldown-окно треда (паттерн ack'а H3380), флаг `SUPPORT_WEBCHAT_AUTO_ACK`; 2) живой FAQ-ответ по категории F выше того же BM25-порога `support.faq_rag.shadow_min_score*`, что и в TG-полосе H3768, с цитатой раздела — флаг `SUPPORT_WEBCHAT_LIVE_FAQ`; уверенный FAQ-ответ замещает ack. Деньги (D) и доступы (E) вычеркнуты В КОДЕ (R3): конфиг может только сузить живые категории до жёсткого allowlist {F}; гостевой тред не получает фактов LMS вовсе. Телеметрия — `dm_auto_sent`-события с `via=support_webchat_auto_reply` (kind ack/faq_rag, chunk_id, score, floor, guest), недельный отчёт `support:auto-reply-weekly` показывает их отдельной строкой «Веб-чат (samskrte.ru)», TG-агрегаторы считают только TG. При OFF ответ эндпоинта байт-в-байт прежний (ключ `auto_replies` не добавляется). Виджет подписывает bot-сообщения меткой «Бот». Флип флагов на проде — `@DO` человека (R9/R10). Тесты: [`SupportWebchatAutoReplyTest`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Support/SupportWebchatAutoReplyTest.php) 13/13 (OFF-инвариант, ack-один-на-серию, F-ответ с цитатой, D/E-запрет, гостевой тред, веб-срез отчёта), Support-сьют 499 passed/1 skipped, `tests/Unit` 526 passed, Pint чист.
