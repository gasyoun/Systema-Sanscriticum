- **H3765 wave A (Opus 5 `claude-opus-5`, 31-08-2026): DM-полоса поддержки — диагноз stale-пропусков, расщеплённый потолок свежести, теневая неделя автоответа, кнопка «Отправить как есть».** Перепись A1 ([`support:ingest-latency-report`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/SupportIngestLatencyReport.php), читающая) **опровергла посылку рулинга R2**: 1070 stale-пропусков — не утечка живого трафика, а дозабор истории диалогов. Лаг обработки от приёма до события — **0.0 мин** (конвейер успевает), а 97 % пропущенного старше суток (814 — старше 30 дней, вплоть до 2022 г.); в полосе 6–24 ч, которую вернул бы широкий потолок, лежат **30** сообщений за 30 дней при ~7 живых входящих в день. Такт синка исправен — чинить было нечего. A2: потолок расщеплён на `telegram_support.auto_reply_max_age_hours` (6 ч, исходящее СТУДЕНТУ) и `telegram_support.hint_max_age_hours` (24 ч, подсказка КУРАТОРУ) — ставка на устойчивость к перебоям приёма, а не на объём; подсказка на полежавшем вопросе помечается «⏳ вопрос пролежал дольше 6 ч», устаревшее «Намасте» не отвечается и не хинтится. A3: флаг `SUPPORT_DM_AUTO_REPLY_SHADOW` (default OFF) пишет событие `dm_shadow_would_send` (скор, chunk_id, черновик, хэш вопроса) для безопасных категорий A/B/C выше `support.faq_rag.shadow_min_score` — **ни одного лишнего исходящего студенту** (инвариант §5 контракта плана, закреплён тестом). Провизорный порог 8.0 измерен на [`faq_rag_eval.json`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/fixtures/faq_rag_eval.json): полосы скоров верных (6.5–18.1) и неверных (6.3–12.1) top-1 **пересекаются**, обещанных R3 95 % точности на 20 вопросах не даёт ни один порог — настоящую калибровку выводит B5 на 100 вопросах. A5: подсказка куратору несёт inline-кнопку «📨 Отправить как есть» ([`SupportHintSendButton`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Support/SupportHintSendButton.php)) — тап проверяет, что нажал получатель подсказок этого аккаунта, и ставит черновик студенту через существующий `queueAiReply` под клеймом `TelegramSendGuard`; новой точки отправки в Telegram не появилось. Черновик старше `support.hint_send_button_max_age_days` (7) отвечает отказом и помечается expired — суточный сброс статусов остаётся аудитом, а не защитой. A4: [`support:shadow-report`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/SupportShadowReport.php) сводит «отправил бы» с реальным ответом куратора в окне 48 ч и раскладывает по полосам скора, категориям и «тишине»; на пустых данных отчёт прямо отказывается выглядеть убедительным. Живое включение автоотправки остаётся решением человека (R9/R10). Тесты: `SupportDmFreshnessCeilingTest` (+5), `SupportDmShadowModeTest` (+6), `SupportHintSendButtonTest` (+5), `SupportWaveAReportsTest` (+5); полный прогон `tests/Feature/Support` + `tests/Feature/Bot` — 362 зелёных.
