_Created: 05-09-2026 · Last updated: 05-09-2026_

# H4188 (контур «Расходы ИП» в LMS): Filament-панель + импорт книги «Расходы по ИП» + owner-draw реестр (OxAlpha z-ai/glm-5.3-flash, 05-09-2026)

Исполнение [H4188](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4188-OxAlpha_Systema-Sanscriticum_lms-expense-book-panel_05.09.26.md) по рулингу MG 05-09 (H4174-кластер): Google-книга «Расходы по ИП» уходит на пенсию, расходный контур переезжает в админку. Отдельная от opex (`Expense`) сущность: книга зеркалит легаси-расходы CRM за Oct'25–May'26 (двойной счёт, AUDIT_BOOKKEEPING_MISERABLE_MAP §3) — слипать в одну таблицу/enum до решения @DECIDE о сверке нельзя.

- **`ip_expenses`** (миграция `2026_09_05_200000`): spent_at (NULL допустим — в книге есть строки без даты), payee, amount decimal(12,2) RUB, currency, fx_note («400 евро PayPal» из счёт-колонки), account, category (`App\Enums\IpExpenseCategory`: налоги/банк/эквайринг/подрядчики/зп/прочее), note, source_tab (провенанс вкладки), import_hash unique (идемпотентность импорта). **Append-only** (конвенции payment_audits, миссия п.1): удаление запрещено в модели (RuntimeException) + ресурс без Delete/bulk-delete; правки аудируются.
- **`ip_expense_audits`**: append-only лог imported/created/updated/deleted (снапшот имени админа, `updated` → {field: [old,new]}) — `IpExpenseAuditObserver` по образцу `PaymentAuditObserver`, CLI-действия = «Система».
- **`ip-expenses:import-book {path} {--apply}`** ([ImportIpBookExpenses](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/ImportIpBookExpenses.php)): источник — снапшот скрейпера (manifest.json + raskhody-ip/gid\*.jsonl, на этой машине Uprava `data/money_sheets/2026-09-05/`). Dry-run по умолчанию; паритет-гейты до записи: Σ строк bcmath-Decimal-exact == manifest Σ (включая «Итого»-строку вкладки — скрейпер так считал), число jsonl-строк == manifest rows, sha256 csv — мягкое предупреждение (блобы после LF-политики H2004 байт-в-байт с оригиналом Google не совпадают в принципе). «Итого:»-строки и memo-строки без суммы в импорт не попадают («Итого rows excluded»). Дубликаты строки внутри вкладки сохраняются (occurrence в import_hash), не слипаются.
- **Реальные цифры импорта 05-09** (evidence-прогон на снапшоте 2026-09-05): 10 вкладок, 377 строк записано, Σ по месяцам **байт-в-байт совпадает с AUDIT_BOOKKEEPING_MISERABLE_MAP §3** (окт 1 003 511,07 … июль 691 503,42), 10 «Итого»-строк пропущено, 18 строк без даты Σ 1 116 935,37 (ровно undated-строка аудита), повторный прогон — 0 новых. Расхождение с «464 data rows» из миссии: 464 считало csv-строки с заголовками/легендой; в снапшоте 387 строк с суммой, из них 377 расходов + 10 служебных «Итого».
- **`owner_draw_liabilities` + `owner_draw_payments`** (миграция `2026_09_05_200100`, миссия п.3/4): посев реестра 05-09 (февральская нота — EUR 1 606,76 / USD 38,00 / RUB 44 183,19, fixed_at 28-02-2026), пара «выплачено/остаток» (paid + remaining = principal − paid), выплаты — append-only записи с датой и ссылкой (update/delete запрещены моделью), остаток пересчитывается обсервером. Источник: `Uprava data/money/OWNER_DRAW_LIABILITY_REGISTRY_05-09-2026.md`.
- **Filament**: [IpExpenseResource](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Filament/Resources/IpExpenseResource.php) «Расходы ИП» (Финансы, FinanceAccess) — месяц-фильтр как эквивалент вкладок книги, фильтр «Без даты», Σ-summarize, create-форма минимальна для MVP-ввода с телефона в тот же день (дата default сегодня, maxDate-гард H2008); [OwnerDrawLiabilityResource](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Filament/Resources/OwnerDrawLiabilityResource.php) «Долг перед владельцем» с infolist-парой «выплачено/остаток» и append-only relation manager выплат.
- **Границы (по хендоффу)**: деплой human-gated, прод-импорт и прод-записей в этом проходе НЕТ (evidence — локальный sqlite); сумма импорта в ОПиУ/ДДС-отчёты НЕ заведена (двойной счёт с raskhod-мостом остаётся до @DECIDE сверки); PR несёт маркер `money-contour: no-auto-merge` (напоминание о тестах, не запрет слияния).
- Тесты: IpExpenseImportBookTest (5: dry-run, поля/fx/категория/undated, идемпотентность+дубликаты, REFUSE на Σ-расхождении, sha-drift не отказывает), IpExpenseAuditAppendOnlyTest (3: delete запрещён, updated-дифф, CLI=«Система»), OwnerDrawLiabilityTest (3: посев, пара после выплаты, append-only) — 11 новых, green; соседи PaymentAudit/ExpenseBridgeRaskhod/AccountantAccess (18) green; route:list — оба ресурса зарегистрированы. Pint clean.

_Dr. Mārcis Gasūns_
