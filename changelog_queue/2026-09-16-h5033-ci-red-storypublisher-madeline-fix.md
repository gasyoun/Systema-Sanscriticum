### Fixed

- **H5033: fixed `origin/main` CI red — `StoryPublisher.php` Pint violation + `MadelineClientFactory` stub signature drift, closes [#2623](https://github.com/gasyoun/Systema-Sanscriticum/issues/2623).** Both required checks (`PHP 8.3 — tests`, `MySQL 8.4 — finance/webhook transactions`) had been failing on `main` itself since an unrelated commit (`751b3fb6`) left a Pint violation in `app/Services/Stories/StoryPublisher.php`, and separately `App\Services\Telegram\MadelineClientFactory::open()` gained a second parameter (`?string $sessionPath = null`) that 10 test-double subclasses across 4 test files (`MadelineDaemonSupervisorTest.php`'s `SpawningFactoryStub`/`FailingFactoryStub`, `TelegramHarvestSyncTest.php` ×6, `SnapshotGroupRostersTest.php`, `TelegramZapisiRosterTest.php`) never picked up — a PHP fatal signature-incompatibility error under PHP 8.3/8.5's strict inheritance check. Issue #2623 named only 1 of the 10 occurrences; the rest were found via `grep -rl "extends MadelineClientFactory"` and fixed identically. Verified: `./vendor/bin/pint --test` clean, `php -l` clean on all 5 touched files, `phpunit --filter 'Story|Madeline|Telegram|Snapshot|Zapisi'` — 478 tests, 1595 assertions, all pass.
