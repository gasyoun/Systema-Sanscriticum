# H3964 (OxAlpha z-ai/glm-5.3-flash, 03-09-2026): user-сториз персоны @rusamskrtam + repeat-движок + виза анонимизации (Phase 2, flag OFF)

Phase 2 телеграм-автоматизации (рулинг MG 02-09-2026 «персона + канал»: сториз кладёт на СВОЙ профиль @rusamskrtam user-сториз, БЕЗ админ-прав канала; текстовые посты канала остаются Phase 1):
- **Полоса `lane`** (additive migration `2026_09_03_100000`): `channel` — канальные тексты (stories:publish-due, Phase 1, дефолт — поведение байт-в-байт), `persona` — user-сториз персоны. Без разделения текстовые строки забирали бы оба издателя.
- **`stories:publish-story`** ([StoriesPublishStoryCommand](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/StoriesPublishStoryCommand.php)): флаг `features.telegram_story_stories` (default OFF — прод-инертен, ноль HTTP, MadelineProto-сессия не открывается вовсе); издаёт approved+due строки lane=persona (text/photo/video) через [StoryPublisher](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Stories/StoryPublisher.php) — ЕДИНАЯ легаси-сессия (= аккаунт @rusamskrtam, H3380) под madeline-session-локом + watchdog'ом, TTL из `services.telegram_story.stories_timeout_seconds` тем же madelineSessionLockMinutes(); текстовая сториз — `inputMediaEmpty`+caption, фото — `inputMediaUploadedPhoto`, видео — `inputMediaUploadedDocument` (documentAttributeVideo), peer «me», period 24 ч; режимы `--test-text` (отправил+удалил тем же кодом), `--probe-attempts=N` (потолок 30: снять дневной лимит по первому FLOOD, аккаунт общий с поддержкой — не штормить), `--delete-story=ID` (уборка артефактов).
- **Repeat-движок** ([StoryRepeatEngine](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Stories/StoryRepeatEngine.php)): `repeat_rule {every_days: N, times: M}` — после публикации (ОБОИХ издателей; Phase 1 колонки уже были) перепланирует копию (approved, publish_at=now+N дн., source_key=null против unique-конфликта) пока серия не выйдет на M публикаций; инкремент repeat_count — единственная точка правды `markPublished()`.
- **Виза анонимизации** (гейт human:MG, ASK_BATCH_CONTENT_FACTORY_TELEGRAM_2026 §2.8): флаг `features.telegram_story_student_media_visa` (default OFF) — строки source=homework скипаются издателем с журналом, никогда не публикуются до визы на правило blur/crop лиц и имён.
- **Источники (кураторский минимум, без DM-автоимпорта)**: экшен «В сториз» в [HomeworkSubmissionResource](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Filament/Resources/HomeworkSubmissionResource.php) (админ, только ПРИНЯТЫЕ работы, фото/видео → черновик через [StoryPromotionService](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Stories/StoryPromotionService.php) с копией файла в storage app/telegram-story/media, дедуп по homework-file id) + `stories:from-harvest` ([StoriesFromHarvestCommand](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/StoriesFromHarvestCommand.php): `--list` переписывает media_local_path из OUT-OF-GIT raw-стора только чтением, завод черновика дедупится md5(пути), неизвестное расширение требует явного --kind).
- **Filament StoryPostResource**: выбор полосы (канал/персона), поля repeat_rule (каждые N дней × всего M), колонка «Полоса».
- **Kernel**: ежечасный `stories:publish-story` рядом со stories:publish-due, `withoutOverlapping(madelineSessionLockMinutes(stories_timeout_seconds))`, onOneServer.
- Тесты: StoriesPublishStoryTest (10) + StoriesPublishDueTest (+3) + StoriesFromHarvestTest (3) + StoryPromotionServiceTest (4) — 24 passed, Pint clean; MadelineProto-двойник [FakeStoriesMadelineProtoClient](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Doubles/FakeStoriesMadelineProtoClient.php) (send/delete/upload, имитация FLOOD после N отправок).
