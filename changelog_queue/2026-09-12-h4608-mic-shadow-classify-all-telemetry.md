_Created: 12-09-2026 · Last updated: 12-09-2026_

- **H4608 (OxAlpha, z-ai/glm-5.3-flash): MIC shadow classify-all-inbound + null-телеметрия + недельный дайджест.** Каждое входящее сообщение поддержки (telegram-синк + веб-чат `POST /chat/message`) прогоняется через вендоренный MIC PHP-лоадер и пишется ТОЛЬКО в новую таблицу `mic_shadow_classifications` (строка на сообщение×плоскость: `{category, reason, null}` + near-miss top-2 с причинами `negation:`/`runner-up:` — G2+G8). Флаг `features.mic_shadow_classify` default OFF: OFF = ровно ноль вызовов MIC и ноль изменений пути ответа (parity-тест), текст сообщения нигде не пишется — только sha256 нормализованного текста (PII-фенс). Команда `support:mic-null-digest` (`--days/--top/--write/--no-write`) — uncategorized top-50 + near-miss пары в markdown (семантика harness run_corpus.py), слот в планировщике: понедельник 06:55 MSK, read-only без флага. Taxonomy-of-record: [docs/MIC_TAXONOMY_MAPPING_2026-09-12.md](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/MIC_TAXONOMY_MAPPING_2026-09-12.md) — MIC topic ↔ легаси A–F ↔ ORS 11 топиков, указатель в comparative-doc Uprava §4.3. Рантайм-флип не тронут: гейт H3529 (precision ≥93%) стоит. Тесты: `MicShadowClassifyTest` 8/8 (флаг-OFF parity, per-plane записи, null-телеметрия, near-miss negation, идемпотентность, дайджест без эха текста) + SupportChatWidgetTest 18/18 на захученном пути.

_Dr. Mārcis Gasūns_
