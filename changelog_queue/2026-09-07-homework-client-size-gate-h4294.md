- fix(homework): клиентский префлайт размера — тяжёлое видео режется ДО старта загрузки (H4294). 07-09-2026 07:16–07:18 МСК студент гр.61 (user 1875, iPhone) 4 раза пытался сдать ДЗ телом 386 214 344 байта (~368 МБ): nginx (200M) убивал запрос голой английской 413 до Laravel — ни серверная валидация (30 МБ/файл, 90 МБ/итог), ни дружественный `PostTooLargeException`-редирект H1343 не успевали сработать, телефон зря сливал трафик (error.log `.92`: `client intended to send too large body`, server samskrte.ru). Root cause по доктрине H1343 «единый источник»: числа были едины в копии/фильтре/валидации, но единственный барьер, который реально срабатывает первым — браузер — их не знал. Alpine-аккумулятор формы (H1337) теперь режет до отправки: каждый `file.size` против `config homework.max_file_kb`, накопительно против `total_max_kb`; превышение — файл НЕ попадает в список, RU-подсказка с фактическим размером и советом (обрезать до 2–3 минут / сжать / прислать фото), набранный текст ответа больше не теряется. Числа по-прежнему только из `config/homework.php` (проброшены в blade), без дублей. Замер хранилища тем же проходом (MG: «усомнись — сервер умрёт за месяц?»): homework 1,37 ГБ, диск 23%, watchdog зелёный — ни один 400 МБ файл физически не доходит до диска, три барьера стоят ДО записи; внешний стор не нужен, триггер H1345 держится. Тесты: HomeworkClientSizeGateTest (пороги едут в Alpine байтами из конфига, следуют за config-изменениями), регресс HomeworkFlow + ImagesPdf + DuplicateSubmission + FeedbackUpload 37/37, Pint clean. Сопутствующий бэкстоп на проде `.92` (санкция MG «Да, оба»): nginx `error_page 413` с русской страницей вместо голой, `kosha.bak-20260907` убран из sites-enabled (4 варнинга conflicting server name — nginx грузил .bak как живой конфиг), бэкап конфига в sites-available; боевая проверка POST 250 МБ → 413 с русской страницей, GET → 200. (OxAlpha zai-coding-plan/glm-5.3-flash, 07-09-2026, [PR #2404](https://github.com/gasyoun/Systema-Sanscriticum/pull/2404), [H4294](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4294-OxAlpha_Systema-Sanscriticum_homework-client-size-gate_07.09.26.md))
