_Created: 13-09-2026 · Last updated: 13-09-2026_

# H4513: ZOOM 1.4 — no-cover путь доведён до Rutube (перенос ребра с Merge), HEAD-сбой получает честный H3952_INFRASTRUCTURE_FAILURE (OxAlpha z-ai/glm-5.3-flash, 13-09-2026)

Закрыты оба хвоста инцидента 10-09 (ENOSPC + Merge dead-end) на живом воркфлоу [1EIqqNzMl5NNIxST](https://context-ai.ru/workflow/1EIqqNzMl5NNIxST), бэкап `/root/wf_backup_pre_h4513.json`.

- **Граф (точечный PUT, НЕ деактивируя):** ребро «Обложки нет — пропуск» ПЕРЕНЕСЕНО (MOVE, не параллельное) с Merge вход 1 на «ЗАГРУЗКА НА РУТУБ» вход 0. Причина: Merge (combineByPosition) стреляет по входу 0 (прецедент 2356 — прошёл с 2 из 3 входов), а вход 0 Rutube-загрузки питался ТОЛЬКО цепочкой обложки (`Download file` → `IF Hindi YT thumb` → `Add a playlist item*`) — без обложки вход 0 не приходил никогда, прогон умирал зелёным на Merge (exec 2754: 28 нод, Rutube×3/плейлист/урок молча пропущены). Skip-нода (`{cover_skipped:true}`) безвредна для Rutube-ноды — её выражения ссылаются только на именованные ноды. GET-верификация: skip→Merge рёбер 0, skip→Rutube вход 0, active=true.
- **Вердикт:** VERDICT_JS (+ [scripts/n8n_zoom14_h3952_freshlink_verdicts.py](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/scripts/n8n_zoom14_h3952_freshlink_verdicts.py)) получил ветку `error→infrastructure_failure`: HEAD вебхук-токена без HTTP-ответа (status=0 — транспорт/краш ноды через onError/alwaysOutputData, или ≥500) → `H3952_INFRASTRUCTURE_FAILURE` в тексте ошибки, вместо ложного `H3952_WEBHOOK_MISSING` (прецедент exec 2749 — ENOSPC диск .91, else-fallthrough солгал). 404-контроль не дрейфует (форс-тест: error-head/empty-head → infrastructure_failure, http-404 → webhook_missing, PASS).
- **PHP-маппинг:** [N8nZoomExecutionProbe](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Recordings/N8nZoomExecutionProbe.php) — маркер `h3952_infrastructure_failure` → класс `fresh_link_infrastructure` + вердикт дежурному («проверить диск/сеть .91, повторить прогон; НЕ „записи нет“ и НЕ cred»). `RecordingsGapFreshLinkVerdictTest` — 6-й маркер в контракте, **7/7 зелёные** (26 assertions).
- **Доки:** RUNBOOK §3.1 (строка `H3952_INFRASTRUCTURE_FAILURE` в таблице вердиктов) + §3.2 (класс закрыт, семантика переноса, deploy-механизм = §6 скрипта).
- **df .91 до/после задеплоя:** идентичен — `/dev/loop3 49G 37G 9.5G 80%` (инцидент-порог 100% не возвращался).
- **Осторожность:** полный no-cover реплей вебхука запрещён (дубль `Upload a video (Hindi)`); класс закрыт по топологии+GET+форс-тесту вердикт-нод до первого живого no-cover прогона. Риск под наблюдением: без обложки у «ЗАГРУЗКА НА РУТУБ1» (тумба) может не оказаться binary `data` из Merge входа 1 — тогда прогон упадёт ГРОМКО на именованной ноде (не тихий зелёный тупик); при подтверждении — точечный follow-up.
_Dr. Mārcis Gasūns_
