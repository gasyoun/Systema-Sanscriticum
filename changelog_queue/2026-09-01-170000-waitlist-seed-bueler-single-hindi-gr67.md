- waitlist:seed corrections (OxAlpha `z-ai/glm-5.3-flash`, 01-09-2026, MG rulings same day, [PR #2312](https://github.com/gasyoun/Systema-Sanscriticum/pull/2312)): Bueler is ONE waitlist row — MG ruled the unfilled-by-autumn-2027 case is handled by moving `earliest_start` to autumn 2028, never by a second near-identical row (that dupe is what rendered as two identical «Руководство по Бюлеру» lines in the channel post); reseed now deletes `bueler-2-potok` idempotently. Hindi keeps continuous group numbering (DB census: «Грамматика хинди гр.» 1-5 + «начальная №N» series already exist) — titles become «Начальный хинди (гр. 6)» / «(гр. 7)», slugs unchanged so votes survive. Tests: `WaitlistSeedCommandTest` (single-Bueler, dupe-collapse, idempotence), Pint green. This also fixes the whole-post duplication questions: msgs 2298+2299 on @institutsanskrita were one post + one verification POST — the n8n twin posts to Telegram directly, outside Laravel's `TelegramSendGuard`; the Uprava workflow twin now carries a 24h same-text dedup (workflow static data, guard semantics mirror) plus a `?dry=1` webhook mode that renders without sending.
