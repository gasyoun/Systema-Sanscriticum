- ci: main снова зелёный — четыре несвязанные красные приёмки (H4279) — обязательная проверка `PHP 8.3 — tests` держала защищённую ветку в strict-режиме, поэтому не сливался НИ ОДИН PR (в том числе #2395, H3999 Telegram support wave 1, у которого других блокеров не было). Четыре падения из четырёх разных фич, ни одного из Support: (1) `SplitUploadToYandexTest::test_resume_skips_group_whose_local_archive_is_gone` — бомба замедленного действия ровно того класса, что описан в CLAUDE.md после H2541: фикстура прибита к календарной дате `2026-08-19`, а `pruneOldGroups()` меряет `keep_parts_days` (16 в этом сьюте) от `Carbon::now()`, поэтому 06-09 граница дошла до фикстуры и удалила её как устаревшую — ровно то, что сделал бы прод; контракт докатки не менялся, `setUp` пинит `Carbon::setTestNow('2026-08-23 12:00:00')`, `tearDown` отпускает, соседний `2026-08-21` в `test_resume_skips_group_laid_out_by_other_part_size` был в дне от той же участи; (2) `CuratorAdminGuideCoverageTest` — H4253 добавил пункт «Каникулы преподавателей» в группу «Пользователи», но не описал его: в часть II руководства куратора добавлен раздел (колонки «С»/«По»/статус, кнопка «Окно каникул», пустой конец = «уточняется», два следствия окна — пометка каникул с датой выхода в публичном расписании и молчание бота напоминаний, и причина спросить преподавателя — он ставит то же окно командой в чате группы); (3) `StudentCabinetGuideCoverageTest` — восьмой сценарий «Записаться на будущий курс (список ожидания)» из H4206 (77676723) был сознательным (полный текст, строка оглавления, шапка «семь задач» → «восемь»), забыли только счётчик в приёмке — бампнут так же, как CuratorAdminGuideCoverage бампался под H3761, кадров по-прежнему семь и этот известный пробел теперь записан в докблоке; (4) `TeacherGuideCoverageTest::the_panel_page_rewrites_relative_screenshot_paths` — механическая правка ссылок 05-09 (5ad0bad1, «relative-link upgrades (139)») развернула 15 источников кадров руководства преподавателя и 3 гида студента в `github.com/…/blob/…`, а blob-адрес в `<img>` отдаёт HTML-страницу вместо PNG: сломались рендер на GitHub, сборка PDF (её база — `docs/`) и страница панели, которой стало нечего переписывать на raw-адрес; все 18 возвращены к относительному `screenshots/…`, обе приёмки получили пин `screenshot_sources_stay_relative` (корень — правило, не различающее ссылку `[…](…)` и картинку `![…](…)`; хук `check_md_full_urls` живёт вне репозитория и только предупреждает, поэтому здесь стоит защита репозитория). Ни один ассерт не ослаблен и не удалён. Приёмка: фильтр из четырёх классов 39/39 green, Pint clean, полный прогон без новых падений сверх известного локального шума (кадры гидов — в CI `GUIDE_SHOTS_OPTIONAL=1`, GrammarLab import search, catalog pretty URLs, vitrina waitlist). (Opus 5 `claude-opus-5`, 07-09-2026, H4279)
