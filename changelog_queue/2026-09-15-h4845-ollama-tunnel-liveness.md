- **Смерть Ollama-туннеля больше не тихая (H4845, OxAlpha `opencode/z-ai/glm-5.3-flash` tier, executed by Opus 5 `claude-opus-5`, 15-09-2026).** Reverse-туннель `127.0.0.1:11434` с GPU-узла умирал, а приложение писало только WARN «dense-нога недоступна, деградация в BM25» — поиск деградировал молча, тень копила ошибки. Замер 14-09 22:45 UTC: `NO-LISTENER`, `curl` exit 7, при включённых `KNOWLEDGE_EMBEDDING_DRIVER=ollama`, `FAQ_HYBRID_RETRIEVAL`, `BOT_OLLAMA_SHADOW`; WARN-строки есть и в рабочие часы узла (09–15 МСК). Теперь `cabinet:probe` зовёт [OllamaTunnelProbe](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Support/ServerGuards/OllamaTunnelProbe.php) (`GET /api/tags`, 2 попытки). Проверка идёт только при включённом потребителе туннеля и в окне `KNOWLEDGE_TUNNEL_HOURS` (по умолчанию `09:30-20:30` МСК, ночью узел спит штатно). Мёртвый туннель даёт одну soft-тревогу `ollama-tunnel:` с классом отказа: cURL 7 — нет слушателя, cURL 28 — висит, HTTP 5xx — нет Ollama. Fingerprint сводит все варианты к одному классу, живой туннель тревогу снимает. Новые env-ключи: `KNOWLEDGE_TUNNEL_HOURS`, `CABINET_PROBE_CHECK_OLLAMA_TUNNEL`. Владелец, перезапуск и как заглушить: [OLLAMA_TUNNEL_RUNBOOK.md](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/ops/OLLAMA_TUNNEL_RUNBOOK.md). Тесты: `OllamaTunnelProbeTest` (9) + приёмочный `test_dead_ollama_tunnel_alerts_once_and_restored_tunnel_clears`.
