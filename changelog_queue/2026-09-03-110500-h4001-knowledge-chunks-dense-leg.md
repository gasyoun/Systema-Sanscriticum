# H4001 (Wave 3 leverage-плана): knowledge_chunks dense-нога — bge-m3, HybridRetriever (RRF, BM25-пол), knowledge:index, fresh eval-набор (OxAlpha z-ai/glm-5.3-flash, 03-09-2026)

Wave 3 плана Telegram support leverage: dense-нога FAQ-ретривала без внешнего vector store — 22 МБ float32 в MySQL, пол BM25 по контракту. Все студент-видимые поверхности не тронуты: флаг `FAQ_HYBRID_RETRIEVAL` default OFF, при OFF dense-нога даже не зовётся (туннель не в request-path).

- **`config/knowledge.php`** ([создан](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/config/knowledge.php)): driver (`''` → Null, `'ollama'` → Ollama), base_url туннеля `127.0.0.1:11434` (слушатель sshd-session на `.92`, модель на узле Ивана — на `.92` ничего не ставится), модель `bge-m3:latest` (ровно этот тег отвечает на узле; короткое имя даёт 404), dims 1024, таймаут 5 с без ретраев в request-path (рулинг D2), batch 16, RRF k=60, depth 20. Env-инвентарь перегенерирован тем же проходом (+10 ключей, все optional).
- **`knowledge_chunks`** (миграция `2026_09_03_110000`): одна строка на `FaqChunk` — `faq_chunk_id` unique, float32 LE BLOB, dims, `content_hash` = sha256(model|dims|searchText) охраняет ре-эмбеддинг. Ни одной колонки в существующих support-таблицах.
- **Провайдеры** ([`Faq/`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Support/Faq)): интерфейс `EmbeddingProvider` (пустой вектор = «dense-нога недоступна», НИКОГДА «нет результатов»), `OllamaEmbeddingProvider` (ровно одна попытка, 5 с, malformed/расхождение размерности падают громко), `NullEmbeddingProvider` (пусто + ровно один warning; тесты и прод-без-туннеля ведут себя одинаково). Биндинг в `AppServiceProvider` — bind, не singleton (config читается на момент резолва).
- **`knowledge:index`** ([KnowledgeIndex](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/KnowledgeIndex.php)): дельта по `content_hash` — без сдвига не пишется ничего; партии по batch_size уезжают в [KnowledgeEmbedChunksJob](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Jobs/KnowledgeEmbedChunksJob.php) на очередь `imports` (supervisor-long), ретраи внутри джобы (tries=3, backoff 30/120 — осознанное отступление от house «tries=1»: обрыв туннеля штатен). Driver пуст → честный отказ. Расписание: ежедневно 04:40 МСК, двойной гейт (флаг гибрида И настроенный driver), onOneServer.
- **`HybridRetriever`** ([создан](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Support/Faq/HybridRetriever.php)) — дроп-ин уровня `Bm25FaqRetriever`: те же `retrieve()/retrieveChunks()` и шейп цитат (+аддитивный `bm25_score`), reciprocal-rank fusion `Σ 1/(k+rank)`, BM25-ранжирование байт-в-байт при недоступной dense-ноге. Денежный порог (категория D) честно остался в домене BM25: `passesThreshold()` читает `bm25_score`, а не RRF-скор (шкалы несопоставимы — RRF молча уронил бы всё через `min_score`).
- **Fresh eval-набор** ([`tests/fixtures/faq_rag_eval_fresh.json`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/fixtures/faq_rag_eval_fresh.json)): добыт `faq:eval-set-build --since=2026-08-04` на проде — 1175 входящих строк → 95 вопросных кандидатов → 20 размеченных поштучно (R6: неоднозначные выброшены, не угаданы) → исключено пересечение с 80Q-набором по dedupe-ключу (новая опция `--exclude-fixture`). ПДн анонимизированы командой ДО выхода с прода; маскирование доказано тестом над самим фикстуром (`FaqRagFreshSetMaskingTest`: e-mail/телефон/хэндл/ссылка/длинный id + дизъюнктность с 80Q + существование всех expected id в корпусе). Честная оговорка: «100Q из сентября» на 03-09 неисполнима — окно сентября 2 дня, а FAQ-образных вопросов ~8 % входящих (та же стена, что FINDINGS §635); взят честный максимум свежего 30-дневного окна, отклонение зафиксировано в PR.
- **Гейты eval** ([FaqRagEvalTest](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Support/FaqRagEvalTest.php) расширен): CI — гибрид с Null-провайдером даёт ранжирование байт-в-байт BM25 на ОБОИХ наборах (пол); live (`KNOWLEDGE_LIVE_EVAL=1`) — гибрид ≥ BM25 по recall@5/MRR на обоих наборах + p95 ≤ 2 с, с записью dated-раздела в `docs/FAQ_RAG_EVAL_H2448.md`. Живой прогон 03-09 ОСТАНОВЛЕН честно: туннель жив (http 200), но на GPU-узле сменился ростер моделей — `bge-m3`/`qwen3:14b` отсутствуют (только `gemma4:12b`); ставить модели на чужой узел нельзя, live-полнировка — @DO за владельцем узла. BM25-пол обеспечивает lane'у неизменное поведение.
- Тесты: FaqHybridRetrieverTest (6), KnowledgeEmbeddingProviderTest (7), KnowledgeIndexTest (4), FaqRagFreshSetMaskingTest (3), FaqRagEvalTest +2 — 22 новых, все green. Полный набор 5008 тестов: 4 падения, все 4 воспроизводятся на чистом origin/main (GrammarLab import search, Shop pretty URLs, Cabinet guide screenshots, Vitrina waitlist) — pre-existing, к этому diff отношения не имеют. Pint clean.
