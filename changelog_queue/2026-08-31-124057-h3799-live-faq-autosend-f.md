- **H3799 (Opus 5 `claude-opus-5`, 31-08-2026, рулинг MG «только F»): живой автоответ студенту из FAQ — впервые, и ровно в одной категории.** До сих пор BM25-ответ доходил только до куратора (подсказка H2448/H3393) или до теневого журнала (H3765 A3). Теперь в категории **F** (материалы, ДЗ, сертификаты) бот отвечает студенту сам — выше покатегорийного порога, выведенного [H3766 B5](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/FAQ_RAG_EVAL_H2448.md), и всегда со ссылкой на раздел FAQ. Из четырёх теневых категорий живой стала одна: по B5 только у F требуемые рулингом R3 95 % точности достигаются при разумном покрытии (41 %), у A/B/C тот же порог оставляет 2–3 вопроса из выборки. Ворота по порядку: флаг `features.support_dm_auto_reply_live_faq` (`SUPPORT_DM_AUTO_REPLY_LIVE_FAQ`, по умолчанию ВЫКЛ и он же весь откат) → категория из `support.faq_rag.live_categories` (`['F']`) → **D (деньги) и E (доступы) вычёркиваются В КОДЕ, а не конфигом** (рулинг R3 запрещает их безусловно; правка конфига не должна уметь это снять) → `auto_reply_enabled` аккаунта → свежесть (H3765 A2: полежавший вопрос по-прежнему уходит только куратору) → скор не ниже порога. `shadowFloor()` переименован в `scoreFloor()` и обслуживает теперь ОБЕ стороны: разойдись пороги тени и живой отправки, и «мы это неделю мерили в тени» перестало бы что-либо значить про то, что уходит студенту. Черновик строит существующий `faqDraft()`, поэтому цитата FAQ структурна, а не опциональна; доставка идёт через прежний `sendAuto()` → `queueAiReply()` под клеймом `TelegramSendGuard` — новой точки отправки в Telegram не появилось; в событие `dm_auto_sent` пишутся `chunk_id`, скор И действовавший порог, чтобы задним числом было видно, какой была планка. **Оговорка записана, а не спрятана:** порог F (15.7) выведен там, где выше него оказались 7 вопросов, а теневая неделя рулинга R9 к моменту решения не прошла — первая же плохая неделя означает выключение флага. Тесты: [`SupportDmLiveFaqAutoSendTest`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Support/SupportDmLiveFaqAutoSendTest.php) (8: счастливый путь с цитатой, флаг выключен, категория вне живого списка, D и E отказаны даже когда вписаны в конфиг, ниже порога, гейт аккаунта, непривязанный студент, полежавший вопрос); support-набор 87/87.
