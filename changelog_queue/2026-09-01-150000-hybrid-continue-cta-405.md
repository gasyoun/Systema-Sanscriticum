- **Кнопка «Оплатить блоки» в hybrid-кабинете больше не роняет студента в 405 (Fable 5 `claude-fable-5`, 01-09-2026).** Прод-кейс 01-09 14:08 МСК: студент нажал платёжный CTA в карточке «Продолжить» на `/dvaram` и получил «405 Method Not Allowed» — карточка в [`student/hybrid/home.blade.php`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/views/student/hybrid/home.blade.php) рендерила CTA всегда голой ссылкой, игнорируя `method`, а `/debt/course/{id}/pay-bundle` (как и взносы рассрочки `pay-promise`/`pay-all`) принимает только POST. Легаси-дашборд (`partials/continue-learning-card.blade.php`) рендерит те же CTA правильно — баг был только в hybrid-виде. Теперь hybrid-карточка повторяет трёхветочный рендер: POST → форма с @csrf, TAB («Открыть долги») → ссылка на «Оплата и доступ» (`/dvaram/access` — вкладок `#debts` в hybrid-шелле нет, ссылка вела в никуда), GET → ссылка как раньше. Телеметрия `data-cabinet-event` сохранена на всех ветках. Тесты: `HybridPhase1Test` +2 (форма POST на pay-bundle вместо href; CTA урока остаётся ссылкой).
