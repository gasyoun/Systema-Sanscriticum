# Метрика: цели внешнего сайта и кабинета — план по рулингу MG 18-09-2026 (OxAlpha `opencode/glm-5.3-flash`, 18-09-2026)

**Рулинг MG отменяет дефолт «кабинет не тегируем»:** кабинет тегируется тем же счётчиком `106964341`, но с `webvisor:false, clickmap:false` — ни одной записи сессий залогиненных (152-ФЗ), параллельно магазин закручивается до «вебвизор только гостям» (`webvisor => !auth()->check()`). План целей: [docs/METRIKA_GOALS_SHOP_CABINET_2026-09-18.md](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/METRIKA_GOALS_SHOP_CABINET_2026-09-18.md) — внешний контур: 6 существующих целей (проверка в UI) + `lead_form_submit` + `telegram_click` + фикс главной `/` (счётчика нет, проба 18-09); кабинет: Tier 1 из 10 целей на живых эмиттерах (`first_cabinet_action` flip `metrika_goal: null` → цель, `lesson_open/complete`, `lesson_mark_mastered`, `cabinet_home_view`, `library_shelf_view`, `path_station_view`, `access_renewal_*`, `zoom_join_click`), Tier 2 (`offer_click` и др.) — блокер P0 H4134 (потерянные эмиттеры гибрида), Tier 3 не тегируется. Только документация и план; код-PRы и создание целей в UI Метрики (нужен аккаунт MG) — residuals в [Uprava GTD](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md).
