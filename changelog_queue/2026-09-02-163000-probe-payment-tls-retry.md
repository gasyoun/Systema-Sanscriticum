- **probe: outbound payment-TLS проба `cabinet:probe` перепробует соединение до 3 раз перед critical (OxAlpha `z-ai/glm-5.3-flash`, 02-09-2026, [PR #2327](https://github.com/gasyoun/Systema-Sanscriticum/pull/2327)).** TLS-флап к `enter.tochka.com` (~1/5 коннектов умирает на ClientHello, GTD @WAITING 02-09-2026) давал ложные Better Stack-инциденты на каждый одиночный обрыв: `probeOutboundPaymentTls()` бил один раз, любое падение соединения = critical → `/fail` → письмо, через 15 минут следующий зелёный прогон авто-резолвил. Теперь до `cabinet_probe.payment_tls_attempts` (по умолчанию 3) попыток с паузой `payment_tls_pause_seconds` (2 с); сообщение несёт счёт попыток («(3 попыток)»). Флап выживает 3 попытки с вероятностью ~0.8 %, реальный аутейдж (класс 4-дневного CA-инцидента) алертится тем же прогоном. Тесты: `CabinetProbeTest` +2 (транзиентный обрыв → жив, без `/fail`; устойчивый отказ → critical после всех попыток).
