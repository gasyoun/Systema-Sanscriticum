# Кабинет тегируется Метрикой: cabinet-metrika + мост reachGoal — GTD @DO 18-09 (H5154, OxAlpha `opencode/glm-5.3-flash`, 18-09-2026)

Исполнена строка GTD [«Кабинет тегируется Метрикой»](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md) — план [docs/METRIKA_GOALS_SHOP_CABINET_2026-09-18.md](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/METRIKA_GOALS_SHOP_CABINET_2026-09-18.md) §Implementation 1–4. **Кабинет тегируется тем же счётчиком `106964341`, что и магазин** — воронка «оплата → активация → продление» собирается в одном отчёте; `webvisor:false, clickmap:false` в кабинете — ни одной записи сессий залогиненных (152-ФЗ). Параллельная дырка 152-ФЗ в магазине закрыта: `shop-metrika` теперь шлёт `webvisor` гостям (`!auth()->check()`), залогиненный студент на `/k/*`, `/checkout/*`, `/online` больше не пишется. Реестр целей: `config/analytics.php` +10 кабинетных Tier 1 (ids 639422183–639422234 созданы в Метрике заранее, PR #2700), flip `first_cabinet_action.metrika_goal: null` → имя цели. Доставка: клиентский мост в `student/partials/telemetry.blade.php` (`METRIKA_BRIDGE`: `library.shelf.view`, `path.station.view`, `access.renewal.start`, `zoom.join.click` → reachGoal); серверные события — `data-metrika-goal`-маркеры + лоадер в новом партиале `partials/cabinet-metrika.blade.php` (include одной строкой в `layouts/student`): `cabinet_home_view` — каждая загрузка дашборда (легаси + гибрид), `first_cabinet_action` — только если ЭТА загрузка первое действие (`FunnelTelemetry::hasFirstCabinetAction`, проверка до эмита), `lesson_open` — маркер на странице урока, `lesson_mark_mastered` — flash `metrika_goal` после `completeLesson` (только новое завершение), `access_renewal_complete` — на `/payment/success` при `confirmed && is_self_service` (контроллер денег не тронут), кнопки Zoom (`calendar`, `lesson`) получили `data-track-event="zoom.join.click"`. `lesson_complete` остаётся без серверного эмиттера (§4-спека) — цель создана, но считает 0 до появления писателя (residual). Тесты: `CabinetMetrikaGoalsConfigTest` (Tier-1 реестр + flip + Tier-2 не залез).
