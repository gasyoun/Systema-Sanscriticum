- **H3820 (Opus 5 `claude-opus-5[1m]`, 01-09-2026): оба аудита каталога и money-контур `CLAUDE.md` научены «курируемой продаже по прямой ссылке» — остаток H3812.** [#2291](https://github.com/gasyoun/Systema-Sanscriticum/pull/2291) отозвал команду, но правило нигде записано не было: `CatalogFamilyAudit` звал курс 327 дублем, а `CatalogShellAudit` не считал его оболочкой лишь по побочному эффекту пересчёта таблиц (активный тариф — строка в `tariffs` с `course_id`), то есть по совпадению, а не по правилу. Теперь состояние **скрыт с витрины + `is_active` + ≥1 активный тариф** названо явно: [`CatalogFamilyAudit::CLASS_CURATOR_GATED_SALE`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/CatalogFamilyAudit.php) (маркер `curator_gated_sale` в каждой строке-члене семьи + расшифровка класса в `catalog:audit-families --markdown`) и [`CatalogShellAudit::isCuratorGatedSale()`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/CatalogShellAudit.php) (маркер в `courseRow()`, блокер удаления, ранний `false` в `isShellCourse()` — тот самый явный запрет вместо совпадения; `courseRow()` переспрашивается `catalog:retire-shell` непосредственно перед записью). **Убрано приглашение, по которому и пошёл инцидент:** для семьи со скрытой продаваемой строкой follow-up больше не советует «прибираться по желанию через `catalog:retire-shell`» — совет ЗАМЕЩАЕТСЯ запретом «НЕ ТРОГАТЬ … `tariffs.is_active` — гейт ПОКУПКИ, а не доступа». Пин боевой формы курса 327 (скрыт, `is_active`, 5 активных тарифов, 0 уроков, 0 `course_materials`, группа с 43 участниками, 129 оплат 02-06-2025…28-12-2025) + два контроля границы (видимый курс с тарифами — не курируемая продажа; скрытый с погашенными тарифами — тоже) + проверка «аудит ничего не меняет»: [`CuratorGatedHiddenSaleTest`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Catalog/CuratorGatedHiddenSaleTest.php), 7 тестов, красные на коде до правки (5 из 7) и зелёные после. Каталог не тронут: ни одной записи в `courses`/`tariffs`.
