- **H3764 (Opus 5 `claude-opus-5`, 01-09-2026): комментарий у `lesson_views.is_completed` переписан честно — рулинг MG «перепиши комментарий миграции честно» ([issue #2299](https://github.com/gasyoun/Systema-Sanscriticum/issues/2299), вариант 3).** Миграция обещала «синхронизируется с pivot completed_lessons», синхронизации нет и не было: прод 01-09-2026 — 649 строк просмотров, из них с признаком **0**, при 166 пройденных уроках в пивоте `lesson_user`. Теперь комментарий говорит прямо: столбец НЕ ЗАПОЛНЯЕТСЯ, завершаемость считать по `lesson_user.is_completed`, а метрика, севшая сюда, вернёт ровный 0 % по каждому курсу — число, неотличимое от честного нуля. То же предупреждение добавлено докблоком к модели [`LessonView`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Models/LessonView.php): именно её открывают, а не миграцию четырёхмесячной давности. Схема не тронута — правка только комментариев, миграция не перезапускается. Поведение уже запинено `ActivationCompletionMetricsServiceTest`.
