### Fixed

- **Деплой больше не оставляет `telegram-student-poll` на пред-деплойном коде (OxAlpha `zai-coding-plan/glm-5.3-flash`, 17-09-2026, H5063).** Долгоживущий поллер студенческого бота держит в памяти код, загруженный при старте, но `deploy.sh` перезапускал только horizon, systema-madeline-daemon и (условного) zapisi-poll — после деплоя [8a2b87ea](https://github.com/gasyoun/Systema-Sanscriticum/commit/8a2b87ea) поллер (pid стартовал до деплоя) продолжал крутить старый `MicShadowClassifier` и сыпал `mic_shadow_classify_loader_failed` (H5051, 17-09-2026; лечился ручным `supervisorctl restart`). В `deploy.sh` рядом с блоком zapisi-poll добавлен такой же условный рестарт: только если программа сейчас `RUNNING` (рестарт остановленной ЗАПУСТИЛ бы её и увёл бота студента с штатной вебхук-дорожки), с `say`-строкой об успехе и `ВНИМАНИЕ`-строкой при неудаче.
