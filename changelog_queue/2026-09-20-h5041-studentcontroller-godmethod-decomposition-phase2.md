- H5041 (20-09-2026, Sonnet 5 `claude-sonnet-5`): `StudentController` Phase 2 — internal god-method decomposition. `dashboard()` (253→~100 lines) and `showLesson()` (161→~45 lines) split into 16 private helper methods on the same class, no route/signature/behavior change. Continues the H4941 (routes split) → H4952 (Kernel.php split) → H4978 (Phase 1: 2 Concerns traits) programme; the two externally-called statics (`getUserUnlockedTariffs`, `parseVideoId`) remain out of scope, deferred pending an access-control review. Verified: `php -l`/Pint clean, `route:list --json` fingerprint identical (559 routes) against a fresh `origin/main` checkout of the same file, every extracted method body a verbatim slice of the pre-refactor code. Rebased onto `origin/main` past the H5154 cabinet-metrika drift — `emitDashboardTelemetry()` now returns `$metrikaFirstCabinetAction` instead of silently discarding it as a dead local var (a bug the naive rebase would otherwise have introduced). [PR #2729](https://github.com/gasyoun/Systema-Sanscriticum/pull/2729).
