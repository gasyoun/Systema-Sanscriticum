_Created: 06-09-2026 · Last updated: 06-09-2026_

# H4200: месячный импорт банковских выписок в «Расходы ИП» — Точка/Сбер/PayPal без ручного ввода (OxAlpha z-ai/glm-5.3-flash, 06-09-2026)

Новый лейн «Расходы ИП» после остановки книги (июль 2026): выписка банка → `ip-expenses:import-statement` → dry-run отчёт → `--apply` по гейту MG. Рулинг-база — MG 05-09: «Банк-выписка → агент» + «I am not accountant». Реюз конвенций импортёра книги [H4188](https://github.com/gasyoun/Systema-Sanscriticum/commit/24e4375b): Decimal-exact суммы, идемпотентность по `import_hash` с сохранением легитимных дубликатов, эвристика `IpExpenseCategory::guess` с пере-категоризацией в админке, append-only аудит.
- **`ip-expenses:import-statement {bank} {file} --month=YYYY-MM`** ([ImportIpStatementExpenses](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/ImportIpStatementExpenses.php)): dry-run по умолчанию; REFUSE-гейты до записи — месяц каждой строки == `--month`; окно двойного счёта (месяц ≤ 2026-07: книга Oct'25–Jul'26 уже в `ip_expenses` + CRM-«Расход» pseudo-payments — AUDIT_BOOKKEEPING_MISERABLE_MAP §3) закрыт без `--overlap-acknowledged`; `--aggregates` сверяет Σ RUB с `debit_sum` свежайшего снапшота [tochka_monthly_aggregates.tsv](https://github.com/gasyoun/Uprava/blob/main/data/tochka_monthly_aggregates.tsv); `--expect-total CUR=Σ` — с заявленной выпиской суммой. Предупреждение о двойном счёте печатается на каждом прогоне.
- **Парсеры** ([BankStatements/](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/BankStatements)): Точка — канонический CSV дебета нашего сборщика (строгая шапка); Сбер — расширенная выписка CSV с автоопределением кодировки (cp1251) и разделителя, колонки по алиасам шапки (не найдена обязательная — REFUSE со списком найденных), дебет-only, «Итого»-строки мимо; PayPal — Activity export EN, только Completed с отрицательным Gross, внутренние переводы на свой банк и входящие пропускаются со счётчиками, суммы остаются в НАТИВНОЙ валюте (currency + fx_note, курс не выдумывается). Неизвестный вход всегда UnparseableStatementException — молчаливых догадок в деньгах нет. Найденный и закрытый тестом дефект: PHP `trim()` с мультибайтовой маской (« ») байтовый — съедал последний байт кириллической буквы («л» кончается 0xBB = байт из «») и клал в БД битый UTF-8; обрезка переведена на /u-регэксп.
- **Аудит**: новое действие `imported_statement` («Импорт из выписки») в ip_expense_audits с провенансом файла (банк, месяц, sha256).
- **Сборщик Точки** (Uprava [pull_tochka_statement.py](https://github.com/gasyoun/Uprava/blob/main/tools/pull_tochka_statement.py), `--debits-csv DIR`): дебетовые строки месяца → канонический CSV; зачисления (студенты, payer names/INN) остаются memory-only — 152-FZ fence не тронут кодовой формой (пишущая ветка Credit-строки не видит); Σ CSV сверяется с агрегатом debit_sum того же документа, расхождение — REFUSE.
- Тесты: IpExpenseImportStatementTest (10: dry-run чисто, провенанс+идемпотентность+дубликаты, гейты aggregates/expect-total/окна/месяца, cp1251-Сбер, нативная валюта PayPal, неизвестный банк) — green; IpExpenseImportBookTest/IpExpenseAuditAppendOnlyTest/OwnerDrawLiabilityTest/ExpenseBridgeRaskhodTest — green (регрессия чистая). Pint clean.
- Деплой и первый реальный dry-run на проде — human-gated (GTD @DO).

_Dr. Mārcis Gasūns_
