- feat(ops): внешний почасовой монитор джорни-проб прода (H4287) — workflow `.github/workflows/journey-probes.yml`: cron `:23` ежечасно с инфраструктуры GitHub, GET-only, никогда POST и никогда реальных заказов; пробует sale на тест-SKU 5056 (checkout-форма + listPrice — сигналы, убийство которых = инцидент N2), поверхность /register, FAQ-страницы саппортера, карточки календаря (регрессия пустого фида) и срок TLS-сертификата. `tools/journey_probes/probe_journeys.py` — vendored копия из приватного Uprava (provenance-заголовок, ре-вендор после каждого апстрим-изменения, selftest = дизайн-тесты N2/календарь). Доставка тревоги — путь H4245: FAIL пробы → GET `<heartbeat>/fail` (форс-инцидент на телефоне MG немедленно), PASS → пульс; секрет `BETTER_STACK_JOURNEY_HEARTBEAT_URL`, без него — честный MISS в summary + штатное письмо GitHub о падении планового прогона. Ре-ран теста доставки H4245 на новой каденции — one-click: Actions → journey-probes → `test_alert=true` (форсирует /fail, не трогая сайт). Почему здесь, а не в Uprava: Uprava приватный — hosted-минуты Actions биллятся/заблокированы (regen.yml design note), здесь публичный репо = бесплатно (та же логика, что у uptime-samskrte.yml). Никогда .92-from-.92: L6 (провайдер нуль-роутил ingress при зелёных локальных проверках) + L8 (машина не сообщает о собственной смерти). Предпочтительный раннер по миссии — нативный монитор Better Stack (bought over built): GTD 0ac в Uprava, без API-токена заводится только в dashboard. (OxAlpha zai-coding-plan/glm-5.3-flash via opencode, 07-09-2026, H4287)
