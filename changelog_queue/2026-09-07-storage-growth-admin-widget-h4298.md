- feat(admin): дашборд-виджет «Хранилище: рост и прогноз» — месяц исчерпания потолка виден в админке (H4298). MG 07-09-2026: «27% от потолка 5 ГБ — сильно быстро мы поднялись к опасной черте, к какому месяцу исчерпается место? Это должно быть видно в админке». Ответ на вопрос теперь в самом дашборде: таблица watched-каталогов из `config/storage_watch.php` — занято/потолок/доля/статус (reuse `StorageUsageService::snapshot()`, та же точка правды, что у ежедневного `storage:check`) + рост за 30 дней + прогноз месяца достижения потолка при темпе последних 30 дней. Новый `StorageUsageService::growth()`: помесячная разбивка веса каталога по mtime файлов одной итерацией с тем же `scan_file_cap`-предохранителем; у загрузок ДЗ/материалов mtime ≈ моменту создания. Честность метода прописана в футере виджета: оценка по mtime, перезаписи считает как рост (бэкапные каталоги завышены), total-ETA считается по сумме watched-темпов, а не по churn'ному тоталу; ceil месяцев — виджет не имеет права успокаивать раньше времени; RU-месяцы детерминированы без зависимости от app locale; кэш 6 ч — IO-тяжёлый обход не бьёт по диску на каждый рендер, daily-алерты остаются в команде. Гейт `canView` — ADMIN/ACCOUNTANT, как у «Застрявших студентов». Базовый замер 07-09: homework 1,37 ГБ (июн 63 → июл 263 → авг 990 → сен 61 МБ) — при темпе сентября (~260 МБ/мес) до 5 ГБ ~14 мес, при августовском пике (~1 ГБ/мес) ~дек 2026; диск 147 ГБ, свободно 108 ГБ — физика диска не грозит, 5 ГБ — сигнальная линия watchdog, а не место на диске. Тесты: growth-разбивка с pin-нутыми mtime (окно 30 дней, месяцы), file-cap → truncated, отсутствующий каталог = ноль без ошибки, ETA-метки всех состояний (превышен/не растёт/>5 лет/—), ceil-месяц, живой рендер под админом через Livewire. Pint clean. (OxAlpha zai-coding-plan/glm-5.3-flash, 07-09-2026, H4298)
