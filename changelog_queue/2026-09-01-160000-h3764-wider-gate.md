- **H3764 (Opus 5 `claude-opus-5`, 01-09-2026): страницу «Активация и завершаемость» видят админ, бухгалтер, куратор и супер-админ — рулинг MG 01-09-2026, расширение прежнего `accounting()`.** Новый гейт [`RoleGate::learningAnalytics()`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Support/RoleGate.php) = `any(ADMIN, ACCOUNTANT, MANAGER)`, супер-админ проходит через `any()`. Первоначальная адресация была ошибкой: активация и доходимость — не деньги и не зарплата, а рабочий инструмент того, кто ведёт учеников; куратор без этих цифр не видит, кто не дошёл до первого урока. Намеренно НЕ переиспользован `managerSalesReport()`: тот про выручку и сужает строки до своих сделок, а здесь сужать нечего — страница агрегатная, ни сумм, ни персональных данных, все четыре роли видят одно и то же (пин `curator_sees_the_same_numbers_as_the_accountant`). Преподаватель и студент за гейтом остаются: преподавателю нужна доходимость СВОЕГО курса, а не школы, — это отдельная задача с другим знаменателем. Флаг остаётся рубильником для всех разрешённых ролей, а не только для бухгалтера.
