# TelegramSupportPeerWindowTest выведен из time-bomb — относительные даты вместо захардкоженных (OxAlpha `opencode/glm-5.3-flash`, 18-09-2026)

**CI всего репо лёг детерминированным красным** (1/5877): `test_chat_outside_window_and_mp_dialogs_is_polled_only_by_catch_up` засеял «20 дней назад» датой `2026-08-19 10:00` — 18-09-2026 10:00 UTC она пересекла 30-дневную catch-up границу, и catch-up перестал опрашивать peer 55501 (`Failed asserting that an array contains 55501`). Остальные даты файла рванули бы следом (05-09/08-09 → 19-22-09). Все 7 литералов дат переведены на относительные `now()->sub*` с сохранением семантики каждого теста (внутри 14д / вне 14д-внутри 30д / старьё 40-45д / свежий DM 2ч). Только тест; прод-код не тронут.
