_Created: 12-09-2026 · Last updated: 12-09-2026_
# H4597: payroll:backfill-history — заведение исторических выплат по манифесту, dry-run по умолчанию (OxAlpha zai-coding-plan/glm-5.3-flash, 12-09-2026)

Исполнение [H4597](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4597-OxAlpha_Systema-Sanscriticum_backfill-teacher-payouts-history_12.09.26.md) (Uprava, приватный): восстановление исторических выплат Лейтана/Костиной в `teacher_payouts`, чтобы `paidShareKeys` отсекала выплаченное и повторный `payout:run` сходился с ведомостями. class money (H4358): `--apply` на проде — только после PASS независимого верификатора в разделе `## Verifier` хендоффа.

- **+`app/Services/Payroll/BackfillHistoryService.php`:** план/apply по JSON-манифесту (живёт в приватном Uprava `data/h4597_backfill_manifest_*.json`, в публичный Systema не коммитится). Fail-closed: каждая доля перепроверяется против живой базы (существует/paid/real/school/сумма/курс/приход ≤ cutoff), любое расхождение = отказ; идемпотентность по маркеру комментария `H4597 backfill:`; пишет ТОЛЬКО `teacher_payouts` (зеркало в Финансы — отдельный `salary:post-payouts`); `breakdown.prior_blocks_paid` — форма, которую читает `TeacherSalaryService::paidShareKeys()`.
- **+`app/Console/Commands/PayrollBackfillHistory.php`:** `payroll:backfill-history --manifest=… [--teacher=] [--apply]`, dry-run по умолчанию (образец `PostTeacherPayouts`), таблица плана + ⚠-флаги чек-сумм >5 % для верификатора.
- **+`tests/Feature/PayrollBackfillHistoryTest.php`:** 7 тестов — dry-run ничего не пишет; apply создаёт строку и главный инвариант: заведённый breakdown реально отсекается `paidShareKeys`; повторный прогон пропускает (idempotent); fail-closed при изменённом платеже / moved-манифесте / приходе позже cutoff; parked-строки не дают ключей. Соседние зарплатные сюиты зелёные (33 tests/90 assertions).
- **Прод не тронут:** живой dry-run на `.92` выполнен require-скриптом из /tmp (деплой не нужен): план 6 строк/110 долей/199 142,40 ₽ прошёл fail-closed, симуляция apply→payout:run→rollback показала авто-отсечку 2026-07-24 у обоих преподавателей; отпечатки `payments/teacher_payouts/users/finance_snapshots` до==после. Полные чек-суммы, parked-строки и цена гэпа finansy.json — [отчёт в Uprava](https://github.com/gasyoun/Uprava/blob/main/tools/reports/H4597_BACKFILL_TEACHER_PAYOUTS_12-09-2026.md).
_Dr. Mārcis Gasūns_
