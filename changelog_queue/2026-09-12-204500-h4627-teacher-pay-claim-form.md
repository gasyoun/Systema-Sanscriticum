_Created: 12-09-2026 · Last updated: 12-09-2026_

- **H4627 (OxAlpha, z-ai/glm-5.3-flash): анкета «я заплатил преподавателю напрямую» (как PayPal-pending) — заявка → сверка куратора → зачёт в зарплату.** Новый флаг-gated маршрут `GET/POST /teacher-pay/{tariff}` (`TEACHER_PAY_ENABLED`, default OFF — деньги-контур): ученик выбирает преподавателя, сумму/валюту, дату, отправителя, чек (disk `local`); гость с новым email получает аккаунт (зеркало `BankClaimController` H3497). Платёж ложится `pending` c провайдером `teacher_transfer` и **сразу** несёт `received_account=teacher_personal` + `received_by_teacher_id` — куратор видит получателя до подтверждения, а после «Подтвердить перевод преподавателю» в Filament номинал вычтется из гонорара штатным движком (механизм H4597, `directReceiptsForTeacher` по `foreign_amount`). Авто-доверия нет (в отличие от школьного SEPA) — сверку проходит каждая заявка; не заполнил анкету — оплата не зачтена. Админка: фильтр «Оплаты преподавателю на проверке», экшены «Подтвердить перевод преподавателю» / «Нет платежа — отклонить»; кураторам — TG-нотификация `teacherPayReceived` + письма админу/ученику; `teacher_transfer` добавлен в `MANUAL_CLAIM_PROVIDERS` (reaper не трогает). Без миграций (все поля уже есть с H4597). Тесты: `TeacherPayTest` 8/8 + регрессия `BankClaimTest` 8/8, `StaleCheckoutExpiryTest` 10/10.

_Dr. Mārcis Gasūns_
