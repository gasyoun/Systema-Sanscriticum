- H5080-F1 fix (19-09-2026, OxAlpha opencode/z-ai/glm-5.3-flash): .91-verify перестал рисовать ложный `critical backup-offsite — новейший off-site снимок 595 ч назад` — 24 дня тревоги при 26 живых ежедневных копиях. Корень: `restic snapshots --tag n8n --latest 1 --json` группирует по (host, paths), а путь у каждого ночного архива свой — каждый снимок образует СВОЮ группу, и `head -1` в парсере отдавал самый СТАРЫЙ (25-08). Фикс: максимум времени через `sort | tail -1` (ISO сортируется хронологически); регрессионный тест `test_n8n_verify_offsite_parser.sh` извлекает конвейер ИЗ самого verify-скрипта (реверт к `head -1` валит тест по построению) + CI-джоб в `schedule-guard-test.yml` с фильтром `scripts/server_guards_n8n/**`. Эстейт-аудит: .92-инспектор и Uprava `reconcile_census.py` уже брали max/reversed — плоский случай был один. [PR #2711](https://github.com/gasyoun/Systema-Sanscriticum/pull/2711).
