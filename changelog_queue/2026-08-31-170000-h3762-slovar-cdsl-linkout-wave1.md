- **H3762 wave 1: каждая страница `/slovar` получила блок «В словарях CDSL» — исходящие ссылки на кёльнские MW и PWG (Fable 5 `claude-fable-5`, 31-08-2026).** Ссылки строятся на сервере — [`app/Support/CdslLinks.php`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Support/CdslLinks.php) по канонической форме `indexcaller.php?key={slp1}&transLit=slp1&filter=roman` (SHARED_CODE row 22, csl-atlas `cologne-links.mjs`), никакого клиентского фетча к Кёльну. SLP1-ключ даёт [`app/Support/IastToSlp1.php`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Support/IastToSlp1.php) — первый PHP-порт канонической таблицы `sanskrit-util` `_SLP1` (зеркалировать при обновлениях, не форкать). **Разрешимость ключа по всем 11 726 боевым заголовкам — 99,40 %** (сырая конверсия давала 86,22 %; подняли три эвристики: снятие кочергинской обёртки `/слово/`, первый вариант из списков «duṣyanta, duḥṣanta», бесдефисная основа для связанных форм `-akṣa`/`sam-`). Ключ с аваграхой не выпускается вовсе — `getword.php` на нём отдаёт 500 (проба 30-08). **AP90 сознательно НЕ в волне 1:** его ключи — печатные заголовки в номинативе (`agniH`, `yogaH` — живая проверка 31-08), а не основы; ссылка по основе садится на «not found» у большинства существительных, вывод номинатива требует рода, которого в `/slovar` нет. Это же исправляет находку 30-08 «AP90 не имеет yoga/karma/veda/dharma» — имеет, под номинативными ключами. Остаток нерешаемых ключей — грязь источника, включая **кириллические омоглифы в колонке `iast`** (`/ргаdā/`, `/dorака/` — ~40 строк): заведён отдельный integrity-тикет. Тесты: `SlovarCdslLinksTest` (7 тестов: ловушки IAST→SLP1, чистка заголовков, форма URL, рендер/отсутствие блока).