- H5080-F1 (19-09-2026, OxAlpha opencode/z-ai/glm-5.3-flash): .91 nftables-дрейф закрыт по рулингу 0CG2 «nft 8001 docker-only» — в шаблон `nftables/systema-default-deny.nft` внесено scoped-правило `ip saddr 172.18.0.0/16 tcp dport 8001 accept` (kosha-API на .91, вход через caddy-релей H2991), а хвостовой голый `accept` (латентная бомба: при первом reload/reboot погасил бы default-deny целиком) удалён из шаблона и с бокса. Armed-окно H3184 проведено полностью: arm 15 мин → apply → verify 3/3 (новая сессия + кросс-проба с .92 + tailscale) → disarm; попутно доказан механизм отката (`nft add chain` обновляет policy существующей цепочки — тест на прозрачной временной цепочке). Пост-проверки: live `policy drop`, публично 8001 закрыт, kosha `127.0.0.1:8001/health` 200, verify .91 — 77 ok / 0 critical. [PR #2713](https://github.com/gasyoun/Systema-Sanscriticum/pull/2713).
