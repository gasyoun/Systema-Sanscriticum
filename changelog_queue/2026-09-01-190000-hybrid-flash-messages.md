- **Hybrid-кабинет перестал молча глотать ошибки оплаты (Fable 5 `claude-fable-5`, 01-09-2026).** Прод-кейс: студентка начала оплату долга (создан pending-платёж, её увело на страницу банка), не завершила, вернулась и жала «Оплатить блоки» ещё 5 раз — каждый раз анти-дубль `PENDING_HOLD_MINUTES` корректно отбивал повтор с `session('error')` («У вас уже есть незавершённый заказ…»), но hybrid-страницы flash не рендерили вовсе, и для студентки это выглядело как молча сломанная кнопка (видео + nginx: серия POST 302 → GET `/dvaram` 16:16 МСК). Тот же класс бага уже чинили в легаси-дашборде — комментарий у блока это прямо описывал, hybrid-вид баннеры не унаследовал. Блок `session('error')/session('success')` вынесен в партиал [`student/partials/flash-messages.blade.php`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/views/student/partials/flash-messages.blade.php) (разметка 1:1) и подключён в легаси-дашборд (вместо инлайна), `hybrid/home` (над «Сегодня») и `hybrid/access`. Тесты: `HybridPhase1Test` +2 (error на `/dvaram` ровно один раз, error на «Оплате и доступе», success-ветка). Follow-up (не здесь): при живом pending предлагать «вернуться к оплате» по сохранённому paymentLinkId вместо голого отказа.
