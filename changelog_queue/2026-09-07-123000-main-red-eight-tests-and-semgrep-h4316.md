- ci: main снова зелёный — восемь красных приёмок `PHP 8.3 — tests` **и** красная `Semgrep scan` (H4316) — обе обязательные, поэтому защищённая ветка в strict-режиме не пропускала ни один PR. Две независимые причины, обе — регрессии одного дня. **(1) Разметка картинок и CTA-ссылок вырезана механической правкой ссылок.** Коммит `319659f8` («fix(H4092): judgment-link dispositions») прошёлся по закоммиченным `.md` и удалил *всю* разметку вида `![](…)` — 40 картинок в шести документах (гиды бухгалтера, куратора, студента, оба `docs/materials/*/SOURCE.md`, фикстура `tests/fixtures/guides/sample.md`) — а заодно схлопнул пять CTA-ссылок писем марафона `[текст]({link})` в жирный текст без перехода и выел пример `[t](url)`→`<a>` в `docs/cabinet-bot.md`. Это тот же корень, что H4279 разбирал двумя часами раньше (`5ad0bad1` тогда развернула 18 кадров в blob-адреса): правило «относительная ссылка в закоммиченном `.md` → полная blob-ссылка» не отличает ссылку `[…](…)` от картинки `![…](…)`, а теперь ещё и не отличает плейсхолдер рассыльщика `{link}` от битого адреса. Коммит откачен целиком (`git revert`); ни один ассерт не ослаблен, кода приложения диф не касается. Пин H4279 (`screenshot_sources_stay_relative`) ловил только *подмену* адреса на blob, но не *удаление* картинки, поэтому добавлена репозиторная приёмка [`MarkdownImageCorpusTest`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/MarkdownImageCorpusTest.php) на три формы порчи сразу: пол по числу картинок в шести иллюстрированных документах, запрет blob-адреса в источнике картинки по всему корпусу `git ls-files -- '*.md'`, пол по числу CTA-ссылок с плейсхолдером в письмах. Приёмка проверена мутацией: каждая из трёх форм порчи, воспроизведённая на рабочей копии, роняет свой тест с внятным сообщением. **(2) `TelegramWebhookCertificateTest::test_allowed_updates_are_json_encoded_in_multipart`** падал не из-за картинок: H4314 (`87296dcf`) осознанно добавил `my_chat_member` в `allowed_updates` команды `zapisi:set-webhook` (без него не приходит приветственная карточка), но список в приёмке был вшит литералом `'["message","channel_post"]'`. Пин переписан на *форму* контракта — одна часть `allowed_updates` с JSON-массивом строк (именно это и ломается, когда Laravel разворачивает массив в `allowed_updates[]`, которого Bot API не разбирает) плюс явная проверка обязательного минимума `message` + `channel_post`; соседний `ZapisiSetWebhookTest` уже проверял через `in_array` и потому уцелел. **(3) `Semgrep scan`** покраснела на слиянии #2409 (H4287, внешний почасовой journey-probe) и держалась красной шесть коммитов: пять блокирующих находок — `actions/checkout@v4` и `actions/setup-python@v5` в [`journey-probes.yml`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/.github/workflows/journey-probes.yml) (единственный воркфлоу репозитория с подвижными тегами вместо SHA — пиннуты к тем же SHA, что у остальных) и три `dynamic-urllib-use-detected` в `tools/journey_probes/probe_journeys.py`. Последнее починено по существу, а не заглушкой: `urllib` понимает `file://`, а все URL прибора приходят из конфигурации (`--base-url`, `JOURNEY_HEARTBEAT_URLS`), поэтому добавлена обёртка `_urlopen_http()` с белым списком схем http/https, проверяемым до открытия сокета. Файл вендорный («DO NOT EDIT HERE»), поэтому правка сделана в источнике правды — приватный `gasyoun/Uprava tools/probe_journeys.py` — и оттуда перевендорена сюда с сохранением семистрочной шапки происхождения; `selftest` зелёный в обеих копиях (N2 и calendar design tests воспроизводятся), отказ на `file://`/`ftp://` проверен вживую. **Приёмка:** шесть классов гидов + Telegram 47/47 green, `MarkdownImageCorpusTest` 3/3 + мутационная проверка, Pint clean, полный прогон 5167 тестов — 3 падения, ровно известный локальный шум macOS (GrammarLab UTF-8, catalog pretty URLs, vitrina waitlist), тот же список, что записан под H4279. Semgrep локально не прогонялся (ни бинаря, ни docker на этой машине) — проверяется прогоном CI на PR. (Opus 5 `claude-opus-5`, 07-09-2026, H4316)
