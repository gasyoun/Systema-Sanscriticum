- **Schedule-links watchdog + curator call ([d1040fee](https://github.com/gasyoun/Systema-Sanscriticum/commit/d1040fee), OxAlpha `z-ai/glm-5.3-flash`, 02-09-2026): `cabinet:probe` получил проверку `probeScheduleLinks` — БУДУЩИЕ занятия с TG-чатом группы, у которых на всех трёх уровнях fallback-цепочки (zoom_join_url → link → course.zoom_link) пусто, становятся soft-находкой пробы (горизонт 14 дней, `CABINET_PROBE_CHECK_SCHEDULE_LINKS`/`_HORIZON_DAYS`).** Закрывает слепое пятно инцидента 02-09 (курсы 401/399: серии без ссылок; guard уже не шлёт сломанное напоминание, но молча). По рулингу MG тревога идёт не только в soft-канал админа: ссылку заполняет куратор курса (Настя/Иван), поэтому проба напрямую зовёт `CuratorNotifier::scheduleLinkMissing` в curator-чат (`TELEGRAM_CURATORS_CHAT_ID`) с дедупом 24h на курс (Cache::add) — проба ходит */15, куратор не получает 96 постов в день. Звонок только на проде и вне --dry/--no-alert. Тесты: CabinetProbeScheduleLinksTest 6/6 (14 assertions: soft+curator push, silent-with-link, per-schedule-link, no-TG-chat ignored, --dry silent, 24h dedup), регрессия CabinetProbeHardeningTest+CabinetProbeTest 26/26 (72), Pint clean. Прод 4f6b659b→d1040fee, смоук `cabinet:probe --dry` зелёный (пустот нет после бэкфилла).
