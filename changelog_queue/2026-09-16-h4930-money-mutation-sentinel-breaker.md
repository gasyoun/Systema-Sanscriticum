_Created: 16-09-2026 · Last updated: 16-09-2026_

# H4930: sentinel_breaker over the Tochka webhook's paid→access grant — the only unattended money mutator found (Sonnet 5 `claude-sonnet-5`, 16-09-2026)

Mission (E002 layer 1, doctrine C3): put every unattended MUTATOR of access/grant/refund/payment rows under a sentinel_breaker budget that refuses at N+1. Verify-first inventory before wrapping anything: [WebhookController::handleTochkaWebhook()](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Http/Controllers/WebhookController.php) is the codebase's own documented "ЕДИНСТВЕННЫЙ автоматический триггер «оплачено -> доступ»" ([TochkaWebhookTest](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Webhooks/TochkaWebhookTest.php)'s own class comment) — the one guardian this pass wraps. `PaypalSubscriptionsWebhookController`, `ExpireClubMemberships`, `ConditionalAccessGranter`, `GiftCertificateService` and the `Rehearse*` commands (dry-run only, confirmed non-mutating in a live schedule) are inventoried but **deferred** — see follow-up handoff below.

- **`App\Support\SentinelBreakerGate`** ([app/Support/SentinelBreakerGate.php](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Support/SentinelBreakerGate.php)): a generic guardian-name/guardian-class CLI wrapper over Uprava's shared `tools/sentinel_breaker.py`, extracted from the H4691 `MadelineSyncBreaker` pattern (one CLI-call implementation, no second arithmetic copy). Fail-open: missing library, disabled flag, or a CLI error all resolve to "not frozen" — a broken predohranitel must never itself block a real payment.
- **Wired into the Tochka webhook's grant decision** (guardian `tochka_grant`, class `money_mutation`): `check` runs before the `paid` mutation, inside the SAME decision block that already computes resurrection/amount-mismatch rejections, so a frozen breaker journals as `PaymentWebhookEvent.decision = 'breaker_refused'` in the existing append-only audit log rather than a silent drop; `record` runs once, only after the mutation actually happened. The bank still gets a 200 (Tochka retries forever on non-2xx — H2085 lesson) but the group is **not** granted while frozen.
- **`guards:money-breaker-alarm`** ([app/Console/Commands/MoneyBreakerAlarm.php](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/MoneyBreakerAlarm.php)): the money-channel `ALARM_CMD` sentinel_breaker.py calls on freeze AND auto-unfreeze (MG ruling 5 — money gets its own channel, not the generic `guards:breaker-alarm` cabinet-probe chat). Reuses `MoneySliAlerter` (H4672) for the Better Stack heartbeat + TG send — no second delivery implementation.
- Default **OFF** (`features.money_mutation_breaker`, `MONEY_MUTATION_BREAKER` env) — with the flag off or the library absent, behaviour is byte-identical to pre-H4930.
- Tests: [`TochkaWebhookBreakerTest`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/tests/Feature/Webhooks/TochkaWebhookBreakerTest.php) (4 — flag-off never spawns the CLI, healthy breaker still grants, **the DoD's negative test: a frozen breaker refuses the grant and journals `breaker_refused`**, missing library fails open). Fake-CLI shape mirrors `MadelineSyncBreakerTest` (argv log + control file for the exit code); skipped on Windows (POSIX shell fixture), runs on Linux CI.
- **Known blocker for enabling the flag in prod**: Uprava's `tools/sentinel_breaker.py` `GUARDIAN_CLASSES` table does not yet define a `money_mutation` entry — `--class money_mutation` would `KeyError` today. Uprava is a guarded main-tree checkout; adding the class + a budget + SENTINELS.md registry rows for `tochka_grant` needs its own worktree/PR there, filed as a follow-up handoff. **Do not flip `MONEY_MUTATION_BREAKER=true` until that lands.**
- **Not done this pass** (out of the 45-minute unit's budget, honestly scoped): the payments-webhook success-rate SLI wired into Uprava's burn-rate ladder (2%/1h · 5%/6h · 10%/3d) — note that `money:sli-hourly-reconcile` (H4672) already computes and pages on a webhook-success-rate floor breach, so a functioning webhook SLI exists; what's missing is specifically the multi-window burn-ladder wiring the mission named. Follow-up handoff covers this plus the remaining mutator inventory above.

_Dr. Mārcis Gasūns_
