- **Посты расписания в чаты групп перестали падать с 400 «chat not found» (H4846, Opus 5 `claude-opus-5`, 15-09-2026).** Прод-логи 11/13/14-09 ~10:00 МСК: 7 разных chat_id по 3 ретрая, все — из ежедневного свипа `courses:post-schedule --due` (H4328, флаг `SCHEDULE_FULL_POST` включён). Диагноз НЕ «мёртвый чат»: живая read-only проба `getChat` по всем 33 группам с `telegram_chat_id` — основной бот (`services.telegram.bot_token`) не видит ни одного чата обучения (0/33), `@zapisi_ORSbot` видит 32/33; все остальные отправки в чат группы (напоминания, отмены, слот-уведомления) давно идут через `SendZapisiBotMessageJob`. [`SchedulePostSender`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Services/Schedule/SchedulePostSender.php) (свип + кнопка в админке) и тот же латентный класс в [`classes:post-group-link`](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/app/Console/Commands/PostClassLinkToGroupChat.php) переведены на `SendZapisiBotMessageJob`; ни одна строка `groups.telegram_chat_id` не тронута. Единственный чат, мёртвый для обоих ботов (одна группа), вынесен на решение человека в приватный хаб. Тесты: `SchedulePostSenderTest` +1 (пост уходит zapisi-ботом, основной не вызывается), `PostCourseScheduleCommandTest`, `PostClassLinkToGroupChatTest` — 25/25.
