<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Загрузка файлов домашнего задания
    |--------------------------------------------------------------------------
    |
    | Пороги вынесены сюда, а не в HomeworkController: их приходится менять
    | вместе с серверными limits (`upload_max_filesize`, `post_max_size` в
    | php-fpm и `client_max_body_size` в nginx), а те живут на VPS и в
    | репозитории не записаны. Держать оба конца в env — единственный способ
    | синхронизировать их без правки кода.
    |
    | ВАЖНО: `total_max_kb` должен оставаться НИЖЕ прод-значения post_max_size.
    | Иначе превышение ловит не Laravel (вежливая ошибка в форме), а PHP/nginx
    | (пустая страница 413, введённый текст ответа теряется).
    |
    | СВЕРЕНО НА ПРОДЕ 04-08-2026 (.92, /etc/php/8.3/fpm/php.ini — единственный
    | источник, ни conf.d/pool.d, ни .user.ini ничего не переопределяют):
    | upload_max_filesize=100M, post_max_size=110M, max_input_time=300,
    | nginx client_max_body_size=200M. До этой сверки здесь стояло «заявленного
    | post_max_size=100M», а на машине жили 64M/64M — то есть отправка между
    | 64 и 90 МБ ловилась не Laravel, а PHP, ровно тем самым молчаливым 413.
    | Правите пороги — идите на сервер и сверяйте, а не верьте этому абзацу.
    |
    */

    // Сколько файлов можно приложить к одной отправке.
    'max_files' => (int) env('HOMEWORK_MAX_FILES', 10),

    // Потолок на ОДИН файл, килобайты. 30 МБ — это ~2-3 минуты видео с
    // телефона в 720p, чего для ответа по заданию достаточно.
    'max_file_kb' => (int) env('HOMEWORK_MAX_FILE_KB', 30720),

    // Потолок на ОДИН файл С ПРАВКАМИ у проверяющего, килобайты. Ключ отдельный
    // от max_file_kb сознательно: студенту 30 МБ хватает, а проверяющий кладёт
    // разбор ПОВЕРХ работы (разметка, запись экрана), и ему дали 40 МБ, не
    // поднимая порог всем и не увеличивая поток студенческих заливок.
    // Пусто/0 — откат к студенческому порогу, поведение станет как до 04-08-2026.
    'feedback_max_file_kb' => (int) env('HOMEWORK_FEEDBACK_MAX_FILE_KB', 40960),

    // Потолок на ВСЮ отправку, килобайты. Держится с запасом ниже заявленного
    // post_max_size=100M, чтобы студент получал внятную ошибку валидации,
    // а не молчаливый 413 от nginx.
    'total_max_kb' => (int) env('HOMEWORK_TOTAL_MAX_KB', 92160),

    // Разрешённые расширения. Видео добавлено 19-07-2026 по запросу студентов;
    // отдельного пониженного лимита у него нет — оно укладывается в общий
    // max_file_kb, поэтому серверные настройки менять не потребовалось.
    'allowed_extensions' => [
        'pdf', 'doc', 'docx', 'txt',
        'jpg', 'jpeg', 'png', 'heic', 'webp',
        'mp3', 'm4a', 'ogg', 'wav',
        'mp4', 'mov', 'webm',
    ],

    /*
    |--------------------------------------------------------------------------
    | Проверяющие по группам (H1729)
    |--------------------------------------------------------------------------
    |
    | Механизм общий, а включение — данными: у кого нет строк в group_reviewer,
    | для того ничего не меняется. `enabled` тут — аварийный выключатель, чтобы
    | погасить фичу целиком без отката миграции.
    |
    */

    'reviewers' => [
        'enabled' => (bool) env('HOMEWORK_REVIEWERS_ENABLED', true),

        // Каналы оповещения проверяющего о новой сданной работе.
        // Допустимые: database (колокольчик в админке), mail, telegram.
        'notify_channels' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_REVIEWER_CHANNELS', 'database,mail,telegram')),
        ))),

        // Недельная сводка преподавателю курса вместо письма на каждую работу.
        // Заменяет персональные письма ТОЛЬКО для групп с активным проверяющим.
        'digest_enabled' => (bool) env('HOMEWORK_REVIEWER_DIGEST_ENABLED', true),

        // День недели (1 = понедельник) и время отправки сводки.
        'digest_day' => (int) env('HOMEWORK_REVIEWER_DIGEST_DAY', 1),
        'digest_time' => env('HOMEWORK_REVIEWER_DIGEST_TIME', '09:00'),

        /*
        |----------------------------------------------------------------------
        | Курсы, где приём ДЗ есть, а проверки преподавателем нет (H3081)
        |----------------------------------------------------------------------
        |
        | MG 18-08-2026: «продленка с домашкой, но без проверки домашки от
        | преподавателя, как и напевный санскрит».
        |
        | Это НЕ то же самое, что «у группы нет проверяющего». Курс без строк
        | в `group_reviewer` всё равно шлёт письмо преподавателю курса на
        | КАЖДУЮ сданную работу (`HomeworkNotifier::notifyCourseTeacher`) —
        | то есть отсутствие проверяющего означает «проверяет преподаватель
        | лично», а не «никто». Здесь нужно именно «никто»: работа принимается
        | и хранится, но никого не дёргает и студенту ничего не обещает.
        |
        | Задано ПРЕФИКСОМ: правило привязано к формату курса, а не к номеру
        | потока, поэтому «Напевный санскрит — гимн Кали (2027)» унаследует его
        | без дописывания в список.
        |
        */
        'unreviewed_course_prefixes' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_UNREVIEWED_PREFIXES', 'napevnyi-sanskrit-,prodlenka-')),
        ))),
    ],

    /*
    |--------------------------------------------------------------------------
    | Автооткрытие приёма ДЗ после проведённого урока (H1764, волна 1)
    |--------------------------------------------------------------------------
    |
    | Открытие считается от момента, когда у урока ВПЕРВЫЕ появилась запись
    | (`lessons.recording_attached_at`): +delay_hours, затем выравнивание на
    | ближайший align_hour:00 по Москве. Ни одного числа в коде — все они здесь.
    |
    | ОХВАТ ИНВЕРТИРОВАН 18-08-2026 (H3078, рулинг MG). Раньше `course_slugs`
    | был allowlist и по умолчанию пуст — «фича спит». Это дважды дало тихий
    | отказ: гр.62 просто забыли внести (H3068), а каждый новый курс молчал бы
    | так же. Теперь наоборот: в охвате КАЖДЫЙ курс, чей первый урок не раньше
    | `min_course_start`, минус `exclude_course_slugs`. Руками не вносят ничего.
    |
    | Две защёлки, без которых инверсия открыла бы 41 урок задним числом:
    |   `min_course_start` — отсекает архивные потоки гр.51/53/55/57/58 (сотни
    |     уроков с записями). Пусть остаются записями, их не трогают.
    |   `policy_epoch` — урок, чей `homework_opens_at` РАНЬШЕ эпохи, не
    |     открывается НИКОГДА. Это и есть «впредь»: гр.50 (первый урок
    |     02.06.2026) проходит по дате старта, но её июньские уроки уже
    |     в прошлом и приёма не получат.
    |
    */

    'auto_open' => [
        // Аварийный выключатель всей фичи — гасит её без отката миграции.
        'enabled' => (bool) env('HOMEWORK_AUTO_OPEN_ENABLED', true),

        // 'all' — охват по умолчанию (denylist, рулинг MG 18-08-2026).
        // 'listed' — прежнее поведение: только `course_slugs`. Оставлено как
        // путь отката, а не как рабочий режим.
        'scope' => (string) env('HOMEWORK_AUTO_OPEN_SCOPE', 'all'),

        // Курс в охвате, только если его ПЕРВЫЙ урок не раньше этой даты.
        // Пусто — без нижней границы (не рекомендуется: откроет архивы).
        'min_course_start' => (string) env('HOMEWORK_AUTO_OPEN_MIN_COURSE_START', '2026-06-01'),

        // Урок не открывается, если его расчётный момент открытия РАНЬШЕ этой
        // даты. Ретро-выкатка политики не должна вываливать пачку уведомлений
        // за прошедшие недели. Пусто — без эпохи (открывать и задним числом).
        'policy_epoch' => (string) env('HOMEWORK_AUTO_OPEN_POLICY_EPOCH', '2026-08-18'),

        // Исключения из общего охвата. Пусто = исключений нет.
        'exclude_course_slugs' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_AUTO_OPEN_EXCLUDE_COURSES', '')),
        ))),

        // Исключения ПРЕФИКСОМ слага, а не поимённо (H3081, MG 18-08-2026:
        // «Айтарея без домашки»). Формат курса, а не отдельный курс: следующее
        // чтение заведут как `ctenie-…` и оно унаследует правило само, без
        // дописывания в список — ровно то, ради чего охват стал правилом.
        'exclude_course_prefixes' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_AUTO_OPEN_EXCLUDE_PREFIXES', 'ctenie-')),
        ))),

        // Только при `scope = listed`. Рабочим режимом больше не является.
        'course_slugs' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_AUTO_OPEN_COURSES', '')),
        ))),

        // Курсы Кочергиной опознаются по слагу — тем же соглашением, по
        // которому их заводят. Нужно для ДВУХ вещей: текст задания берётся из
        // учебника только здесь, и только здесь действует потолок ниже.
        'kochergina_slug_prefix' => (string) env('HOMEWORK_AUTO_OPEN_KOCHERGINA_PREFIX', 'grammatika-po-kocerginoi-'),

        // «Обязательная сдача в группах санскрита заканчивается до 6 урока
        // Кочергиной, 6 уже не сдают» (MG 18-08-2026). Считаются уроки курса
        // с включённым приёмом — независимо от того, автомат их открыл или
        // человек. 0 — без потолка.
        'kochergina_max_homeworks' => (int) env('HOMEWORK_AUTO_OPEN_KOCHERGINA_MAX', 5),

        // Урок в охвате, у которого `textbook_lesson` не проставлен, ВСЁ РАВНО
        // открывается — с отсылочной формулировкой KocherginaExerciseSource::
        // fallbackPrompt(null). До 18-08-2026 такой урок молча выпадал из
        // выборки: пилот гр.60/гр.61 встал 28.07.26 на занятии #3 и студенты
        // три недели не имели куда прикрепить работу (H3068). Ровно этот риск
        // был записан в VERIFICATION §«textbook_lesson никто не проставит» и
        // отложен в волну 3 — здесь он закрыт с другой стороны: отсутствие
        // маппинга ухудшает ТЕКСТ задания, но не отменяет приём.
        // false — вернуть прежнее поведение (жёсткое требование маппинга).
        'open_without_textbook_lesson' => (bool) env('HOMEWORK_AUTO_OPEN_WITHOUT_TEXTBOOK_LESSON', true),

        // Кому слать напоминание «урок открыт без textbook_lesson»: пусто —
        // не слать. Значения: admins (TelegramAdminNotifier).
        'missing_mapping_alert' => (bool) env('HOMEWORK_AUTO_OPEN_MISSING_MAPPING_ALERT', true),

        // Какие занятия учебника участвуют (lessons.textbook_lesson).
        'textbook_lessons' => array_values(array_filter(array_map(
            'intval',
            array_filter(array_map(
                'trim',
                explode(',', (string) env('HOMEWORK_AUTO_OPEN_LESSONS', '1,2,3,4,5')),
            )),
        ))),

        // Выдержка после появления записи, часы (D3).
        'delay_hours' => (int) env('HOMEWORK_AUTO_OPEN_DELAY_HOURS', 12),

        // Час МСК, на который выравнивается открытие: не шлём пуши ночью.
        'align_hour' => (int) env('HOMEWORK_AUTO_OPEN_ALIGN_HOUR', 9),

        // Каналы пуша об открытии. Письма тут нет сознательно (D9): письмо
        // после каждого занятия — шум. Допустимые: telegram, vk.
        'notify_channels' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_AUTO_OPEN_CHANNELS', 'telegram,vk')),
        ))),

        // D8 — закрытие предыдущего ДЗ при открытии следующего. Волна 2;
        // включается отдельно и осознанно, после спайка по опоздавшим сдачам.
        'close_previous' => (bool) env('HOMEWORK_AUTO_OPEN_CLOSE_PREVIOUS', false),

        // Оцифровка учебника Кочергиной — источник текста упражнений.
        // Читается из СОСЕДНЕГО клона: текст 1998 года под охраной авторского
        // права и в этот публичный репозиторий не копируется (D6, D14).
        'source_path' => env(
            'KOCHERGINA_SOURCE_PATH',
            base_path('../SanskritGrammar/KocherginaUchebnik_1998/Kochergina_unicode.mdx'),
        ),

        /*
        |----------------------------------------------------------------------
        | Generic / Hindi track (immediate open, fixed prompt)
        |----------------------------------------------------------------------
        |
        | Отдельный охват от Кочергиной: без textbook_lesson, без +12ч/09:00.
        | При первом появлении записи урок открывается сразу с формулировкой
        | generic_prompt («Домашнее задание»). Пусто generic_course_slugs =
        | трек спит (выкатка безопасна по умолчанию).
        |
        | Не кладите один slug и в course_slugs, и в generic_course_slugs:
        | generic-immediate сработает на saved раньше hourly kochergina-path.
        |
        */

        'generic_course_slugs' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('HOMEWORK_AUTO_OPEN_GENERIC_COURSES', '')),
        ))),

        'generic_prompt' => (string) env(
            'HOMEWORK_AUTO_OPEN_GENERIC_PROMPT',
            'Домашнее задание',
        ),

        // Kill-switch только для immediate-трека (не гасит Кочергину).
        'generic_immediate' => (bool) env('HOMEWORK_AUTO_OPEN_GENERIC_IMMEDIATE', true),
    ],

    /*
    |--------------------------------------------------------------------------
    | #ДЗ из Telegram-чата группы (волна 2)
    |--------------------------------------------------------------------------
    |
    | Преподаватель пишет в чат группы `#ДЗ …` (или `/dz …`) — платформа
    | обновляет homework_prompt у урока. Резолвер: C (reply на пост бота) →
    | B (одно открытое placeholder-ДЗ) → A (свежая запись) → G (кнопки).
    |
    | Требует: бот кабинета в чате группы, groups.telegram_chat_id заполнен,
    | privacy mode OFF у бота ИЛИ команда /dz (иначе Telegram не пришлёт
    | свободный текст с хэштегом).
    |
    */

    'tg_tag' => [
        'enabled' => (bool) env('HOMEWORK_TG_TAG_ENABLED', true),

        // Сколько часов назад смотреть recording_attached_at для шага A.
        'lookback_hours' => (int) env('HOMEWORK_TG_TAG_LOOKBACK_HOURS', 72),

        // Постить в чат группы «ответьте #ДЗ» при generic-автооткрытии (звено C).
        'post_open_invite' => (bool) env('HOMEWORK_TG_TAG_POST_OPEN_INVITE', true),

        // TTL черновика prompt при неоднозначности (кнопки G), минуты.
        'pending_ttl_minutes' => (int) env('HOMEWORK_TG_TAG_PENDING_TTL', 60),
    ],

];
