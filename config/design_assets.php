<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Форматы баннеров курса
    |--------------------------------------------------------------------------
    |
    | Порядок = порядок колонок в матрице «Дизайн курсов». Курс считается
    | укомплектованным, когда закрыты ВСЕ перечисленные форматы. Добавление
    | четвёртого формата здесь автоматически добавляет колонку на странице и
    | делает уже укомплектованные курсы неполными — это осознанное поведение
    | (появилось новое требование к дизайну), а не баг.
    |
    */

    'formats' => ['16:9', '9:16', '4:3'],

    /*
    |--------------------------------------------------------------------------
    | Где лежат файлы
    |--------------------------------------------------------------------------
    |
    | Картинки — на публичном диске: их потребитель это посты и лендинги, где
    | нужна прямая ссылка, и это рекламные баннеры, а не персональные данные.
    | PSD — на приватном диске (как proof_path у платежей и файлы импорта):
    | исходник миру не нужен, отдаём его только персоналу через маршрут с гейтом.
    |
    */

    'image_disk' => env('DESIGN_IMAGE_DISK', 'public'),
    'image_dir' => 'course-design',

    'psd_disk' => env('DESIGN_PSD_DISK', 'local'),
    'psd_dir' => 'course-design-psd',

    /*
    |--------------------------------------------------------------------------
    | Лимиты загрузки (КБ)
    |--------------------------------------------------------------------------
    |
    | ВНИМАНИЕ: max_psd_kb ОБЯЗАН быть ниже LIVEWIRE_UPLOAD_MAX_KB (config/
    | livewire.php, дефолт 102400). Livewire валидирует размер РАНЬШЕ, чем
    | maxSize() конкретного поля, поэтому поле с бо́льшим лимитом молча режет
    | загрузки — по симптомам это не диагностируется. Инвариант закрыт тестом
    | CourseDesignConfigTest.
    |
    | Прод (проверено 10-08-2026): PHP-FPM upload_max_filesize=100M,
    | post_max_size=110M, nginx client_max_body_size=200M. То есть потолок
    | загрузки — 100 МБ; всё, что тяжелее, учитывается только ссылкой psd_url.
    |
    */

    'max_image_kb' => (int) env('DESIGN_MAX_IMAGE_KB', 10240),   // 10 МБ
    'max_psd_kb' => (int) env('DESIGN_MAX_PSD_KB', 98304),       // 96 МБ

    /*
    |--------------------------------------------------------------------------
    | Допуск сверки пропорции
    |--------------------------------------------------------------------------
    |
    | Относительное отклонение w/h от заявленного формата, при котором картинка
    | считается соответствующей. 1600×900 и 1920×1080 — оба ровно 16:9, но
    | дизайнер может сдать 1200×901. Расхождение НЕ блокирует загрузку (это
    | учёт, а не гейт) — оно помечается бейджем в матрице.
    |
    */

    'ratio_tolerance' => (float) env('DESIGN_RATIO_TOLERANCE', 0.02),

    /*
    |--------------------------------------------------------------------------
    | Перенос обложки витрины в слот баннера
    |--------------------------------------------------------------------------
    |
    | cover_disk — диск, где лежит courses.image_path (обложка витрины). Задан
    | отдельным ключом, а НЕ взят из filesystems.default: в phpunit.xml
    | переменная FILESYSTEM_DISK не задана, и тесты разъехались бы с продом.
    |
    | import_copy_tolerance — при каком отклонении от формата картинку НЕ
    | трогаем, а копируем байт-в-байт. Ноль означает «копия только если
    | пропорция уже точная, иначе кадрируем» — так каждый перенесённый баннер
    | получается ровно 4:3, правило простое и проверяемое глазом. Поставьте
    | 0.02, если дороже сохранность исходных байт: перекодирование JPEG всегда
    | теряет качество, а GIF и WebP — ещё и анимацию. На цвет ячейки в матрице
    | это не влияет, ratio_tolerance выше и так прощает такое отклонение.
    |
    | Качество 88 визуально неотличимо от 100 на фотографиях и градиентах при
    | вдвое меньшем весе; ниже 80 на кириллице внутри баннера лезут артефакты
    | вокруг букв. Сжатие PNG 6 — дефолт GD, оно без потерь.
    |
    */

    'cover_disk' => env('DESIGN_COVER_DISK', 'public'),

    'import_copy_tolerance' => (float) env('DESIGN_IMPORT_COPY_TOLERANCE', 0.0),

    'import_jpeg_quality' => (int) env('DESIGN_IMPORT_JPEG_QUALITY', 88),
    'import_webp_quality' => (int) env('DESIGN_IMPORT_WEBP_QUALITY', 88),
    'import_png_compression' => (int) env('DESIGN_IMPORT_PNG_COMPRESSION', 6),

];
