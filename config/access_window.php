<?php

declare(strict_types=1);

return [

    /*
     | H4468 — скоуп окон доступа (course_access_windows). Рулинг MG 09-09-2026
     | касается курсов Парибока («не надо к курсам Парибка вечный доступ») —
     | окна через Filament выдаются только на эти курсы. Расширение скоупа —
     | правка этого списка, не код. Пусто = UI-выдача запрещена ( artisan
     | access:set-window остаётся выручкой).
     */
    'enabled_course_ids' => [327, 336, 396],

    // Дефолтный срок окна по запросу студента — «пакет 30 дней» (MG 09-09).
    'default_window_days' => 30,

];
