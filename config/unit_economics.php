<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Юнит-экономика ученика — пороги светофора и горизонты (H256).
    |--------------------------------------------------------------------------
    | Держим пороги здесь (а не хардкодом в вёрстке страницы), чтобы
    | делегированный оператор/бухгалтер видел «красное/жёлтое/зелёное» без
    | правки кода, а MG мог поменять числа через .env без деплоя.
    |
    | Смысл метрик — из антикейса школы «Лингвистик»: продукт хороший, а денег
    | нет, потому что CAC (стоимость привлечения) превышал LTV (доход с ученика),
    | а окупаемость требовала ≥9 месяцев удержания, которого не было.
    */

    // Отток: при разовых курсах без подписки ученик считается «ушедшим», если у
    // него нет ни одной оплаты N месяцев к концу выбранного периода.
    'churn_months' => (int) env('ECON_CHURN_MONTHS', 6),

    // Горизонт кривой удержания в месяцах (сколько столбцов строить).
    'retention_horizon_months' => (int) env('ECON_RETENTION_HORIZON', 12),

    // Светофор отношения LTV/CAC. ≥ green — здоровая экономика; < red — привлечение
    // в убыток (ученик приносит меньше, чем стоило его привести).
    'ltv_cac' => [
        'green' => (float) env('ECON_LTV_CAC_GREEN', 3.0),
        'red' => (float) env('ECON_LTV_CAC_RED', 1.0),
    ],

    // Светофор «месяцев до окупаемости CAC»: ≤ green — окупается быстро; > red —
    // окупаемость дольше реального удержания (как в антикейсе — ≥9 мес.).
    'payback' => [
        'green_months' => (float) env('ECON_PAYBACK_GREEN', 3.0),
        'red_months' => (float) env('ECON_PAYBACK_RED', 9.0),
    ],
];
