<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | O2 activation + C4 завершаемость (H3764)
    |--------------------------------------------------------------------------
    |
    | Пороги и окна живут здесь, а не в вёрстке страницы: цифра без названного
    | знаменателя — не факт (урок измерительного хребта H2378). Страница
    | «Активация и завершаемость» читает ТОЛЬКО эти значения.
    */

    /*
     | Сколько последних месячных когорт показывать в воронке активации.
     | Когорта = календарный месяц ПЕРВОЙ доступо-открывающей покупки ученика
     | (тот же якорь, что у юнит-экономики ученика — знаменатель не раздваиваем).
     */
    'cohort_months' => (int) env('ACTIVATION_COHORT_MONTHS', 12),

    /*
     | Доля пройденных уроков, начиная с которой ученик считается «дошедшим до
     | конца» курса/потока. 0.8 = 80 %. Сертификат считается отдельной, более
     | строгой метрикой (выдан человеком, а не порогом).
     */
    'completion_lesson_ratio' => (float) env('ACTIVATION_COMPLETION_RATIO', 0.8),

    /*
     | Сколько курсов и потоков показывать в блоке завершаемости (по убыванию
     | размера знаменателя). 0 — все.
     */
    'completion_rows' => (int) env('ACTIVATION_COMPLETION_ROWS', 20),

    /*
     | Минимальный размер когорты/знаменателя, ниже которого процент НЕ
     | показывается: 1 из 2 — это не 50 % завершаемости, это шум.
     */
    'min_denominator' => (int) env('ACTIVATION_MIN_DENOMINATOR', 5),
];
