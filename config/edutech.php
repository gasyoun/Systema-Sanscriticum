<?php

declare(strict_types=1);

/*
 * H4435 (MG 08-09/09-09): Edutech-конфиг «Канва» — шкала учебника для
 * грамматических курсов. Правится без деплоя.
 *
 * МОДЕЛЬ v2: две шкалы никогда не смешиваются — «наши занятия» (календарные)
 * и «уроки учебника». Связь событийная; прогресс группы = курсор по учебнику.
 * Вес урока эмпирический: середина книги — 2-5 наших занятий на урок,
 * КОНЕЦ ТЯЖЁЛЫЙ (MG 09-09) — урок у конца может занимать 2-3 наших занятия.
 */

return [

    // Семейства канвы: имя → regex заголовков записей уроков + всего уроков в учебнике.
    'canvas_families' => [
        'kochergina' => ['pattern' => '/Кочергина\s+(\d+)\s*\(([^)]+)\)/u', 'total' => 40, 'title' => 'Кочергина'],
        'buhler' => ['pattern' => '/Бюллер\s+(\d+)\s*\(([^)]+)\)/u', 'total' => 40, 'title' => 'Бюллер'],
    ],

    // Бюджеты ответвлений-источников в НАШИХ занятиях (эталон Гасунца, 09-09).
    'deviation_budgets' => [
        'emeno' => 5,
    ],

    // Окно проекции «до конца учебника» — по последним M тронутым урокам группы.
    'projection_window' => 5,

    // H4443: блоки. Занятий-записей в блоке (H3823-структура); истина — маркеры
    // заголовков (*X.Y*) и Tariff.block_number, конфиг — последний фолбэк.
    'lessons_per_block' => 4,

    // H4443: каникулы для прогноза финала (MG 09-09): НГ 2 недели, лето
    // норма 15.06-15.09, максимально поздно до 15.10.
    'holidays' => [
        'new_year' => ['from' => '12-29', 'to' => '01-11'], // 2 недели через НГ
        'summer_normal' => ['from' => '06-15', 'to' => '09-15'],
    ],

    // Окно rolling-темпа группы (занятий/неделю) в неделях.
    'cadence_window_weeks' => 8,
];
