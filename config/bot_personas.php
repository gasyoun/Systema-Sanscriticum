<?php

declare(strict_types=1);

/*
 * Персоны ИИ-куратора (H3520). Каркас переключаемых голосов для
 * BotKnowledgeBase::persona(string $key).
 *
 * Активна только 'default' — текущий контракт FAQ-куратора байт-в-байт.
 * Флаг BOT_MULTI_PERSONA=false полностью выключает механику (любой ключ
 * возвращает дефолтную персону).
 *
 * Содержания не-FAQ персон (долги, реактивация) и правило переключения по
 * контексту — отдельное решение MG (@DECIDE, след х3520); до того сюда
 * новые ключи НЕ добавлять.
 */

return [

    'personas' => [

        'default' => [
            'label' => 'ИИ-куратор Академии (FAQ)',
            'enabled' => true,
        ],

    ],

];
