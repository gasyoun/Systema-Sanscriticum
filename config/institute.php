<?php

return [
    // Строка реквизитов для страницы /mecenaty (многострочная).
    // Заполняется MG: ИНН/счёт/банк ИП Гасунса М. Ю. Пусто = блок показывается пустым.
    'donate_requisites' => env('INSTITUTE_DONATE_REQUISITES', ''),

    // N2: онлайн-приём пожертвований через Точку (одношиотовая ссылка).
    // Флаг по умолчанию OFF — включается MG после ратификации пресетов и
    // юр-формулировки ст. 582 (money row, INSTITUTE_MONETIZATION_PLAN_2026H2).
    'donate_online' => (bool) env('INSTITUTE_DONATE_ONLINE', false),

    // Кандидаты пресетов и нижняя граница свободной суммы (₽). Цифры — money row:
    // публикуются только после ратификации MG.
    'donate_presets' => [500, 1000, 2500, 5000],
    'donate_min' => 100,
];
