<?php

return [
    // Строка реквизитов для страницы /mecenaty (многострочная).
    // Заполняется MG: ИНН/счёт/банк ИП Гасунса М. Ю. Пусто = блок показывается пустым.
    'donate_requisites' => env('INSTITUTE_DONATE_REQUISITES', ''),

    // Онлайн-приём пожертвований через Точку (шаг N2 плана института).
    // Default OFF: /mecenaty живёт в режиме реквизитов, POST-роут отдаёт 404 —
    // тёмный деплой безопасен. Флаг включает человек ПОСЛЕ ратификации пресетов
    // и юр-рамки (пожертвование ст. 582 vs услуга по оферте — @DECIDE MG).
    'donations_enabled' => env('INSTITUTE_DONATIONS_LIVE', false),

    // Пресеты сумм — money row MG: публикуются только после его слова.
    // Пусто = форма показывает только поле свободной суммы.
    // Ратифицировано MG 23-08 поздним вечером («закрываю все 4 — да»):
    // пресеты подсказки на форме доната; свободная сумма остаётся основным вводом.
    'donate_presets' => [500, 1000, 2500, 5000],

    // Механические границы суммы в рублях (не ценник): отсекают нули, мусор
    // и опечатки. Не «рекомендованная сумма» — просто валидация ввода.
    'donate_min' => 100,
    'donate_max' => 300000,

    // Открытые лекции МГ (план института N5): id курсов, записи которых
    // подчиняются лекционному контракту доступа — живьём бесплатно всем,
    // запись бесплатна при ЛЮБОМ платном членстве и покупаема неплатными
    // через обычный тарифный контур курса. Пусто = правило нигде не действует
    // (поведение RecordingAccessPolicy для остальных курсов не меняется).
    'lecture_course_ids' => array_map(
        intval(...),
        array_filter(explode(',', (string) env('INSTITUTE_LECTURE_COURSE_IDS', '')))
    ),
];
