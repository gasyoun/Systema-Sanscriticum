<?php

declare(strict_types=1);

/*
 |--------------------------------------------------------------------------
 | Деловые часы поддержки (H1199, Jivo-паритет S4/S5)
 |--------------------------------------------------------------------------
 |
 | Когда 'enabled' = false (по умолчанию), веб-виджет всегда показывает себя
 | «онлайн» — текущее поведение не меняется. Когда true, App\Support\
 | SupportAvailability::isOnline() сверяет текущее время (в 'timezone') с
 | 'schedule' и виджет переключается на оффлайн-копирайт вне окна (H1199 S4).
 | Это НЕ реальный presence кураторов — просто расписание, которое настраивает
 | человек; см. App\Support\SupportAvailability.
 |
 | 'schedule' — ключи D-формата PHP (Mon..Sun, нижний регистр), значение —
 | [start, end] в 'H:i' (полуоткрытый интервал [start, end)) или null (весь
 | день недоступно).
 */
return [
    'enabled' => (bool) env('SUPPORT_HOURS_ENABLED', false),

    'timezone' => env('SUPPORT_HOURS_TIMEZONE', 'Europe/Moscow'),

    'schedule' => [
        'mon' => ['10:00', '20:00'],
        'tue' => ['10:00', '20:00'],
        'wed' => ['10:00', '20:00'],
        'thu' => ['10:00', '20:00'],
        'fri' => ['10:00', '20:00'],
        'sat' => null,
        'sun' => null,
    ],
];
