<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Юнит-экономика — ставки для страницы «Юнит-экономика» (Filament).
    |--------------------------------------------------------------------------
    | Держим ставки здесь (а не хардкодом в расчёте и не в миграции), чтобы
    | менять через .env без деплоя логики. Значения по умолчанию — как заданы
    | ИП Гасунс: УСН 6% с дохода; эквайринг Точки — карта 2.9%, СБП 0.4%.
    */

    // УСН «доходы», % с выручки.
    'usn_income_pct' => (float) env('ECON_USN_PCT', 6.0),

    // Комиссия эквайринга Точки, % по способу оплаты.
    'acquiring' => [
        'card' => (float) env('ECON_ACQ_CARD_PCT', 2.9),
        'sbp' => (float) env('ECON_ACQ_SBP_PCT', 0.4),
    ],
];
