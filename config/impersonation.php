<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Режим просмотра за пользователя (H1947) — предохранители
|--------------------------------------------------------------------------
|
| Мастер-рубильник живёт в config/features.php ('staff_impersonation'), здесь —
| только границы режима. Правило, выбранное в H1947 (открытый вопрос №1):
| режим ЧИТАЮЩИЙ. Обычная навигация, просмотр уроков и домашек работают, а любая
| ЗАПИСЬ по денежному контуру или контуру выдачи доступа возвращает 403 и пишется
| в лог. Иначе «посмотреть глазами студента» однажды закончится реальным списанием,
| возвратом или выданным доступом от имени человека, который об этом не просил.
|
| Списки — префиксные и по ИМЕНИ маршрута, и по URI: имя надёжнее (переживает
| смену пути), URI ловит то, что именем не названо.
|
*/

return [

    /*
     | Методы, считающиеся записью. GET/HEAD/OPTIONS не блокируем никогда —
     | иначе режим потеряет смысл (нельзя будет даже открыть страницу оплаты,
     | чтобы увидеть, что видит студент).
     */
    'write_methods' => ['POST', 'PUT', 'PATCH', 'DELETE'],

    /*
     | Префиксы ИМЁН маршрутов, запись по которым запрещена в режиме.
     | checkout./payment./deposit./paypal. — покупка и возврат;
     | student.debt. — оплата и перенос обещаний;
     | partner./referral. — партнёрские выплаты и реферальный кошелёк.
     */
    'blocked_route_prefixes' => [
        'checkout.',
        'payment.',
        'deposit.',
        'paypal.',
        'student.debt.',
        'partner.',
        'referral.',
    ],

    /*
     | Префиксы URI (без ведущего слэша), запись по которым запрещена в режиме —
     | страховка для маршрутов без имени. Сопоставление — как в Request::is(),
     | то есть 'checkout' закрывает и '/checkout/{tariff}/promo'.
     */
    'blocked_uri_prefixes' => [
        'checkout',
        'payment',
        'deposit',
        'paypal',
        'debt',
    ],

    /*
     | Высота плашки режима в пикселях: на неё сдвигается контент (padding-top
     | у body + top у фиксированных сайдбара и топбара Filament).
     */
    'banner_height' => 46,
];
