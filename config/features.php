<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Фича-флаги
    |--------------------------------------------------------------------------
    | Зачёт ранее оплаченного при докупке (половина блока → целый блок,
    | блоки → полный курс) теперь всегда включён и реализован корректно
    | в Tariff::upgradeCreditForUser(). Отдельный флаг больше не нужен.
    */
];
