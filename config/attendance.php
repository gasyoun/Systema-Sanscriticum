<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Консолидированный дашборд посещаемости (GetCourse-паритет GC-B2, H553).
    |--------------------------------------------------------------------------
    | Пороги отчёта — здесь, не хардкодом в странице/сервисе (см. паттерн
    | config/conversion.php, config/receivables.php).
    */

    // Сколько ПОСЛЕДНИХ ПОДРЯД занятий студент должен пропустить, чтобы попасть
    // в список «хронические неявки». @DECIDE MG: точный порог.
    'chronic_absence_threshold' => (int) env('ATTENDANCE_CHRONIC_THRESHOLD', 3),

    // Окно (дней) отчёта по умолчанию — рейты по студенту/группе/курсу и список
    // хронических неявок считаются по занятиям за последние N дней.
    'default_window_days' => (int) env('ATTENDANCE_DEFAULT_WINDOW_DAYS', 90),

    // Сколько недель показывать в тренде посещаемости по неделям.
    'trend_weeks' => (int) env('ATTENDANCE_TREND_WEEKS', 12),
];
