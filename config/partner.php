<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Партнёрская (агентская) программа — B2B, отдельная от студенческой
    |--------------------------------------------------------------------------
    | Внешний партнёр (коллега, знакомый по совместным проектам) рекомендует
    | наши курсы и получает ФИКСИРОВАННОЕ денежное вознаграждение за каждого
    | приведённого клиента, который впервые оплатил курс. В отличие от
    | студенческой реферальной программы (config/referral.php — кредит на счёт
    | самого студента), здесь вознаграждение — реальная ВЫПЛАТА партнёру:
    | начисляется как «долг к выплате» (partner_conversions), администратор
    | отмечает выплату вручную в админке.
    |
    | Всё за флагом `enabled`, по умолчанию ВЫКЛ — включать осознанно, когда
    | программа готова к запуску (см. docs/partner-program.md).
    */

    // Мастер-переключатель. Пока ВЫКЛ: приём заявок партнёров, атрибуция клиентов
    // и начисление вознаграждений не активны, публичный лендинг отдаёт 404.
    'enabled' => (bool) env('PARTNER_PROGRAM_ENABLED', false),

    // Фиксированное вознаграждение (₽) за одного приведённого клиента, впервые
    // оплатившего курс. Может быть переопределено на конкретном партнёре
    // (partners.reward_amount_override) — напр. для особых условий.
    'reward_amount' => (float) env('PARTNER_REWARD_AMOUNT', 1000),

    // Общий секрет API внешнего Telegram-бота (X-Partner-Bot-Secret).
    // Enforce-if-configured: пусто → проверка секрета не требуется (dev).
    'bot_secret' => (string) env('PARTNER_BOT_SECRET', ''),
];
