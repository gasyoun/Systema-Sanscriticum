# Project Objective: Laravel-платформа онлайн-обучения (кабинет, магазин курсов, лендинги, редактор лекций, админка) — поддержка и развитие; денежно-/доступно-критичное ядро под особой защитой.

## ➡️ Next Steps (Queue) — следующая волна

Весь агент-доступный P0/P1/P2/P3-бэклог доставлен (см. ✅ ниже / git). Новая волна — три направления (решение пользователя 2026-06-26):

### A. Hardening / технический долг (приоритет волны)
- [x] **Cast-ловушка `User`/`Announcement` — устранена** (ветка `chore/roadmap-next-wave-and-user-casts`): корень — метод `casts()` появился только в Laravel 11, а проект на **Laravel 10.50.2**, где Eloquent читает ИСКЛЮЧИТЕЛЬНО свойство `$casts` (подтверждено в вендоре `HasAttributes::getCasts`). Поэтому `casts()` в `User` и `Announcement` молча игнорировался — НИ ОДИН каст не применялся (datetime/bool/int приходили строками; `password=>hashed` не работал). Оба переведены на `protected $casts = [...]`. **Побочно починен латентный баг:** `Announcement::target_groups/target_courses` теперь реально кастуются в array → `array_intersect` в `StudentController:561` больше не TypeError-in-waiting (раньше спасал только guard `empty()`). Безопасность активации проверена: `password=>hashed` идемпотентен (`Hash::isHashed`, все записи и так через `Hash::make`), mobile-API payload — явный whitelist без datetime (контракт не меняется), нет string-зависимого кода (`json_decode`/`=== '1'`). **Полный прогон 643 passed (1684 assertions), Pint clean.** Ручные `Carbon::parse()`/`(int)` оставлены как дешёвая идемпотентная защита (комментарии обновлены).
- [ ] **Тест-покрытие денежно-/доступно-критичных путей** — добить остатки: upgrade-credit (`Tariff::upgradeCreditForUser`, containment-модель), loyalty/deposit в `calculateFinalPriceForUser`, грани `block_N_hH` (половины блоков) в `unlockingKeys`/`isUnlockedBy`. Цель — ни одного непокрытого ветвления в доступе.
- [ ] **Security-проход** по вебхукам/secret-rotation: ревизия всех webhook-эндпоинтов (Tochka/Telegram/VK/MAX/Zoom) на единый паттерн подписи + fail-policy; проверить, что `max_webhook_secret` (секрет в URL) и прочие шифруются и ротируемы (см. CLAUDE.md). Запустить `/security-review` на накопленном diff волны.
- [ ] **Долговой свип:** инлайн-литералы/дубли констант, мёртвые конфиги (`prana.rewards.referral` после перехода рефералки на денежный кредит — удалить/задокументировать), Pint clean по всему дереву.

### B. Cross-repo lecture-ui (Python, follow-up редактора v2)
- [ ] **Разметка dialog-блоков в `template.html.j2`** (lecture-ui) — Phase B add-block/dialog требует `data-block-index` не только на speech/figure. Сейчас структурное редактирование работает только по размеченному. Расширить шаблон + pytest (`lecture-ui/tests/test_template_render.py`).
- [ ] **Визуальный pre-apply AI-diff** — отложено в Phase C: нужен diff-payload от Python-сервиса (apply=false) для показа правок ИИ до применения. «Reject» уже покрыт откатом к бэкапу; это — UX-улучшение поверх.
- *Прежде чем строить — свериться с `SHARED_CODE.md` и `Uprava/PROJECT_INTERLINKS.md` (кросс-репо владелец шаблона/пайплайна).*

### C. Polish / UX
- [ ] Проход по кабинету (`/dvaram`: prana-таб, онбординг, чат поддержки, карточки курсов), админ-панелям и лендингам — вёрстка/адаптив/копирайт. Использовать skill `blade-styling` (Playwright-loop, не верстать вслепую) для `resources/views/`.
- [ ] Конкретные кандидаты собрать из реального прохода по страницам; держать изменения вне денежной семантики.

### D. Telegram support analytics
- [x] Локально выполнен Composer install с PHP 8.2+ и MadelineProto autoload (`danog/madelineproto 8.7.0`). Рабочий workaround: `composer config source-fallback true`, затем `composer install`; deprecated config НЕ оставлен в `composer.json`.
- [x] Локально запущен XAMPP MySQL (`mysqld.exe`) и применены Telegram support migrations точечно: `2026_06_29_120000...` и `2026_06_29_130000...`. Полный `artisan migrate --force` всё ещё блокируется старой unrelated миграцией `dictionary_words` → FK на `dictionaries`.
- [x] Локально добавлен `.env` блок Telegram support с `TELEGRAM_SUPPORT_ENABLED=false`; команда `telegram-support:sync` безопасно возвращает `disabled; synced=0`.
- [ ] После появления реальных credentials включить `TELEGRAM_SUPPORT_ENABLED=true`, задать `TELEGRAM_SUPPORT_API_ID/API_HASH/SESSION`, выполнить первый interactive `php artisan telegram-support:sync` для session login и проверить страницу `/admin/telegram-support-analytics`. Первый запуск 2026-06-30 дошёл до MadelineProto login QR; неинтерактивный Codex-run не может отсканировать QR/ввести телефон, поэтому завершился `PHONE_NUMBER_INVALID`. Следующий запуск должен делать пользователь в терминале: сканировать QR через Telegram Settings → Devices → Link Desktop Device или ввести телефон/код/2FA.
- [ ] На реальных данных донастроить `support_responder_mappings`: favicon/marker → `users.id`, чтобы human responder breakdown показывал имена команды, а не `unknown`.

### Продуктовое решение — ПРИНЯТО
- [x] **Прана в проде = ПОЛНАЯ ГЕЙМИФИКАЦИЯ** (решение 2026-06-26). Включаем earn+spend слой целиком (лидерборд, бейджи, streak, магазин праны, P2P, ранги) рядом со скидочным использованием. **Decay остаётся ВЫКЛ** (`PRANA_DECAY_ENABLED=false` по умолчанию) — не выбрано включать сгорание; флаг доступен без правки расписания. **NB деплой:** прогнать миграции праны; админ создаёт перки магазина (`PranaPerkResource`); проверить, что геймификация-партиалы видимы в prana-табе. Отдельного фиче-флага «скрыть геймификацию» не вводим — оба сценария уже сосуществуют (two-counter).

### Остаётся @DECIDE (не блокирует волну)
- [ ] **Webinar follow-ups** (#78 функционально закрыт): поллинг-альтернатива записям и более богатая посещаемость — делать только по запросу.
- [ ] Прочие продуктовые развилки появятся по ходу — фиксировать в `Uprava/GTD_NEXT_ACTIONS.md`.

## 🚧 Current Work-In-Progress (WIP)

- **Открыт [PR #211](https://github.com/gasyoun/Systema-Sanscriticum/pull/211)** (ветка `chore/roadmap-next-wave-and-user-casts`) — roadmap next-wave + фикс касты `User`/`Announcement` (задача A.1 закрыта). Полный прогон 643 passed, Pint clean. Ждёт ревью/мержа. Остальная волна (A.2+, B, C) — после.
- **NB деплой накопленный** (см. GTD-хаб `Uprava`): `php artisan migrate` (vk_auth_token, zoom-поля + `webinar_attendances`, прана-таблицы, referral_credit); Zoom — S2S OAuth app (`ZOOM_*`) + Event Subscription на `/api/webhooks/zoom` (`ZOOM_WEBHOOK_SECRET`, события `recording.completed` + `meeting.participant_joined/left`); редактор лекций v2 — Horizon-воркер на очереди `lectures` для настоящего async; `REFERRAL_CREDIT_AMOUNT` опц. (дефолт 500); прана-геймификация — включается публикацией партиалов + созданием перков (decay оставить ВЫКЛ).
- **Telegram support analytics live-ingestion slice добавлен локально**: MadelineProto зафиксирован в composer lock (requires PHP 8.2+), `sync()` переведён на per-peer incremental cursor в `telegram_support_accounts.sync_state`, добавлены operational logs, admin-editable Filament resources для `support_topic_rules` и `support_responder_mappings`, expanded tests. В дефолте `telegram-support:sync` безопасно возвращает `disabled` без DB.

## 🧠 Dev Notes & Hypotheses

- **ЛОКАЛЬНЫЕ ТЕСТЫ РАБОТАЮТ:** XAMPP php CLI с включёнными `intl/zip/gd` в `C:\xampp\php\php.ini`. Запуск: `/c/xampp/php/php.exe artisan test [--filter=...]` (в bash `php` не на PATH — звать полным путём или через PowerShell). `.env` из `.env.example` + `key:generate` (gitignored). `openssl_pkey_new` на Windows падает без openssl.cnf — тестовые RSA-ключи фиксируем константой, не генерим в рантайме.
- **ВАЖНО — фронт нужно собрать перед тестами:** layout'ы (`layouts/promo.blade.php`, `layouts/articles.blade.php`) используют `@vite`. Без `public/build/manifest.json` директива бросает исключение → **500** на homepage/лендингах. Это причина «фейлов» `ExampleTest`/`LandingListingVisibilityTest`/`CourseStreamsBlockTest` — НЕ seed и НЕ регрессия. Лечится один раз: `npm install && npm run build` (оба artifact'а gitignored). CI собирает ассеты сам.
- **Cast-ловушка `casts()` — УСТРАНЕНА** (ветка `chore/roadmap-next-wave-and-user-casts`): на **Laravel 10** касты задаются ТОЛЬКО свойством `protected $casts` — метод `protected function casts(): array` появился в Laravel 11 и здесь молча игнорируется (`HasAttributes::getCasts` читает только свойство). `User` и `Announcement` были на методе → касты не работали. Переведены на свойство. **Урок на будущее: в этом проекте всегда `protected $casts = [...]`, не метод.** Ручные `Carbon::parse()`/`(int)` в коде оставлены — они идемпотентны и безвредны поверх активных кастов.
- **«Оплачено» = `['paid','success']`**, не только `'paid'`. Канон `Payment::PAID_STATUSES`/`scopePaid()`. Все инлайн-литералы сведены к канону (#108/#172/#179) — при новом коде использовать `scopePaid()`/`PAID_STATUSES`, не литералы.
- **CodeQL не умеет PHP.** `Analyze (php)` падал на каждом PR. Исправлено в #107 (php→только javascript). **Урок:** grep по локальным рабочим деревьям ≠ origin/<default> (клоны устаревают / трекают не ту ветку — нужен `git pull`).
- **Telegram support analytics validation (2026-06-29):** `C:\xampp\php\php.exe artisan test --filter=TelegramSupportAnalyticsTest` → 4 passed / 38 assertions after incremental cursor tests; `--filter=CuratorBadgeTest` → 2 passed; `--filter=CabinetWebChatTest` → 4 passed; `--filter=CuratorWebhookTest` → 7 passed; `php artisan telegram-support:sync` → `disabled; synced=0`; MadelineProto class autoloads. Full `artisan migrate --force` now reaches DB but blocks on old unrelated `dictionary_words` FK; Telegram support migrations applied via `--path`. Local DB has 8 initial `support_topic_rules`.
- **MadelineProto 8.7 constructor nuance (2026-06-30):** `danog\MadelineProto\API::__construct()` requires `?SettingsAbstract`; old array settings cause TypeError before login. `TelegramSupportSyncService` now builds `danog\MadelineProto\Settings` and sets app info through `getAppInfo()->setApiId()/setApiHash()`.
- **Telegram support session path nuance (2026-06-30):** first live sync also requires the parent session directory to exist. `TelegramSupportSyncService` now ensures `dirname(base_path(config('services.telegram_support.session')))` before constructing MadelineProto.
- **Telegram support live-login status (2026-06-30):** after the fixes, `C:\xampp\php\php.exe artisan telegram-support:sync` connects to Telegram DC 2, creates `storage/app/telegram-support/session.madeline`, and renders the QR login prompt. Codex noninteractive stdin caused `PHONE_NUMBER_INVALID`; this should be completed manually from a real terminal with phone/QR.
- **Telegram AUTH_RESTART handling (2026-06-30):** first manual phone login can return `AUTH_RESTART` after saving session/login-token updates. `TelegramSupportSyncService` now retries MadelineProto fetch once on `AUTH_RESTART`; covered by `TelegramSupportAnalyticsTest` (5 passed / 42 assertions).
- **Telegram flood mitigation (2026-06-30):** first successful login imported against an account with hundreds of dialogs and hit Telegram `Flood, waiting ...` on `messages.getDialogs/getHistory`. Added `TELEGRAM_SUPPORT_DIALOG_LIMIT` (default 20; local `.env` set to 10) so each scheduled run processes a bounded number of dialogs; `TelegramSupportAnalyticsTest` now 6 passed / 45 assertions.
- **Telegram first real import (2026-06-30):** noisy MadelineProto runtime logs moved to `storage/logs/madelineproto.log`; import no longer assumes `users.telegram_id` exists in partially migrated local DB. Real `telegram-support:sync` completed: `ok; synced=84`; local DB now has 84 `telegram_support_messages`, 9 chats, 31 daily conversations.
- **Telegram contact/user linking (2026-06-30):** local `users.telegram_id` migration applied via targeted path; sync keeps schema guard as degraded mode but logs when auto-link is unavailable. Added admin Filament resource for manual Telegram support contact → user links; contact links propagate to chat links and sync never overwrites existing manual `linked_user_id`. Validation: `TelegramSupportAnalyticsTest` 10 passed / 57 assertions; chat regressions still green.
- **Telegram contact auto-link automation (2026-06-30):** added `SupportContactUserAutoLinker` to avoid manual linking for large cohorts. Sync now enriches contacts from Madeline `history.users`, backfills existing contact profiles via `getInfo`, then auto-links by exact `users.telegram_id`, unique Telegram username in `users.note` (`Telegram: @...`), or unique full-name match. Manual links still win. `telegram-support:sync` reports `auto_linked=N`. Local `2026_03_29...` migration hardened for partial schemas and recorded successfully; validation: Telegram analytics 14 passed / 72 assertions + chat regressions green.

## ✅ Completed — доставлено (сжатая сводка; детали в git / прежних коммитах роадмапа)

**Денежно-/доступно-критичное ядро:** канон `PAID_STATUSES`/`scopePaid()` консолидирован (#104/#108/#172/#179); фикс латентного бага доступа (success-платёж не открывал урок); покрытие Tochka-вебхука, лояльности, сборки цены чекаута; throttle `/login` + защита `vk_id`; VK-IDOR → `vk_auth_token` (#173); рефералка → **денежный кредит** `referral_credit` (PR #201).

**Прана (геймификация, earn+spend полностью):** two-counter (balance + lifetime, ранги) · лидерборд (#202) · бейджи (#204) · streak-награды · магазин праны (perks + redemptions, Filament CRUD) · P2P-перевод + decay-команда (по умолчанию выкл, в расписании #180). **Решение: ПОЛНАЯ ГЕЙМИФИКАЦИЯ в прод (decay выкл).**

**EduTech P0/P1/P2/P3:** кураторская очередь ДЗ (#159) + карточка прогресса (#160); онбординг-чеклист (#161) + «следующий урок» в карточках курсов (#177); статусы ДЗ + пуш (#162); сигналы застрявших + виджет; сегментная рассылка; веб-чат поддержки в кабинете; аналитика преподавателей; mobile API (Sanctum `/api/v1`); соц-авторизация (scaffold + VK/Yandex community-драйверы #208).

**Редактор лекций v2 (A/B/C, #184/#186/#189):** async-пайплайн (очередь `lectures`, `meta.processing`, поллинг-виджет); структурное редактирование (move/delete_block, split/merge_para, add-block #210); advisory-лок + откат к бэкапу; кросс-репо template-фикс рендера абзацев без таймкода (#209). **Остаток — задача B выше.**

**Вебинары Zoom (#78, все 3 фазы, #194/#195/#197/#198):** Фаза 1 автосоздание встречи (S2S OAuth, `ZoomService`); Фаза 2 автоимпорт записей (`recording.completed` вебхук); Фаза 3 посещаемость (`participant_joined/left` → `webinar_attendances`); вшито в `TeacherAnalytics` (воронка посещаемости).

**Инфра/CI:** CodeQL php→javascript (#107); Dependabot #84–#87; README обогащён + `docs/onboarding-student.md`; CodeQL-свип сиблингов (RuWritingStyles #14, csl-websanlexicon #62).

**Telegram support analytics MVP/live slice (локально, 2026-06-29):** добавлен Laravel-native слой для саппорт-аккаунта Telegram без замены Helpdesk: новые таблицы/модели, normalized ingestion, daily conversation aggregation (`chat_id + date`), first-time contacts, unanswered, human/AI reply counts, keyword topics + uncategorized fallback, `dashboard.live` packet, Filament page `/admin/telegram-support-analytics`, cron-safe `telegram-support:sync`, MadelineProto dependency lock, incremental per-peer cursor, admin CRUD для topic rules/responder mappings.
