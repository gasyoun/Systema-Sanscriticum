# Project Objective: Laravel-платформа онлайн-обучения (кабинет, магазин курсов, лендинги, редактор лекций, админка) — поддержка и развитие; денежно-/доступно-критичное ядро под особой защитой.

## ➡️ Next Steps (Queue)

- [ ] **Security PR (3 проверенных находки).** Высокий приоритет, подтверждены adversarial-проверкой 2026-06-13:
  - [ ] `routes/api.php:27,29` — у `POST /api/telegram/webhook` и `POST /api/vk-webhook` НЕТ проверки подписи (в отличие от 4 magnet-вебхуков и Точки). Добавить fail-closed middleware по образцу `VerifyTelegramMagnetWebhook` (header `X-Telegram-Bot-Api-Secret-Token`) и `VerifyVkMagnetCallback` (secret в body). **Осторожно:** VK-запрос `type==='confirmation'` идёт без секрета — его надо whitelist'ить, иначе сломается серверное подтверждение VK. Нужны новые ключи `services.telegram.bot_webhook_secret` и `services.vk.callback_secret` → **сначала задать секреты в проде/.env, иначе fail-closed положит живой вебхук.**
  - [ ] `VkBotController.php:49-57` — account-takeover IDOR: `User::find($ref)` берёт `$ref` (= угадываемый user id) из неаутентифицированного тела и делает `update(['vk_id'=>...])`. Чинить как Telegram: одноразовый неугадываемый токен + запрет перезаписи уже установленного `vk_id`.
  - [ ] `routes/web.php:65` — у основного `POST /login` нет throttle, хотя у всех соседних credential-роутов есть. Добавить `->middleware('throttle:5,1')` (или per-credential RateLimiter как в Breeze).
- [ ] **Тесты денежного ядра (CI-verifiable).** Tochka-webhook (подпись+идемпотентность; нужен мелкий рефактор — читать RSA-ключ из конфига вместо хардкода `WebhookController.php:20`), тиры лояльности, сборка цены в чекауте.
- [ ] **Защитить ветки/CI:** `Analyze (php)` (CodeQL) падает на КАЖДОМ Dependabot-PR за ~15 c из-за ограниченного Dependabot-токена («Secret source: Dependabot») — не связано с бампами. Починить (gating только на push/PR от членов, не на Dependabot) или убрать из required.
- [ ] Дождаться авто-rebase Dependabot #86 (axios) — конфликтовал по `package-lock.json` после мержа #85 (vite).

## 🚧 Current Work-In-Progress (WIP)

- [ ] PR #104 `fix/paid-success-access-gate` — открыт, ждём зелёного CI (PHP 8.1/8.2 на SQLite). Локально `vendor/` неполон (нет composer) → верификация только через Actions.
- [ ] PR (docs) `docs/roadmap-reconcile` — этот README + .ai_state.

## 🧠 Dev Notes & Hypotheses

- **Локальная среда (2026-06-13):** PHP есть только через XAMPP (`C:\xampp\php\php.exe`, 8.2), но `vendor/composer/ClassLoader.php` отсутствует и composer не на PATH → `php artisan test` локально не запускается. Верификация PHP-изменений = CI. Для локальных прогонов нужен `composer install`.
- **«Оплачено» = `['paid','success']`**, не только `'paid'`. Это размазано по ~12 местам; PR #104 ввёл канон `Payment::PAID_STATUSES` + `scopePaid()`. Осталось перевести на него локальные дубль-константы (`PaymentObserver:16`, `StudentDebtsService:17`, `DebtorsReport:19`) и инлайн-литералы (`Tariff:91/224/327`, виджеты и т.д.) — низкий риск, есть тесты.
- **Прана раздвоена** (см. README-заметку): прод = скидочная валюта (`config/prana.php`); `PRANA_ROADMAP.md` = несовместимая геймификация, не реализована. **Нужно продуктовое решение** (retire / надстроить ранги / две валюты).
- **Webhook-подпись + тесты:** у Tochka-вебхука RSA-ключ захардкожен (`WebhookController.php:20`, не инжектится) — это мешает написать success-path тест; перед тестом сделать ключ конфигурируемым.

## ✅ Completed (Recent only)

- 2026-06-13: **PR #104** — фикс латентного бага доступа (`success`-платёж не открывал урок, хотя группа выдана): канон `Payment::PAID_STATUSES`/`scopePaid()`, 3 гейта на `->paid()`, тесты `LessonAccessTest`/`PromoCodeTest`/`GrantAccessTest`.
- 2026-06-13: **Dependabot** — смержены #87 (guzzle), #84 (autoprefixer), #85 (vite) squash+delete-branch; #86 (axios) на авто-rebase.
- 2026-06-13: **docs/roadmap-reconcile** — README-роадмап приведён к факту (чат готов через мессенджеры, ~50 тестов, прана-предупреждение, social-auth = greenfield); заполнен этот `.ai_state.md`.
