# Project Objective: Laravel-платформа онлайн-обучения (кабинет, магазин курсов, лендинги, редактор лекций, админка) — поддержка и развитие; денежно-/доступно-критичное ядро под особой защитой.

## ➡️ Next Steps (Queue)

- [ ] **Security: проверка подписи бот-вебхуков** (gated на секреты в проде):
  - [ ] `routes/api.php:27,29` — `POST /api/telegram/webhook` и `POST /api/vk-webhook` БЕЗ проверки подписи. Fail-closed middleware по образцу `VerifyTelegramMagnetWebhook` / `VerifyVkMagnetCallback`. **Сначала задать `services.telegram.bot_webhook_secret` и `services.vk.callback_secret` в проде**, whitelist VK `type==='confirmation'`, иначе fail-closed положит живой вебхук.
  - [ ] Полный фикс VK-IDOR: одноразовый неугадываемый токен вместо сырого `$ref` (как `telegram_auth_token`). Частичный guard (не перезаписывать установленный `vk_id`) уже в #106.
- [ ] **Доконсолидировать `PAID_STATUSES`**: инлайн-литералы `whereIn('status', ['paid','success'])` в `Tariff:91/224/327`, `ShopController`, Filament-виджетах и т.д. → `->paid()` (проверив, что запрос идёт по `Payment`). #108 закрыл константы + внутренние литералы `Payment`.
- [ ] **Продуктовые решения** (не автономно): судьба геймификации праны (`PRANA_ROADMAP.md` vs скидочная прана в проде); модель вознаграждения рефералки; видеоплатформа для вебинаров (issue #78).

## 🚧 Current Work-In-Progress (WIP)

- [ ] 3 PR ждут мержа: #108 (консолидация `PAID_STATUSES`), #109 (Tochka-вебхук тест), #110 (лояльность + чекаут + фикс SQLite-миграции). Все зелёные локально/CI.

## 🧠 Dev Notes & Hypotheses

- **ЛОКАЛЬНЫЕ ТЕСТЫ ТЕПЕРЬ РАБОТАЮТ (2026-06-13):** восстановил `vendor/` через `composer install` (скачал composer.phar — он удалён, не коммитим). XAMPP php CLI был без `intl/zip/gd` — **включил их в `C:\xampp\php\php.ini`** (раскомментировал `extension=gd/intl/zip`). Запуск: `/c/xampp/php/php.exe artisan test [--filter=...]`. `.env` создан из `.env.example` + `key:generate` (gitignored). openssl в CLI есть; `openssl_pkey_new` на Windows падает без openssl.cnf (`C:\xampp\apache\conf\openssl.cnf`) — поэтому тестовые RSA-ключи фиксируем константой, а не генерим в рантайме.
- **`ExampleTest` (`GET /` → 500)** падает ЛОКАЛЬНО (домашняя страница хочет seed-данные, которых нет в in-memory SQLite) — НЕ регрессия, зелёный в CI. Единственный фейл из 285.
- **«Оплачено» = `['paid','success']`**, не только `'paid'`. Канон `Payment::PAID_STATUSES`/`scopePaid()` (#104). #108 свёл константы; остались инлайн-литералы (см. очередь).
- **CodeQL не умеет PHP.** `Analyze (php)` падал на КАЖДОМ PR («Did not recognize languages: php»). Исправлено в #107 (php→только javascript). Свип сиблингов: владелец уже починил большинство upstream 2026-06-02; реально остались RuWritingStyles (PR #14) + csl-websanlexicon (PR #62). **Урок:** мой grep по локальным рабочим деревьям ≠ origin/<default> (многие клоны устарели / трекают не ту ветку — нужен `git pull`).

## ✅ Completed (Recent only)

- 2026-06-13: **#104 merged** — фикс латентного бага доступа (success-платёж не открывал урок): канон `PAID_STATUSES`/`scopePaid`, 3 гейта, тесты `LessonAccessTest`/`PromoCodeTest`/`GrantAccessTest` (все зелёные локально).
- 2026-06-13: **#105 merged** — README приведён к факту + заполнен `.ai_state.md`.
- 2026-06-13: **#106 merged** — security: throttle `/login` + запрет перезаписи чужого `vk_id`.
- 2026-06-13: **#107 merged** — CI: убран неподдерживаемый `php` из матрицы CodeQL.
- 2026-06-13: **Dependabot** — #84/#85/#86/#87 все смержены.
- 2026-06-13: **#108 open** — консолидация `PAID_STATUSES` (3 константы + внутренние литералы `Payment`).
- 2026-06-13: **#109 open** — тест вебхука Точки (подпись + идемпотентность) + config-ключ.
- 2026-06-13: **#110 open** — тесты лояльности + сборки цены чекаута + фикс SQLite-миграции `2026_03_27_182336` (drop+add разнесены). Закрыты все 3 непокрытых денежных пути.
- 2026-06-13: **CodeQL-свип сиблингов** — RuWritingStyles #14, csl-websanlexicon #62 (остальные уже починены upstream).
