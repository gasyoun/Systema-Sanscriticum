# Project Objective: Laravel-платформа онлайн-обучения (кабинет, магазин курсов, лендинги, редактор лекций, админка) — поддержка и развитие; денежно-/доступно-критичное ядро под особой защитой.

## ➡️ Next Steps (Queue)

- [x] **Полный фикс VK-IDOR** (PR #173, ветка `fix/vk-idor-auth-token`): `users.vk_auth_token` + `VkController::connect` (Str::random(32) → редирект на vk.me с токеном) вместо сырого `?ref={auth()->id()}`; `VkBotController::handle` ищет по токену и гасит его после привязки (`ref` только как string, guard `!vk_id` сохранён для гонок); кнопка в `dvaram` → `route('vk.connect')`; `disconnectMessenger(vk)` чистит токен. `VkAuthTokenLinkingTest` 5/5, регрессий нет (BotWebhookSignature/VkMagnetCallback/VkLandingForward 19/19). Требует `php artisan migrate`. Частичный guard (#106) и подпись вебхуков (#158) — были раньше.
- [x] **Доконсолидировать `PAID_STATUSES`** (ветка `refactor/paid-statuses-consolidation`): все инлайн-литералы `whereIn('status', ['paid','success'])`/`in_array(..., ['paid','success'])` сведены к `Payment::scopePaid()` (Eloquent: Tariff×3, PromoCode, ShopController×2, CourseCatalog, Payment×2, Debtors×2, TeacherSalaryService×2, виджеты×3 + 2 Filament-фильтра; где был `is_conditional=false` — добавлен `->real()`) и `Payment::PAID_STATUSES` (DB::table в Backfill; `in_array` в SendPaymentToSheetJob/PromiseFulfillment). Удалён дублирующий `private const PAID_STATUSES` в TeacherSalaryService. Display-арм `match()` (status→цвет/лейбл) оставлены намеренно — не paid-фильтр. 78 тестов зелёные, Pint clean. #108 ранее закрыл константы + литералы внутри `Payment`.
- [x] **Реферальная награда → денежный кредит — СМЕРЖЕНО (PR #201, 2026-06-26)** (ветка `feat/referral-cash-credit`): заменил прана-награду рефереру на реальный денежный кредит. `users.referral_credit` (кошелёк, decimal) + `referral_rewards` (аудит, unique referred_id → идемпотентность) + `payments.referral_credit_applied`. `ReferralService::rewardForPayment` начисляет `config('referral.credit_amount')` (дефолт 500₽) на кошелёк реферера при ПЕРВОЙ оплате приглашённого (гейт учитывает и старые прана-награды — без двойного начисления при миграции). Чекаут (`PaymentController`) авто-зачитывает кредит в счёт цены (как прана: списание в той же транзакции, `referral_credit_applied` на платеже); срыв оплаты → `Payment::refundReferralCreditIfApplied()` возвращает в кошелёк (зеркалит `refundPranaIfSpent`). UI: карточка-приглашение в prana-табе показывает баланс кредита + новый текст. `ReferralProgramTest` 7/7 (обновлены на кредит) + `ReferralCreditCheckoutTest` 3/3 (частичный зачёт+списание, полный зачёт→free-access без Точки+остаток, refund при fail). Денежный сьют 75/75 без регрессий. **NB деплой:** `php artisan migrate`; `REFERRAL_CREDIT_AMOUNT` в `.env` (опц., дефолт 500). Прана-награда за рефералов больше не начисляется (config `prana.rewards.referral` осталась, но не используется).
- [~] **Прана — углубление геймификации** (ветка `feat/prana-leaderboard`, P3-приоритет): **таблица лидеров** — `App\Support\PranaLeaderboard` (топ по `lifetime_prana`, только студенты `is_admin=false` + lifetime>0; имена замаскированы «Фамилия И.» без города; позиция 1-based; текущий юзер подсвечен и добавлен отдельной строкой, если вне топ-10). Партиал `prana-leaderboard.blade.php` (медали 🥇🥈🥉, ранг под именем, «вы») в prana-табе после ранга. `PranaLeaderboardTest` 4/4 (порядок+маска, исключение админов/нулей, вне-топа+позиция, в-топе без дубля) — **смержено #202**. [x] **бейджи** (та же ветка-серия `feat/prana-badges`): `App\Support\Badges` — каталог `config('badges.catalog')` (метрика+порог), вычисляется из сигналов (`lessons_completed`/`courses_completed` по prana `course_complete`/`referrals` по `referral_rewards`/`lifetime_prana`/`gifts_given` по prana `p2p_sent`), без отдельного хранилища. Партиал `prana-badges` (грид, полученные цветные, остальные приглушены с прогрессом). `BadgesTest` 4/4. Дальше: новые сферы заработка/трат праны.
- [ ] **EduTech-роадмап (см. README, приоритеты P0/P1/P2):**
  - [x] **P0 кураторы — закрыт** (#159 + #160): [x] очередь проверки ДЗ (#159 — фильтр по уроку, сортировка «дольше всех ждёт», bulk «Принять»/«Вернуть» через `HomeworkService::recordReview`, шаблоны комментариев) + [x] карточка прогресса студента (#160 — секция «Прогресс обучения» в `UserResource::infolist`: % пройденных уроков по курсам + сводка ДЗ; прана/визит/время уже были). Тесты `HomeworkReviewQueueTest` + `StudentProgressCardTest`.
  - [x] **P0 студенты — закрыт** — [x] онбординг-чеклист (#161 — карточка «С чего начать» на `/dvaram`, 4 шага по существующим сигналам, шаг ДЗ авто-выполнен без ДЗ, прячется при 100%; `OnboardingChecklist::for()`, `OnboardingChecklistTest` 4/4). [x] «следующий урок» в карточках курсов (ветка `feat/course-card-next-lesson`): `StudentController::dashboard` считает `$nextLessonByCourseId` (первый по `sort_order→created_at` доступный по группе и непройденный урок, без N+1 — `isVisibleToGroupsOf` по уже загруженным `$user->groups`); карточка показывает «Первый/Следующий урок: <title>» либо «Все уроки пройдены», кнопка ведёт прямо в урок (`student.lesson`), а при 100% — на курс. `CourseCardNextLessonTest` 4/4 (fresh→урок 1, частично→следующий, всё пройдено→done+курс, пропуск чужой группы). Прогресс-бар/денежные семантики не трогал.
  - [~] **P1** — [x] понятные статусы ДЗ + пуш (#162): явное «ДЗ нет»/«скоро»; пуш в TG/VK о вердикте после коммита; баннер «вернул на доработку» на дашборде. [x] сигналы «застрявших» студентов (ветка `feat/stuck-students-signals`): `StuckStudentsReport` (неактивен 14+ дн / ДЗ на доработке 7+ дн без пересдачи) + дашборд-виджет `StuckStudentsWidget` (admin-only), `StuckStudentsTest` 5/5. [x] сегментная рассылка (ветка `feat/segmented-bulk-messaging`): bulk-действие «Написать в TG/VK» в `UserResource` (сегмент = выбранные через фильтры, включая новый фильтр «Застрявшие»), превью охвата (`messengerReach()` + blade) и отправка через `SendMessengerAlerts`. `SegmentedBulkMessagingTest` 4/4. Курсовой/групповой broadcast уже был в `AnnouncementResource` — не дублировал. [x] веб-чат поддержки в кабинете (ветка `feat/cabinet-web-chat`): вкладка «Поддержка» на `/dvaram` (Livewire `StudentChat`, опрос 10с), `StudentChatService` переиспользует тот же «мозг» (`CuratorAi`/`StudentSelfService`) и роли `user`/`bot`/`curator`; ответ ИИ — в очереди (`ProcessStudentChatReply`); «позови куратора» → режим человека, куратор отвечает из Helpdesk/Dialogs, реплики видны в кабинете. TG-вебхук не трогал (прод). `CabinetWebChatTest` 4/4. **→ P1 закрыт.**
  - [~] **P2** — [x] аналитика преподавателей (ветка `feat/teacher-analytics`): Filament-страница «Аналитика студентов» (teacher + admin), `TeacherAnalytics`-сервис из `lesson_views` — посуточная воронка открытий (`first_opened_at`), funnel (начали/прошли), таблица прогресса студентов (меньше всего прошедшие сверху); скоуп по `Course.teacher_id` (admin = все). `TeacherAnalyticsTest` 4/4. [x] mobile API (ветка `feat/mobile-api`): Sanctum-токены — **`HasApiTokens` на `User` НЕ был подключён** (роадмап «Sanctum настроен» был полу-правдой: пакет+миграция+`auth:sanctum` на `/user` были, но токены не выдавались), добавил. Эндпоинты `/api/v1`: `auth/login` (throttle 10/1) · `auth/me` · `auth/logout` · `courses` (с прогрессом) · `courses/{slug}/lessons` (флаги `locked`/`is_completed`, та же логика доступа: группы+оплаченные тарифы+grant). `MobileApiTest` 5/5. [x] рефералка → прана (ветка `feat/referral-program`, решение пользователя): `users.referral_code` (ленивый) + `referred_by`; `ReferralService` (attachReferrer по коду + idempotent reward); награда 100 праны рефереру при ПЕРВОЙ оплате приглашённого через `PaymentObserver` (created-as-paid и pending→paid); `CaptureReferral` middleware ловит `?ref` в сессию; захват кода при создании юзера в `PaymentController`; карточка-приглашение в кабинете (prana-таб). `ReferralProgramTest` 7/7. [x] соц-авторизация — scaffold (ветка `feat/social-auth-scaffold`): `social_accounts` + `SocialAccount`, `User::socialAccounts()`, `SocialAuthService` (find-or-create, decoupled от Socialite → тестируемо без сети/секретов), `SocialAuthController` (whitelist-guard → 404), маршруты `/auth/{provider}/redirect|callback`, провайдер-блоки в `config/services` (google/vkontakte/yandex), кнопки на `/login` (видны только при заданном client_id). `laravel/socialite` добавлен в composer.json. `SocialAuthScaffoldTest` 6/6. **NB:** нужен `composer install` (socialite не в vendor локально); Google работает из коробки, **VK/Yandex требуют community-драйверов** `socialiteproviders/*` + listener — оставлено как follow-up; секреты — в .env. [x] прана two-counter (ветка `feat/prana-two-counter`): добавлен `users.lifetime_prana` (накопительный, тратами не падает) рядом с `prana_balance` (скидка как была); бэкфилл из истории `prana_transactions`. `award` и `adminAdjust(+)` растят оба счётчика; `spend`/`refund`/`adminAdjust(-)` — только balance. Ранги `config('prana.ranks')` (Śiṣya→Paṇḍita) + `PranaSettings::rankFor()` + `User::pranaRank()`; карточка ранга в prana-табе. Скидка и геймификация сосуществуют (противоречие было только при одном счётчике). `PranaTwoCounterTest` 5/5. **Распад и P2P-перевод — НЕ делал** (follow-up по PRANA_ROADMAP). [x] прана распад + P2P (ветка `feat/prana-decay-p2p`): `PranaService::transfer()` (P2P-подарок, balance-only, lifetime/ранг не растёт ни у кого; дневные лимиты `daily_p2p_limit`/`daily_p2p_per_user_limit`); `decayInactive()` + команда `prana:decay` (сжигает % balance у неактивных N+ дней, lifetime не трогает, **по умолчанию ВЫКЛ** `prana.decay.enabled=false`); форма «подарить прану» в prana-табе. Reasons `p2p_sent/p2p_received/decay`. `PranaDecayP2PTest` 8/8. [x] `prana:decay` повешен в расписание (ветка `feat/schedule-prana-decay`): `Kernel::schedule` — `weeklyOn(1,'04:00')` + `withoutOverlapping/onOneServer`, команда сама no-op при `prana.decay.enabled=false` (дефолт), включение — флагом `PRANA_DECAY_ENABLED` без правки расписания. `PranaDecayScheduleTest` 2/2 (зарегистрирована + cron `0 4 * * 1`). [x] **редактор лекций v2 — Phase A (async pipeline, PR #184 merged)** (ветка `feat/lecture-editor-v2-async`, решение пользователя: «Phase A first, advisory lock»): длинные шаги (препроцесс/сборка/4×ИИ) больше НЕ блокируют HTTP-запрос админки — вынесены в очередь `lectures` (отдельный Horizon-супервизор, timeout 960). Новый `RunLectureAiJob` (был синхронный вызов клиента + dispatchSync прямо в экшене). Признак «идёт обработка» — `meta.processing` на `LectureDraft` (`markProcessing/clearProcessing/isProcessing/processingLabel`, снимается джобой в finally; `BuildLectureHtmlJob` получил флаг `ownsProcessing`, чтобы вложенная сборка не снимала флаг владельца). Кнопки экшенов заблокированы пока `isProcessing()`. Виджет-баннер `LectureProcessingWidget` (poll 4с) показывает «⏳ выполняется…» и перезагружает страницу по завершении; итог ИИ — в `meta.last_ai`. `LectureAsyncPipelineTest` 7/7 + `LectureProcessingWidgetTest` 2/2. **NB деплой:** воркер на очереди `lectures` (Horizon-конфиг добавлен в prod/local); реальная асинхронность — только при `QUEUE_CONNECTION=redis` (дефолт `sync` → инлайн, тоже корректно). Полный scope v2 (фазы A/B/C) — см. чат/обсуждение. [x] **Phase B (структурное редактирование, PR #186 merged)** (ветка `feat/lecture-editor-v2-structural`): `LecturePatcher` расширен структурными операциями по собранной форме (speech/figure, абзацы-объекты `{text,t?,dg_ref?}`): `move_block`/`delete_block` (по `section.content`, переиндексация), `split_para` (по символьному offset; таймкод/dg_ref остаются у первой половины, вторая — чистый абзац), `merge_para` (в предыдущий, метаданные предыдущего сохраняются). Контракт `LectureDraftController::patch` принимает `op` (whitelist) — смысловые проверки в патчере (422). JS `lecture-editor.js`: тулбары на блоках (↑↓🗑) и абзацах (⤴ слить / ✂ разбить по курсору), структурная операция шлётся по одной с ребилдом+reload (при незасохранённых правках — просит сперва сохранить). `LecturePatcherStructuralTest` 9/9 + `LectureStructuralPatchTest` 4/4. **NB:** работает по тому, что уже размечено в шаблоне (`template.html.j2`: speech-блоки + figure имеют `data-block-index`); добавление новых блоков «с нуля» и редактирование dialog-реплик требуют правок Jinja-шаблона в lecture-ui (Python) — отдельный кросс-репо шаг. [x] **Phase C (совместное редактирование + откат, PR #189 merged)** (ветка `feat/lecture-editor-v2-collab`): **advisory-лок** — `LectureDraft` meta-лок с TTL `LOCK_TTL_SECONDS=120` (`activeLock`/`isLockedByOther`/`heartbeatLock`/`releaseLock`); хартбит шлёт поллинг-виджет (4с) + `mount()`, баннер «Сейчас редактирует X» (rose) у других; закрытая вкладка освобождает лок по TTL; мягкая (не жёсткий запрет). **Откат к бэкапу** — `LectureStorage::listBackups()` (новые сверху, человекочитаемые лейблы) + `restoreBackup()` (guard от path-traversal, бэкап текущего перед откатом → обратимо); экшен «Откатить к бэкапу» в `EditLectureDraft` (Select версий → restore → async-пересборка). Откат бэкапа = и механизм «reject» правок ИИ (вернуть pre-AI бэкап). `LectureCollabTest` 7/7. **Отложено:** полноценный pre-apply визуальный diff ИИ (apply=false) — нужен diff-payload от Python-сервиса (кросс-репо); «reject» уже покрыт откатом. **→ Редактор лекций v2 закрыт (A/B/C смержены).** [ ] осталось из P2: вебинары (Zoom — **scope ниже**, blocked на OAuth-кредах); кросс-репо разметка доп. блоков в `template.html.j2` (lecture-ui, Python) для Phase B add-block/dialog + опц. визуальный AI-diff.

### 🧩 Scope: вебинары на Zoom (issue #78)

**Уже есть (Фаза 0 — не переделывать):** `Schedule` (title/description/**link**/start/end/group/course) с ручной Zoom-ссылкой; календарь кабинета; напоминания (`RemindUpcomingClasses` + `Schedule.reminded_at`); импорт записей как уроков — push-эндпоинт `POST /api/lessons/from-zoom` (`LessonController::storeFromZoom`); `TrialZoomLinkMail`/`TrialController` (zoom-ссылки на пробное).

**Решено:** движок — **Zoom**.

**[x] Фаза 1 (MVP) — СМЕРЖЕНА (PR #194, 2026-06-26)** (ветка `feat/zoom-webinars-phase1`): миграция `schedules` (+`zoom_meeting_id`/`zoom_join_url`/`zoom_start_url`); `ZoomService` (Server-to-Server OAuth: `account_credentials` grant, токен кэшируется 55 мин; `createMeeting` → POST `/v2/users/me/meetings`; `isConfigured()`); config `services.zoom` (`ZOOM_ACCOUNT_ID/CLIENT_ID/CLIENT_SECRET`); биндинг в `AppServiceProvider` (bind, не singleton — креды из config на момент резолва). Экшен «Создать Zoom-встречу» в `ScheduleResource` (виден только при заданных кредах и без встречи) → сохраняет zoom_* + **дублирует `join_url` в `link`** → кабинетная кнопка «Подключиться» работает без правок вьюх (переиспользует существующий рендер `calendar.blade`). Хост-ссылка `start_url` — readonly-поле в форме (только препод). `ZoomServiceTest` 5/5 (Http::fake: payload/тип 2/duration/Bearer, кэш токена, not-configured/API-error → throw) + `ZoomScheduleActionTest` 2/2 (экшен сохраняет+зеркалит link; скрыт при наличии встречи). Pint clean. **NB деплой:** создать S2S OAuth app в Zoom → три значения в `.env` + `php artisan migrate`. **Не делал (Фаза 2/3):** автоимпорт записей (вебхук `recording.completed`) и посещаемость — отдельные ветки.

**Предлагаемые фазы:**
- **Фаза 1 (MVP) — автосоздание встречи:** из `ScheduleResource` создавать Zoom-встречу через Zoom API (**Server-to-Server OAuth**) → хранить `zoom_meeting_id`/`join_url`/`start_url` (новые колонки в `schedules`). Кнопка «Подключиться» в кабинете, когда вебинар идёт/скоро (Schedule уже на календаре).
- **[x] Фаза 2 — автоимпорт записей — СМЕРЖЕНА (PR #195, 2026-06-26)** (ветка `feat/zoom-webinars-phase2`): `ZoomWebhookController` на `POST /api/webhooks/zoom` — проверка подписи Zoom (`x-zm-signature` = `v0=hmac_sha256("v0:{ts}:{body}", ZOOM_WEBHOOK_SECRET)`, enforce-if-configured, fail-open при пустом секрете); челлендж `endpoint.url_validation` (plainToken + encryptedToken); событие `recording.completed` → находит `Schedule` по `zoom_meeting_id`, пишет `zoom_recording_url` (+`received_at`) и, если занятие привязано к курсу, идемпотентно заводит/обновляет урок (по `course_id`/`group_id`/дата старта; `video_url`=запись — зеркалит `LessonController::storeFromZoom`). Миграция `schedules` (+`zoom_recording_url`/`zoom_recording_received_at`); config `services.zoom.webhook_secret` (`ZOOM_WEBHOOK_SECRET`); экшен «Открыть запись» в `ScheduleResource`. `ZoomWebhookTest` 8/8 (url-validation, recording→урок, идемпотентность, fallback на MP4 play_url, неизвестная встреча→200, плохая/отсутствующая подпись→403, пустой секрет→пропуск). **NB деплой:** настроить Event Subscription в Zoom-приложении на `/api/webhooks/zoom` + `ZOOM_WEBHOOK_SECRET` в `.env` + `php artisan migrate`. Альтернатива поллингом — не делал (вебхук выбран).
- **[x] Фаза 3 — посещаемость — СМЕРЖЕНА (PR #197, 2026-06-26)** (ветка `feat/zoom-webinars-phase3`): таблица `webinar_attendances` (schedule_id, user_id nullable, zoom_participant_uuid, name/email, joined_at/left_at, duration_seconds; unique по `schedule_id`+`uuid`) + модель `WebinarAttendance` + `Schedule::attendances()`. `ZoomWebhookController` обрабатывает `meeting.participant_joined`/`participant_left` → upsert по сессии участия (`participant_uuid`, фолбэк `user_id|join_time`); студент сопоставляется по email (иначе гость, user_id NULL); `left` дополняет ту же строку (left_at + duration), null-значения не затирают записанное. Колонка «Участников» (count) в `ScheduleResource`. `ZoomAttendanceTest` 5/5 (match по email, гость, joined→left одна строка с duration, разные участники = разные строки, неизвестная встреча→ignored). Suite Zoom|Schedule|Webhook 90/90. **NB:** подписаться на события participant_joined/left в том же Event Subscription; миграция включена. **[x] Вшито в TeacherAnalytics — СМЕРЖЕНО (PR #198, 2026-06-26)** (ветка `feat/webinar-attendance-analytics`): `TeacherAnalytics::webinarAttendance()` агрегирует `webinar_attendances`⋈`schedules` по курсам преподавателя (скоуп `Course.teacher_id`), на вебинар: `attendees` (COUNT DISTINCT COALESCE(user_id,uuid) — студент дедуп по user_id, гость по сессии), `known` (сопоставленные студенты), `avg_minutes`; секция-таблица «Посещаемость вебинаров» на странице аналитики (показывается при наличии данных). `TeacherAnalyticsTest` +1 (агрегация+скоуп+дедуп студента) = 5/5.

**Нужны решения/доступы (@DECIDE / @DO):**
- Zoom **Server-to-Server OAuth app** — вы создаёте, даёте `account_id` + `client_id` + `client_secret` (в `.env`); в коде секретов нет.
- Записи: **вебхук** (нужен публичный эндпоинт + секрет, как у magnet-вебхуков) **vs поллинг**. Рекомендую вебхук (паттерн уже есть).
- Объём MVP: только Фаза 1 (автосоздание + кнопка в кабинете) или сразу с записями (Фаза 2).
- Оценка: Фаза 1 ≈ 1 ветка (миграция + ZoomService + хук в ScheduleResource + кнопка + тесты на ZoomService с `Http::fake`); Фаза 2 — отдельная ветка.
- [ ] **Продуктовые решения** (не автономно, @DECIDE): судьба геймификации праны (`PRANA_ROADMAP.md` vs скидочная прана в проде); модель вознаграждения рефералки; видеоплатформа для вебинаров (issue #78).

## 🚧 Current Work-In-Progress (WIP)

- **Открытых PR нет — вся очередь сессии смержена в `main` (9b28abe, 2026-06-26).** Сессия 2026-06-25/26: #172/#179 (PAID_STATUSES → `scopePaid()`), #173 (VK-IDOR → `vk_auth_token`), #177 (next-lesson в карточке), #180 (prana:decay в расписание), **#184/#186/#189 (редактор лекций v2 — Phase A/B/C)**, **#194/#195/#197/#198 (вебинары Zoom Фаза 1/2/3 + посещаемость в аналитике — issue #78 полностью закрыт)**, плюс параллельная работа коллеги (#174–#178/#181/#183/#185/#187/#196 — каталог Arzamas/synchronize, composer.lock socialite, VK-binding тест, schedule-блоки, docs-маркетинг).
- **NB деплой (см. GTD-хаб Uprava):** #173 → `php artisan migrate` (`users.vk_auth_token`); Zoom Фаза 1/2/3 (#194/#195/#197) → `php artisan migrate` (zoom-поля + `webinar_attendances`) + S2S OAuth app (`ZOOM_*`) + Event Subscription на `/api/webhooks/zoom` с `ZOOM_WEBHOOK_SECRET` (события `recording.completed` + `meeting.participant_joined/left`); редактор лекций v2 → Horizon-воркер на очереди `lectures` для настоящего async (иначе инлайн); `PRANA_DECAY_ENABLED=true` только при желании включить сгорание.
- #157/#158 смержены 2026-06-24; ветка `fix/bot-webhook-signatures` удалена.

## 🧠 Dev Notes & Hypotheses

- **ЛОКАЛЬНЫЕ ТЕСТЫ РАБОТАЮТ:** XAMPP php CLI с включёнными `intl/zip/gd` в `C:\xampp\php\php.ini`. Запуск: `/c/xampp/php/php.exe artisan test [--filter=...]` (в bash `php` не на PATH — звать полным путём или через PowerShell). `.env` из `.env.example` + `key:generate` (gitignored). `openssl_pkey_new` на Windows падает без openssl.cnf — тестовые RSA-ключи фиксируем константой, не генерим в рантайме.
- **ВАЖНО — фронт нужно собрать перед тестами:** layout'ы (`layouts/promo.blade.php`, `layouts/articles.blade.php`) используют `@vite`. Без `public/build/manifest.json` директива бросает исключение → **500** на homepage/лендингах. Это и есть причина «фейлов» `ExampleTest` / `LandingListingVisibilityTest` / `CourseStreamsBlockTest` — НЕ seed-данные и НЕ регрессия (раньше в .ai_state была ошибочная гипотеза про seed). Лечится один раз: `npm install && npm run build` (оба artifact'а gitignored, не коммитим). CI собирает ассеты сам. После сборки полный прогон зелёный.
- **Cast-ловушка `User`:** datetime/integer-атрибуты (`last_activity_at`, `last_login_at`, `*_connected_at` и т.п.) приходят **строками** — метод `User::casts()` в этой версии Laravel не применяется (касты надо было бы в свойстве `$casts`). Поэтому весь код (UserResource, новый `StuckStudentsReport`) оборачивает их в `Carbon::parse()` / `(int)` вручную. Не полагаться на то, что `$user->last_activity_at` — это Carbon.
- **«Оплачено» = `['paid','success']`**, не только `'paid'`. Канон `Payment::PAID_STATUSES`/`scopePaid()` (#104). #108 свёл константы; остались инлайн-литералы (см. очередь).
- **CodeQL не умеет PHP.** `Analyze (php)` падал на КАЖДОМ PR («Did not recognize languages: php»). Исправлено в #107 (php→только javascript). Свип сиблингов: владелец уже починил большинство upstream 2026-06-02; реально остались RuWritingStyles (PR #14) + csl-websanlexicon (PR #62). **Урок:** мой grep по локальным рабочим деревьям ≠ origin/<default> (многие клоны устарели / трекают не ту ветку — нужен `git pull`).

## ✅ Completed (Recent only)

- 2026-06-26: **Посещаемость вебинаров в TeacherAnalytics смержена (#198)** — `TeacherAnalytics::webinarAttendance()` агрегирует `webinar_attendances` по курсам преподавателя (attendees/known/avg_minutes, дедуп студента по user_id) + секция-таблица «Посещаемость вебинаров» на странице аналитики. `TeacherAnalyticsTest` 5/5. **→ вся Zoom-фича (#78) end-to-end: создание→запись→посещаемость→аналитика.**
- 2026-06-26: **Вебинары Zoom Фаза 3 смержена (#197) — issue #78 закрыт (все 3 фазы).** `webinar_attendances` + `WebinarAttendance`; `ZoomWebhookController` обрабатывает `meeting.participant_joined/left` → посещаемость (match по email, гость = user_id NULL, идемпотентно по `participant_uuid`); колонка «Участников» в `ScheduleResource`. `ZoomAttendanceTest` 5/5. Данные готовы под TeacherAnalytics-воронку (интеграция — отдельный шаг).
- 2026-06-26: **Вебинары Zoom Фаза 2 смержена (#195)** — `ZoomWebhookController` (`POST /api/webhooks/zoom`): подпись `x-zm-signature` (enforce-if-configured), `endpoint.url_validation` челлендж, `recording.completed` → запись на `Schedule` по `zoom_meeting_id` + идемпотентный урок (`video_url`=запись). Миграция recording-полей, `services.zoom.webhook_secret`, экшен «Открыть запись». `ZoomWebhookTest` 8/8, suite Zoom|Schedule|Webhook 85/85. Деплой = Event Subscription + `ZOOM_WEBHOOK_SECRET` + migrate. Фаза 3 (посещаемость) — отдельно.
- 2026-06-26: **Вебинары Zoom Фаза 1 смержена (#194)** — `ZoomService` (S2S OAuth), миграция zoom-полей `schedules`, экшен «Создать Zoom-встречу» в `ScheduleResource` (join_url зеркалится в `link` → кабинетная кнопка «Подключиться» без правок вьюх), хост-ссылка readonly препод. `ZoomServiceTest` 5/5 + `ZoomScheduleActionTest` 2/2. Dormant без кредов; деплой = Zoom S2S app + `ZOOM_*` в `.env` + migrate. Фаза 2/3 (записи/посещаемость) — отдельные ветки.
- 2026-06-26: **Редактор лекций v2 — A/B/C смержены** — #184 (async-пайплайн: очередь `lectures`, `meta.processing`, поллинг-виджет), #186 (структурное редактирование: `move/delete_block`, `split/merge_para` + тулбары в `lecture-editor.js`), #189 (advisory-лок + откат к бэкапу). Тесты: Lecture-сьют 29/29. GTD-хаб Uprava обновлён (что осталось — креды/решения).
- 2026-06-25/26: **сессия merged** — #172/#179 (PAID_STATUSES), #173 (VK-IDOR), #177 (next-lesson в карточке курса), #180 (prana:decay в расписании). Параллельно коллега: #174–#178/#181/#183/#185/#187 (каталог Arzamas/synchronize, composer.lock socialite, VK-binding тест, schedule-блок-1).
- 2026-06-24: **#158 merged** — security: проверка подписи легаси бот-вебхуков Telegram/VK (`VerifyTelegramBotWebhook`/`VerifyVkBotWebhook`, fail-open при пустом секрете, VK confirmation в whitelist). Извлечено из устаревшей ветки `fix/bot-webhook-signatures` (отставала на 104 коммита) и перенесено на актуальный `main`; конфликт в `config/services.php` разрешён сохранением обоих ключей. Тест `BotWebhookSignatureTest` 7/7.
- 2026-06-24: **#157 merged** — README обогащён (архитектура/доменная модель, санскритская педагогика, интеграции/вебхуки, роадмап P0/P1/P2) + новый [docs/onboarding-student.md](docs/onboarding-student.md) (русский онбординг студента).
- 2026-06-24: **Полный прогон зелёный** — 472 passed (1165 assertions) после `npm run build` (см. Dev Notes про `@vite`).
- 2026-06-23 (PR-номера в git log): **#108 / #109 / #110 merged** — консолидация `PAID_STATUSES`; тест Tochka-вебхука (подпись+идемпотентность); тесты лояльности + сборки цены чекаута + фикс SQLite-миграции. Все 3 ранее непокрытых денежных пути закрыты.
- 2026-06-13: **#104 merged** — фикс латентного бага доступа (success-платёж не открывал урок): канон `PAID_STATUSES`/`scopePaid`, 3 гейта, тесты `LessonAccessTest`/`PromoCodeTest`/`GrantAccessTest` (все зелёные локально).
- 2026-06-13: **#105 merged** — README приведён к факту + заполнен `.ai_state.md`.
- 2026-06-13: **#106 merged** — security: throttle `/login` + запрет перезаписи чужого `vk_id`.
- 2026-06-13: **#107 merged** — CI: убран неподдерживаемый `php` из матрицы CodeQL.
- 2026-06-13: **Dependabot** — #84/#85/#86/#87 все смержены.
- 2026-06-13: **CodeQL-свип сиблингов** — RuWritingStyles #14, csl-websanlexicon #62 (остальные уже починены upstream).
