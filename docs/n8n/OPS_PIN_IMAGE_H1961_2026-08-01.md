# Ops note — pin n8n Docker image (H1961)

_Created: 01-08-2026 · Last updated: 01-08-2026_

**Host:** `193.232.229.91` · `samskrtam50` · `/opt/n8n` · UI `https://context-ai.ru`  
**Executor:** Grok 4.5 (`grok-4.5`) · handoff [H1961](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1961-Grok_Systema-Sanscriticum_n8n-hygiene-pin-image_30.07.26.md)  
**Finding closed:** credential audit **C07** (was 🟠 unpinned `n8nio/n8n:latest`)

## Goal (met)

| Check | Result |
|---|---|
| compose has non-`:latest` pin for n8n | **yes** — version + digest |
| n8n UI / healthz up | **200** on `http://127.0.0.1:5678/healthz` after recreate |
| 5 Active workflows still listed | **yes** (same five as pre-pin) |

## Pin (live)

| Field | Value |
|---|---|
| Compose path | `/opt/n8n/docker-compose.yml` |
| Image line | `n8nio/n8n:2.27.5@sha256:d53243d06c7f7de81910ac922ff55ed4b58c9c3c761d7f2f8443d0567990def3` |
| Runtime version | `2.27.5` (`n8n --version` in container) |
| Was | `n8nio/n8n:latest` (same digest content at pin time) |
| Caddy | still `caddy:latest` (out of H1961 scope) |
| Host backup | `/opt/n8n/backups/h1961/docker-compose.yml.pre-20260731T215131Z` |
| Host rollback note | `/opt/n8n/backups/h1961/ROLLBACK.md` |

## Active workflows after pin (COUNT=5)

| id | name |
|---|---|
| `GGs0G2azzkLqLbJj` | Lecture clip extract (H1452) — ffmpeg + VK + Laravel callback |
| `1EIqqNzMl5NNIxST` | ZOOM 1.4 (Final) + АДМИНКА ТЕСТ |
| `XWQHAwlxBAFe6xfj` | АДМИНКА+ТАБЛИЦА ОПЛАТ |
| `b2LCmGsT8gKcQV6t` | Ютуб список |
| `egCkjS06dYtYcnmv` | ловим названия copy |

## What was done

1. Preflight: `docker ps`, image `RepoDigests`, active count via host `sqlite3` on `/opt/n8n/storage/database.sqlite`, healthz.
2. Backup compose under `/opt/n8n/backups/h1961/`.
3. `docker tag n8nio/n8n:latest n8nio/n8n:2.27.5` (label currently running image).
4. Edit compose: replace `image: n8nio/n8n:latest` with version+digest pin above.
5. Staged recreate: `cd /opt/n8n && docker compose up -d n8n` (caddy left running).
6. Smoke: healthz 200 (~5–10 s after start); version 2.27.5; active COUNT=5 unchanged.

**Not done (optional / risky):** live ZOOM dry execution — dual Zoom webhooks + money-adjacent sibling Active; would touch production event paths. Image pin is independent of workflow graphs.

## Rollback

```sh
cd /opt/n8n
cp /opt/n8n/backups/h1961/docker-compose.yml.pre-20260731T215131Z docker-compose.yml
docker compose up -d n8n
# verify: curl -sS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5678/healthz
# verify: docker exec n8n-n8n-1 n8n --version
```

To move forward deliberately later: pull a chosen newer tag, pin **that** digest in compose, recreate, re-smoke the five Active.

## Residual / next hygiene

| Item | Hand-off / note |
|---|---|
| Pin `caddy:latest` | optional follow-up; not H1961 |
| Prune binary storage ~5.6G | 🟢 [H1962](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1962-Grok_Systema-Sanscriticum_n8n-hygiene-prune-storage_30.07.26.md) done 01-08-2026 — [OPS note](https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/n8n/OPS_PRUNE_STORAGE_H1962_2026-08-01.md) (6.6G→1.4G) |
| Archive-tag inactive workflows | [H1963](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1963-Grok_Systema-Sanscriticum_n8n-hygiene-archive-tags_30.07.26.md) |

_Dr. Mārcis Gasūns_
