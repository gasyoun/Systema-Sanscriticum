# Reactivation wave A — dry-run report (H5288, 23-09-2026)

_Created: 23-09-2026 · Last updated: 23-09-2026_

Сухой прогон когорты реактивационной волны A: **отправлено 0 сообщений**.
Команда: `php artisan reactivation:wave-a-dry-run` (только чтение БД, пишет отчёт
+ PII-send-list в `storage/app/reactivation/`, в git не попадает). Отправка —
волна B (H5294), только после явного «да» MG на этот отчёт.

## Живой прогон на проде (23-09-2026, read-only)

| Сегмент | Людей |
|---|---|
| Non-continuers (платил, текущий блок не куплен — DebtorsReport `not_renewed`) | 149 |
| Lapsed pool (реальный плательщик без действующей группы) | 2 |
| **Итого когорта** | **151** |

Каналы (первичный: бот Telegram при chat id + согласии, иначе email):

| Канал | Людей |
|---|---|
| Бот Telegram | 13 |
| Email fallback | 134 |
| Нет канала | 4 |

Сегмент × канал: non-continuers 12 tg / 133 email / 4 none; lapsed 1 tg / 1 email / 0 none.

Исключения:

| Исключение | Людей |
|---|---|
| Активные студенты (в forming/active группе) | 659 |
| Только-возвраты (реальной оплаты не было) | 10 |
| Исключён/покинул по всем курсам | 117 |
| Email opt-out / suppression | 4 |

## Дельта к источникам (563 + 919) — главный вывод

Срезы 25–29-08 ([BUSINESS_HEALTH_ASSESSMENT_2026-09-14.md](https://github.com/gasyoun/Uprava/blob/main/reports/BUSINESS_HEALTH_ASSESSMENT_2026-09-14.md)) считали
563 lapsed + 919 non-continuers **исторически** (все непродлённые блоки за всё
время, включая студентов действующих групп). Живой запрос волны A намеренно
консервативен и даёт 151:

1. **659 «активных»** — плательщики, числящиеся в forming/active группах. Им
   адресован существующий тёплый поток (DebtorsReport → напоминания куратору,
   Filament «Реактивация»), а не холодная волна. Большая часть дельты 919→149
   живёт здесь.
2. **117 исключённых/покинувших** — вычтены поимённо (в срезах 25–29-08 могли
   оставаться).
3. **Lapsed-пул 563→2** — почти весь пул Uprava-переписи (56 VIP + 507) — это
   плательщики, чьи группы давно в архиве, но чьё членство в `group_user` живо
   (`left_at IS NULL`) при архивной группе. Текущее определение волны A берёт
   только «совсем без группы»; расширение до «нет НИ одной forming/active
   группы» (т.е. архив разрешён) — вопрос к MG перед волной B.

**Решение за MG (волна B):** гнать волну по 151, либо расширить lapsed-пул до
«payer без forming/active группы» (архивные группы разрешить) и/или включить
часть 659 как отдельный тёплый сегмент. Команда пересчитывается за секунды —
любой вариант определения даётся одной правкой `ReactivationWaveCohort`.

## Шаблоны (два, вокруг трёх лифтов; без срочности)

Засеяны в `MessageTemplateSeeder`, категория reactivation; тексты — черновик,
MG правит перед волной B:

1. `/реактивация-волна-а · записи и свой темп` — занятия в записи, свой темп,
   догнать без спешки (лифт +5).
2. `/реактивация-волна-а · рассрочка` — платёж делится на части, персональный
   расчёт с учётом прежних оплат (лифт +8/+9).

Срочность (−7) и соц-доказательство (−6) не используются — win/loss-грунт в
`app/Enums/ReactivationTemplate.php`.

## Проверки

- `./vendor/bin/phpunit --filter ReactivationWaveADryRunTest` — PASS (28
  assertions): сегменты, каналы, исключения, PII вне отчёта, Mail/Queue пусты,
  `DebtWinBackAttempt` = 0 (гарантия «отправки нет»).
- Живой прогон: скрипт-обёртка на проде (read-only, counts-only, /tmp, удалён
  после) — числа выше.

_Гасунс_
