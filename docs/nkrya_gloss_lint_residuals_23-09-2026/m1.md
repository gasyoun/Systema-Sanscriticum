_Created: 23-09-2026 · Last updated: 23-09-2026_

# H5285 residual m1 — mission text for mint_handoff.py

## Mission
Finish the NKRYa frequency-band pass that H5285 started, then cut the next review sheet.

Parent: H5285 (NKRYa gloss lint for Russian beginner SRS decks). Report: https://github.com/gasyoun/Systema-Sanscriticum/blob/main/docs/NKRYA_GLOSS_LINT_BEGINNER_SRS_REPORT_23-09-2026.md

1. In a Systema-Sanscriticum worktree off origin/main, on a box whose keychain holds service `ruscorpora-api` (the Mac; or env `RUSCORPORA_API_TOKEN`), and with a sibling `SanskritLexicography` checkout (for `RussianTranslation/src/nkrya_client.py`), run unattended: `python3 scripts/nkrya_gloss_lint.py lemmas roots` (live, resumable). About 880 lookups remain; the key allows about 60 an hour per account, so this takes about 15 h. First run `ps aux | grep -i nkrya` — any other NKRYa consumer halves the rate.
2. Commit progress every few hours: `python3 scripts/nkrya_gloss_lint.py lemmas roots --offline` harvests the raw cache (`storage/app/nkrya_cache`, gitignored) into the committed `resources/data/nkrya_lint/nkrya_evidence_cache.tsv` and regenerates the lint TSVs.
3. When `offline_misses` reaches 0: for every new `rare` row without `synonym_proposal`, add an agent rewording to `resources/data/nkrya_lint/reword_proposals.tsv` (band it first with `--measure "<wording>"`), then `python3 scripts/nkrya_gloss_lint_sheet.py` with a new SHEET_ID. Cards: 10 or fewer. Pass `Uprava/tools/sheet_evidence_gate.py --html` and `vote_readiness_gate.py`, publish to gasyoun.github.io/vote/sheets/, and add REVIEW_SHEETS_INDEX + GTD rows.
4. Update the report's Findings table with the complete numbers.

Acceptance: `offline_misses: 0` on the offline run; evidence cache committed; the next sheet published (or a written "no new rare rows" verdict in the report).

_Гасунс_
