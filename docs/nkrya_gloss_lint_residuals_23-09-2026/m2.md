_Created: 23-09-2026 · Last updated: 23-09-2026_

# H5285 residual m2 — mission text for mint_handoff.py

## Mission
Apply the mechanical dictionary-form fixes the H5285 NKRYa gloss lint found, in the UPSTREAM sources (never in Systema's generated seeds).

Parent: H5285. Evidence: https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/data/nkrya_lint/roots_gloss_lint.tsv and https://github.com/gasyoun/Systema-Sanscriticum/blob/main/resources/data/nkrya_lint/lemmas_gloss_lint.tsv — rows flagged `inflected` (not `inflected_ambiguous`) carry the proposed form in `gloss_lemma`. Grill 23-09-2026 Q2: mechanical, reversible, no vote.

1. Roots (382 rows, e.g. kṛ «сделал» -> «сделать»): the gloss comes from WhitneyRoots `crosswalk/ru_root_glosses.tsv` column `gloss_ru_1`, read by Systema `database/seeders/data/build_roots_frequency_ru.py`. Fix `gloss_ru_1` in WhitneyRoots (PR), then rebuild `roots_frequency_ru.tsv` in Systema with the builder (PR).
2. Lemmas (96 rows, e.g. «сближении» -> «сближение»): the gloss comes from kosha `reading/data/hitopadesa-0.json` (pinned by Systema `resources/data/cohort_start_chteniya/MANIFEST.json`, vendored by `scripts/vendor_cohort_start_chteniya_packs.py`). Fix in kosha (PR), re-pin + re-vendor in Systema (PR).
3. The 14 `inflected_ambiguous` rows: resolve by the Sanskrit part of speech (verb root -> infinitive, noun -> nominative); list each decision in the PR body.
4. Re-run `python3 scripts/nkrya_gloss_lint.py lemmas roots --offline` in Systema: `flag:inflected` should drop to ~0.

Acceptance: upstream PRs merged, Systema seeds regenerated, the offline lint shows `inflected` <= 5 on both files.

_Гасунс_
