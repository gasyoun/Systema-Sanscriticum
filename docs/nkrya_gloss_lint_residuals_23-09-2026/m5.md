_Created: 23-09-2026 · Last updated: 23-09-2026_

# H5285 residual m5 — mission text for mint_handoff.py

## Mission
Run the H5285 NKRYa gloss lint over the remaining Russian-gloss decks: the Memrise teacher decks and the corpus glossary; the Бюлер dictionary is a later wave (grill 23-09-2026 Q5).

Parent: H5285. Tool: `Systema-Sanscriticum/scripts/nkrya_gloss_lint.py --tsv database/seeders/data/memrise_<ID>/level_*.csv --gloss-col col_b --id-cols col_a --sa-col col_a --name memrise_<ID>` (the /anki-srs-import Phase 3-bis command) and a preset for `resources/data/sa_ru_glossary.json` (field `g`, top-3 RU glosses per IAST lemma) — add the preset.

1. Memrise decks are teacher decks: flag-only (grill Q3) — the teacher's wording stays; a modern hint only after a vote.
2. The glossary is ours: `rare` glosses there propose swaps normally.
3. Rate limit ~60 NKRYa calls/h per account, shared by every session: run unattended, commit the evidence cache, resume with `--offline`.
4. Cut review sheets of 10 cards or fewer; REVIEW_SHEETS_INDEX + GTD rows.

Acceptance: lint TSVs for every memrise_* deck and the glossary; sheets published for the swap candidates (or written "none" verdicts).

_Гасунс_
