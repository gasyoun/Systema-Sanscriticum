[Service]
# MANAGED FILE — scripts/server_guards_apply.sh. Числа — из scripts/server_guards.conf.
#
# Авария 28.07.2026: завиcший telegram-support:sync копил процессы schedule:run
# (~300 МБ/мин) и за ~20 минут занял весь лимит LXC-контейнера (8 ГиБ, свопа нет) —
# контейнер замер на ~20 часов. Потолок на поддерево cron: при повторении ядро
# убьёт жирный процесс ВНУТРИ этой группы, а сайт/БД/очередь продолжат работать.
#
# ЭТО ЕДИНСТВЕННЫЙ файл, задающий числа памяти для cron.service. Второй drop-in
# с теми же директивами дал бы эффективное значение по сортировке имён, а не по
# замыслу — guards:verify ловит это как single-definition-нарушение.
MemoryHigh=@@CRON_MEMORY_HIGH@@
MemoryMax=@@CRON_MEMORY_MAX@@
