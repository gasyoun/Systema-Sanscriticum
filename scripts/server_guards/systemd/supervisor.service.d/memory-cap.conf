[Service]
# MANAGED FILE — scripts/server_guards_apply.sh. Числа — из scripts/server_guards.conf.
#
# Тот же предохранитель (см. cron.service.d/memory-cap.conf) для horizon/reverb:
# воркеры Horizon перезапускаются сами, OOM-kill внутри группы безопаснее,
# чем замёрзший контейнер. Единственный файл с числами памяти для supervisor.service.
MemoryHigh=@@SUPERVISOR_MEMORY_HIGH@@
MemoryMax=@@SUPERVISOR_MEMORY_MAX@@
