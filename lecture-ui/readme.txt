Начать с перерабоки скрипта для генерации json для html

sgi_01.json (WhisperX)
        ↓
1. Исправить артефакты (fix_transcript.py)
        ↓
2. Сгруппировать сегменты в абзацы
   (по паузам между репликами одного спикера)
        ↓
3. Каждый абзац = {текст + таймкод начала}
        ↓
4. Разбить на разделы (по таймкодам из YouTube/вручную)
        ↓
5. Сгенерировать JSON для build.py
        ↓
sgi_01_lecture.json


ИИ нужен на этапе: 
1) Генерация заголовков H2 и H3. (H1- Название лекции)
2)  Подстановка тайм кодов для заголовков из субтитров YT.
3) Правка стенограммы (минимум участия).
4) Подстановка слайдов в нужно место доклада.


Правила подготовки JSON из структурированного текста:
dialog — чередование реплик разных ролей
text — [Лекция]
interjection — одиночная реплика другой роли внутри длинного монолога
figure — <figure><img N>...

json_to_txt.py - обратный конвертер из JSON в структурированный текст
Для pdf_to_jpg.py вначале pip install pymupdf

make_text_from_json_dg.py
— единый флаг --para включает вывод §N везде. (для отладки)